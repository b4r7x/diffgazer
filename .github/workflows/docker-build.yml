name: Docker Build

on:
  pull_request:
    paths:
      - .dockerignore
      - Dockerfile
      - deploy/**
      - scripts/**
      - apps/docs/**
      - apps/landing/**
      - libs/**
      - cli/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - turbo.json
      - .github/workflows/docker-build.yml
  push:
    branches:
      - main
    paths:
      - .dockerignore
      - Dockerfile
      - deploy/**
      - scripts/**
      - apps/docs/**
      - apps/landing/**
      - libs/**
      - cli/**
      - package.json
      - pnpm-lock.yaml
      - pnpm-workspace.yaml
      - turbo.json
      - .github/workflows/docker-build.yml

concurrency:
  group: docker-build-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build ${{ matrix.image }} image
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        include:
          - image: docs
            dockerfile: Dockerfile
          - image: registry
            dockerfile: deploy/registry.Dockerfile
          - image: landing
            dockerfile: deploy/landing.Dockerfile
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Build image
        run: docker build --pull -f "${{ matrix.dockerfile }}" -t "diffgazer-${{ matrix.image }}:ci" .

      # Same severity gate deploy.yml applies to the pushed digest, run here on the
      # image this change set produces. Without it a Dependabot base-image bump merges
      # green and a CRITICAL/HIGH regression first surfaces inside a manual production
      # deploy instead of on the PR that introduced it.
      - name: Scan built image
        uses: aquasecurity/trivy-action@915b19bbe73b92a6cf82a1bc12b087c9a19a5fe2 # v0.28.0
        with:
          image-ref: diffgazer-${{ matrix.image }}:ci
          format: table
          exit-code: 1
          severity: CRITICAL,HIGH
