---
"@diffgazer/add": patch
---

Audit pass on the `dgadd` CLI. Refactors the add/init/remove/diff commands, the registry transform and namespace handling, CSS-chunk extraction, and the keys copy-bundle integration. Behavior-preserving cleanup and bug fixes to the install paths.

Reconstructed retroactively from the post-0.1.1 history; these changes predate the changeset-based flow.
