{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "panel",
  "title": "Panel",
  "description": "Card-like container with composable header, title, description, content, row, and footer primitives",
  "dependencies": [
    "class-variance-authority"
  ],
  "registryDependencies": [
    "https://r.b4r7.dev/r/ui/theme.json",
    "https://r.b4r7.dev/r/ui/utils.json"
  ],
  "files": [
    {
      "path": "registry/ui/panel/index.ts",
      "content": "\"use client\";\nimport { Panel as PanelRoot, type PanelProps } from \"./panel\";\nimport { PanelHeader, type PanelHeaderProps } from \"./panel-header\";\nimport { PanelTitle, type PanelTitleProps } from \"./panel-title\";\nimport { PanelDescription, type PanelDescriptionProps } from \"./panel-description\";\nimport {\n  PanelContent,\n  panelContentVariants,\n  type PanelContentProps,\n} from \"./panel-content\";\nimport { PanelFooter, type PanelFooterProps } from \"./panel-footer\";\nimport { PanelRow, type PanelRowProps } from \"./panel-row\";\n\nconst Panel = Object.assign(PanelRoot, {\n  Header: PanelHeader,\n  Title: PanelTitle,\n  Description: PanelDescription,\n  Content: PanelContent,\n  Row: PanelRow,\n  Footer: PanelFooter,\n});\n\nexport { Panel, type PanelProps };\nexport { PanelHeader, type PanelHeaderProps };\nexport { PanelTitle, type PanelTitleProps };\nexport { PanelDescription, type PanelDescriptionProps };\nexport { PanelContent, type PanelContentProps, panelContentVariants };\nexport { PanelFooter, type PanelFooterProps };\nexport { PanelRow, type PanelRowProps };\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel.tsx",
      "content": "\"use client\";\n\nimport {\n  Children,\n  isValidElement,\n  useId,\n  useMemo,\n  type ComponentPropsWithRef,\n  type ElementType,\n  type ReactNode,\n} from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { PanelContext, type PanelContextValue } from \"./panel-context\";\nimport { PanelTitle } from \"./panel-title\";\nimport { PanelDescription } from \"./panel-description\";\n\nexport type PanelElement = \"div\" | \"article\" | \"section\" | \"aside\";\n\n/** All visual chrome (frame, tone, density) is driven by data-attributes in\n *  shared/panel.css; no class names are emitted from the root. */\nexport type PanelFrame = \"hairline\" | \"rail\" | \"viewfinder\" | \"surface\";\nexport type PanelTone = \"info\" | \"success\" | \"warning\" | \"error\" | \"accent\";\nexport type PanelDensity = \"default\" | \"compact\";\n\ninterface PanelOwnProps {\n  frame?: PanelFrame;\n  tone?: PanelTone;\n  density?: PanelDensity;\n}\n\nexport type PanelProps<T extends PanelElement = \"div\"> = Omit<\n  ComponentPropsWithRef<T>,\n  keyof PanelOwnProps | \"as\"\n> &\n  PanelOwnProps & {\n    as?: T;\n  };\n\n/**\n * Card-like container. Composes `Panel.Header`, `Panel.Title`,\n * `Panel.Description`, `Panel.Content`, `Panel.Row`, and `Panel.Footer`.\n *\n * Accessibility:\n *  - If `Panel.Title` is in the subtree OR `aria-label`/`aria-labelledby` is\n *    supplied, the root renders as `<section>` (a landmark) with\n *    `aria-labelledby` auto wired to the title's id.\n *  - Otherwise the root renders as a plain `<div>` (no nameless landmark).\n *  - `Panel.Description` is auto-wired via `aria-describedby` when present.\n *\n * Subcomponent detection is a synchronous child walk (mirrors `DialogContent`),\n * so the first commit already carries the correct element type and ARIA\n * relationships — no remount on second render, no a11y gap on initial paint.\n *\n * `tone` is a purely visual border-color tint. Status messaging (icon,\n * dismissable, live region, role=alert) belongs in `Callout`, not Panel.\n */\nexport function Panel<T extends PanelElement = \"div\">(props: PanelProps<T>) {\n  const {\n    as,\n    ref,\n    className,\n    frame,\n    tone,\n    density,\n    children,\n    \"aria-label\": ariaLabel,\n    \"aria-labelledby\": ariaLabelledBy,\n    ...rest\n  } = props as PanelProps<PanelElement>;\n\n  const id = useId();\n  const titleId = `${id}-title`;\n  const descriptionId = `${id}-description`;\n\n  const contextValue = useMemo<PanelContextValue>(\n    () => ({ titleId, descriptionId }),\n    [titleId, descriptionId],\n  );\n\n  const hasRenderableTitle = containsPanelTitleElement(children);\n  const hasRenderableDescription = containsPanelDescriptionElement(children);\n  const hasAriaName = isNonEmptyString(ariaLabel) || isNonEmptyString(ariaLabelledBy);\n  const isNamedRegion = hasRenderableTitle || hasAriaName;\n\n  const resolvedFrame = frame ?? \"hairline\";\n  const resolvedDensity = density ?? \"default\";\n  const Tag = (as ?? (isNamedRegion ? \"section\" : \"div\")) as ElementType;\n\n  const accessibleName = resolvePanelAccessibleName({\n    ariaLabel,\n    ariaLabelledBy,\n    titleId,\n    hasRenderableTitle,\n  });\n  const resolvedAriaDescribedBy = hasRenderableDescription ? descriptionId : undefined;\n\n  return (\n    <PanelContext value={contextValue}>\n      <Tag\n        {...rest}\n        ref={ref}\n        data-slot=\"panel\"\n        data-frame={resolvedFrame}\n        data-tone={tone ?? undefined}\n        data-density={resolvedDensity}\n        aria-label={accessibleName[\"aria-label\"]}\n        aria-labelledby={accessibleName[\"aria-labelledby\"]}\n        aria-describedby={resolvedAriaDescribedBy}\n        className={cn(className)}\n      >\n        {resolvedFrame === \"viewfinder\" ? (\n          <span aria-hidden=\"true\" data-slot=\"panel-corners\">\n            <span className=\"vf-tl\" />\n            <span className=\"vf-tr\" />\n            <span className=\"vf-bl\" />\n            <span className=\"vf-br\" />\n          </span>\n        ) : null}\n        {children}\n      </Tag>\n    </PanelContext>\n  );\n}\n\nfunction isNonEmptyString(value: unknown): value is string {\n  return typeof value === \"string\" && value.trim().length > 0;\n}\n\n/**\n * Resolves the panel's accessible name following ARIA precedence:\n * explicit `aria-labelledby` > explicit `aria-label` > `<Panel.Title>` id.\n * Exactly one of `aria-label` / `aria-labelledby` is set; the other is undefined.\n * Mirrors Dialog's `resolveAccessibleName` at dialog-content.tsx:89-105.\n */\nfunction resolvePanelAccessibleName({\n  ariaLabel,\n  ariaLabelledBy,\n  titleId,\n  hasRenderableTitle,\n}: {\n  ariaLabel: string | undefined;\n  ariaLabelledBy: string | undefined;\n  titleId: string;\n  hasRenderableTitle: boolean;\n}): { \"aria-label\": string | undefined; \"aria-labelledby\": string | undefined } {\n  if (isNonEmptyString(ariaLabelledBy)) {\n    return { \"aria-label\": undefined, \"aria-labelledby\": ariaLabelledBy };\n  }\n  if (isNonEmptyString(ariaLabel)) {\n    return { \"aria-label\": ariaLabel, \"aria-labelledby\": undefined };\n  }\n  if (hasRenderableTitle) {\n    return { \"aria-label\": undefined, \"aria-labelledby\": titleId };\n  }\n  return { \"aria-label\": undefined, \"aria-labelledby\": undefined };\n}\n\nfunction containsPanelTitleElement(children: ReactNode): boolean {\n  return Children.toArray(children).some((child) => {\n    if (!isValidElement<{ children?: ReactNode }>(child)) return false;\n    if (child.type === PanelTitle) return true;\n    return containsPanelTitleElement(child.props.children);\n  });\n}\n\nfunction containsPanelDescriptionElement(children: ReactNode): boolean {\n  return Children.toArray(children).some((child) => {\n    if (!isValidElement<{ children?: ReactNode }>(child)) return false;\n    if (child.type === PanelDescription) return true;\n    return containsPanelDescriptionElement(child.props.children);\n  });\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-context.tsx",
      "content": "\"use client\";\n\nimport { createContext, useContext } from \"react\";\n\nexport interface PanelContextValue {\n  titleId: string;\n  descriptionId: string;\n}\n\nexport const PanelContext = createContext<PanelContextValue | undefined>(undefined);\n\nexport function usePanelContext(): PanelContextValue {\n  const context = useContext(PanelContext);\n  if (!context) {\n    throw new Error(\"Panel compound components must be used within a Panel\");\n  }\n  return context;\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-header.tsx",
      "content": "\"use client\";\n\nimport { Children, isValidElement, type HTMLAttributes, type ReactNode } from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { PanelTitle } from \"./panel-title\";\nimport { PanelDescription } from \"./panel-description\";\n\nexport interface PanelHeaderProps extends HTMLAttributes<HTMLDivElement> {\n  /**\n   * \"bar\" renders a 4px foreground accent bar to the left of the title and\n   * description column, matching Dialog's `marker=\"bar\"`. Color follows\n   * `--panel-tone` (tone-tinted when the panel has a tone).\n   * \"none\" suppresses the bar; useful for `frame=\"rail\"` (the rail is the\n   * marker) or custom header layouts.\n   */\n  marker?: \"bar\" | \"none\";\n}\n\n/**\n * Compound header. Title and Description go in a left column; any other\n * sibling (eyebrow tag, badge, button) lands in a right slot, vertically\n * centered against the title baseline. The marker bar spans the full\n * header height via `align-self: stretch`.\n */\nexport function PanelHeader({\n  className,\n  children,\n  marker = \"bar\",\n  ...props\n}: PanelHeaderProps) {\n  const { bodyChildren, endChildren } = partitionHeaderChildren(children);\n  const hasEnd = endChildren.length > 0;\n\n  return (\n    <div\n      {...props}\n      data-slot=\"panel-header\"\n      data-marker={marker}\n      className={cn(className)}\n    >\n      <div data-slot=\"panel-header-body\">{bodyChildren}</div>\n      {hasEnd ? <div data-slot=\"panel-header-end\">{endChildren}</div> : null}\n    </div>\n  );\n}\n\nfunction isBodyChild(child: ReactNode): boolean {\n  if (!isValidElement(child)) return false;\n  return child.type === PanelTitle || child.type === PanelDescription;\n}\n\nfunction partitionHeaderChildren(children: ReactNode): {\n  bodyChildren: ReactNode[];\n  endChildren: ReactNode[];\n} {\n  const bodyChildren: ReactNode[] = [];\n  const endChildren: ReactNode[] = [];\n  for (const child of Children.toArray(children)) {\n    if (isBodyChild(child)) bodyChildren.push(child);\n    else endChildren.push(child);\n  }\n  return { bodyChildren, endChildren };\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-title.tsx",
      "content": "\"use client\";\n\nimport { type HTMLAttributes } from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { usePanelContext } from \"./panel-context\";\n\nexport interface PanelTitleProps extends HTMLAttributes<HTMLHeadingElement> {\n  as?: \"h2\" | \"h3\" | \"h4\" | \"h5\" | \"h6\";\n}\n\nexport function PanelTitle({ children, className, as: Tag = \"h2\", ...props }: PanelTitleProps) {\n  const { titleId } = usePanelContext();\n\n  return (\n    <Tag\n      {...props}\n      id={titleId}\n      data-slot=\"panel-title\"\n      className={cn(\"m-0 text-sm font-bold leading-tight text-foreground\", className)}\n    >\n      {children}\n    </Tag>\n  );\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-description.tsx",
      "content": "\"use client\";\n\nimport { type HTMLAttributes } from \"react\";\nimport { cn } from \"@/lib/utils\";\nimport { usePanelContext } from \"./panel-context\";\n\nexport interface PanelDescriptionProps extends HTMLAttributes<HTMLParagraphElement> {}\n\n/**\n * Auto-wires `aria-describedby` on the Panel root. The root walks its children\n * at render time to detect `Panel.Description`, so this component just reads\n * the shared id from context and applies it to its rendered element.\n */\nexport function PanelDescription({ className, ...props }: PanelDescriptionProps) {\n  const { descriptionId } = usePanelContext();\n\n  return (\n    <p\n      {...props}\n      data-slot=\"panel-description\"\n      id={descriptionId}\n      className={cn(\"m-0 text-xs leading-normal text-foreground/70\", className)}\n    />\n  );\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-content.tsx",
      "content": "import type { ComponentPropsWithRef } from \"react\";\nimport { cva, type VariantProps } from \"class-variance-authority\";\nimport { cn } from \"@/lib/utils\";\n\n/**\n * Vertical rhythm for direct children of `Panel.Content`. Use `spacing=\"none\"`\n * when composing `Panel.Row` siblings (rows own their own padding and an\n * automatic adjacent-sibling divider).\n *\n * - `none`: no gap (rows / custom layout own their own spacing).\n * - `sm`:   8px gap.\n * - `md`:   16px gap (default — comfortable for prose/form bodies).\n */\nexport const panelContentVariants = cva(\"text-sm text-foreground\", {\n  variants: {\n    spacing: {\n      none: \"\",\n      sm: \"space-y-2\",\n      md: \"space-y-4\",\n    },\n  },\n  defaultVariants: { spacing: \"md\" },\n});\n\nexport type PanelContentProps = ComponentPropsWithRef<\"div\"> &\n  VariantProps<typeof panelContentVariants>;\n\nexport function PanelContent({ className, spacing, ...props }: PanelContentProps) {\n  return (\n    <div\n      {...props}\n      data-slot=\"panel-content\"\n      className={cn(panelContentVariants({ spacing }), className)}\n    />\n  );\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-row.tsx",
      "content": "import type { HTMLAttributes, ReactNode } from \"react\";\nimport { cn } from \"@/lib/utils\";\n\nexport interface PanelRowProps extends Omit<HTMLAttributes<HTMLDivElement>, \"children\"> {\n  label: ReactNode;\n  value: ReactNode;\n}\n\n/**\n * Key-value row primitive. Adjacent `Panel.Row` siblings get an automatic\n * top border via the `[data-slot=\"panel-row\"] + [data-slot=\"panel-row\"]`\n * adjacent-sibling combinator in shared/panel.css.\n */\nexport function PanelRow({ label, value, className, ...props }: PanelRowProps) {\n  return (\n    <div\n      {...props}\n      data-slot=\"panel-row\"\n      className={cn(\"text-xs\", className)}\n    >\n      <span data-slot=\"panel-row-label\" className=\"text-muted-foreground\">\n        {label}\n      </span>\n      <span data-slot=\"panel-row-value\" className=\"text-foreground\">\n        {value}\n      </span>\n    </div>\n  );\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/panel/panel-footer.tsx",
      "content": "import type { ComponentPropsWithRef } from \"react\";\nimport { cn } from \"@/lib/utils\";\n\nexport type PanelFooterProps = ComponentPropsWithRef<\"div\">;\n\nexport function PanelFooter({ className, ...props }: PanelFooterProps) {\n  return (\n    <div\n      {...props}\n      data-slot=\"panel-footer\"\n      className={cn(\"text-xs text-muted-foreground\", className)}\n    />\n  );\n}\n",
      "type": "registry:ui"
    },
    {
      "path": "registry/ui/shared/panel.css",
      "content": "/*\n * Panel primitive styles. Three orthogonal axes drive visual chrome via\n * data-attribute selectors on [data-slot=\"panel\"]:\n *   [data-frame]   = hairline | rail | viewfinder | surface\n *   [data-tone]    = info | success | warning | error | accent\n *   [data-density] = default | compact\n *\n * Frame chrome (border, rail, viewfinder corners, surface) is selected by\n * [data-frame]. Tone tints --panel-tone (inherited by the header marker bar)\n * and the frame's color signal (border-color, rail color, corner color, or\n * surface header tint). Density propagates to header/content/footer padding.\n *\n * Adjacent panel-row siblings get an automatic top border for tabular layouts\n * (the rule consumers most commonly hand-rolled).\n */\n@layer components {\n  [data-slot=\"panel\"] {\n    --panel-bg: var(--background);\n    --panel-fg: var(--foreground);\n    --panel-hairline: color-mix(in oklab, var(--border) 60%, transparent);\n    --panel-tone: var(--foreground);\n\n    position: relative;\n    background: var(--panel-bg);\n    color: var(--panel-fg);\n    border-radius: var(--radius);\n  }\n\n  /* Density rhythm — single source of truth. */\n  [data-slot=\"panel\"][data-density=\"default\"] [data-slot=\"panel-header\"]  { padding: 14px 20px 12px; }\n  [data-slot=\"panel\"][data-density=\"default\"] [data-slot=\"panel-content\"] { padding: 14px 20px; }\n  [data-slot=\"panel\"][data-density=\"default\"] [data-slot=\"panel-footer\"]  { padding: 10px 20px; }\n  [data-slot=\"panel\"][data-density=\"compact\"] [data-slot=\"panel-header\"]  { padding: 10px 14px 8px; }\n  [data-slot=\"panel\"][data-density=\"compact\"] [data-slot=\"panel-content\"] { padding: 10px 14px; }\n  [data-slot=\"panel\"][data-density=\"compact\"] [data-slot=\"panel-footer\"]  { padding: 8px 14px; }\n\n  /* Header. align-items: center so action chips sit on the title's vertical\n     center; the marker bar uses align-self: stretch to span full header height. */\n  [data-slot=\"panel-header\"] {\n    display: flex;\n    gap: 12px;\n    align-items: center;\n  }\n  [data-slot=\"panel-header\"][data-marker=\"bar\"]::before {\n    content: \"\";\n    flex: none;\n    align-self: stretch;\n    width: 4px;\n    background: var(--panel-tone);\n    border-radius: 1px;\n  }\n  [data-slot=\"panel-header\"] > [data-slot=\"panel-header-body\"] {\n    display: flex;\n    flex-direction: column;\n    gap: 4px;\n    min-width: 0;\n    flex: 1 1 auto;\n  }\n  [data-slot=\"panel-header\"] > [data-slot=\"panel-header-end\"] {\n    display: inline-flex;\n    align-items: center;\n    gap: 8px;\n    flex-shrink: 0;\n  }\n\n  /* Footer — flex row with space-between for primary text + meta badge. */\n  [data-slot=\"panel-footer\"] {\n    display: flex;\n    align-items: center;\n    justify-content: space-between;\n    gap: 12px;\n  }\n\n  /* Row primitive — adjacent siblings get the auto-divider. */\n  [data-slot=\"panel-row\"] {\n    display: flex;\n    align-items: center;\n    justify-content: space-between;\n    gap: 12px;\n    padding-block: 6px;\n  }\n  [data-slot=\"panel-row\"] + [data-slot=\"panel-row\"] {\n    border-block-start: 1px solid var(--panel-hairline);\n  }\n\n  /* ============================================================\n     Frame: hairline (default)\n     ============================================================ */\n  [data-slot=\"panel\"][data-frame=\"hairline\"] {\n    border: 1px solid var(--panel-hairline);\n  }\n  [data-slot=\"panel\"][data-frame=\"hairline\"] [data-slot=\"panel-header\"] {\n    border-block-end: 1px solid var(--panel-hairline);\n  }\n  [data-slot=\"panel\"][data-frame=\"hairline\"] [data-slot=\"panel-footer\"] {\n    border-block-start: 1px solid var(--panel-hairline);\n  }\n\n  /* ============================================================\n     Frame: rail — inline-start rail, hairline under header only.\n     The rail IS the marker, so suppress the header bar.\n     ============================================================ */\n  [data-slot=\"panel\"][data-frame=\"rail\"] {\n    border: 0;\n    border-inline-start: 2px solid var(--border);\n    border-radius: 0;\n    padding-block: 2px;\n  }\n  [data-slot=\"panel\"][data-frame=\"rail\"] [data-slot=\"panel-header\"] {\n    border-block-end: 1px solid var(--panel-hairline);\n    padding-inline-start: 14px;\n  }\n  [data-slot=\"panel\"][data-frame=\"rail\"] [data-slot=\"panel-header\"][data-marker=\"bar\"]::before {\n    display: none;\n  }\n  [data-slot=\"panel\"][data-frame=\"rail\"] [data-slot=\"panel-content\"],\n  [data-slot=\"panel\"][data-frame=\"rail\"] [data-slot=\"panel-footer\"] {\n    padding-inline-start: 14px;\n  }\n  [data-slot=\"panel\"][data-frame=\"rail\"] [data-slot=\"panel-footer\"] {\n    border: 0;\n  }\n\n  /* ============================================================\n     Frame: viewfinder — four corner brackets. Shares the viewfinder\n     visual language with diff-view via the same contract:\n       - CSS vars: --vf-size, --vf-weight, --vf-color, --vf-offset\n       - Corner element classes: .vf-tl / .vf-tr / .vf-bl / .vf-br\n     Dialog uses a separate `.dlg-corners` + [data-corners] pattern\n     for its own (in-flow vs. modal) reasons, so there is no literal\n     `.viewfinder` class to share across all three components today.\n     ============================================================ */\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] {\n    --vf-size: 20px;\n    --vf-weight: 2px;\n    --vf-color: var(--foreground);\n    --vf-offset: 0px;\n    padding: 8px;\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > span {\n    position: absolute;\n    pointer-events: none;\n    width: var(--vf-size);\n    height: var(--vf-size);\n    border: 0 solid var(--vf-color);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > .vf-tl {\n    top: var(--vf-offset);\n    left: var(--vf-offset);\n    border-top-width: var(--vf-weight);\n    border-left-width: var(--vf-weight);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > .vf-tr {\n    top: var(--vf-offset);\n    right: var(--vf-offset);\n    border-top-width: var(--vf-weight);\n    border-right-width: var(--vf-weight);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > .vf-bl {\n    bottom: var(--vf-offset);\n    left: var(--vf-offset);\n    border-bottom-width: var(--vf-weight);\n    border-left-width: var(--vf-weight);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > .vf-br {\n    bottom: var(--vf-offset);\n    right: var(--vf-offset);\n    border-bottom-width: var(--vf-weight);\n    border-right-width: var(--vf-weight);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-header\"] {\n    border-block-end: 1px solid var(--panel-hairline);\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-footer\"] {\n    border-block-start: 1px solid var(--panel-hairline);\n  }\n\n  /* ============================================================\n     Frame: surface — elevated --surface-1 background, transparent\n     border (forced-colors fallback), inner top highlight.\n     ============================================================ */\n  [data-slot=\"panel\"][data-frame=\"surface\"] {\n    background: var(--surface-1);\n    border: 1px solid transparent;\n    box-shadow: inset 0 1px 0 color-mix(in oklab, var(--foreground) 4%, transparent);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"] [data-slot=\"panel-header\"] {\n    border-block-end: 1px solid var(--panel-hairline);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"] [data-slot=\"panel-footer\"] {\n    border-block-start: 1px solid var(--panel-hairline);\n  }\n\n  /* ============================================================\n     Tone — paints --panel-tone (the header marker bar) and the\n     frame's color signal. Tone is a pure visual cue; semantic\n     status messaging belongs in Callout (see callout.css).\n     ============================================================ */\n  [data-slot=\"panel\"][data-tone=\"info\"]    { --panel-tone: var(--info-fg); }\n  [data-slot=\"panel\"][data-tone=\"success\"] { --panel-tone: var(--success-fg); }\n  [data-slot=\"panel\"][data-tone=\"warning\"] { --panel-tone: var(--warning-fg); }\n  [data-slot=\"panel\"][data-tone=\"error\"]   { --panel-tone: var(--error-fg); }\n  [data-slot=\"panel\"][data-tone=\"accent\"]  { --panel-tone: var(--action); }\n\n  /* Hairline frame: border + bar adopt the tone color. */\n  [data-slot=\"panel\"][data-frame=\"hairline\"][data-tone=\"info\"]    { border-color: var(--info-border); }\n  [data-slot=\"panel\"][data-frame=\"hairline\"][data-tone=\"success\"] { border-color: var(--success-border); }\n  [data-slot=\"panel\"][data-frame=\"hairline\"][data-tone=\"warning\"] { border-color: var(--warning-border); }\n  [data-slot=\"panel\"][data-frame=\"hairline\"][data-tone=\"error\"]   { border-color: var(--error-border); }\n  [data-slot=\"panel\"][data-frame=\"hairline\"][data-tone=\"accent\"]  { border-color: var(--action); }\n\n  /* Rail frame: rail color. */\n  [data-slot=\"panel\"][data-frame=\"rail\"][data-tone=\"info\"]    { border-inline-start-color: var(--info-border); }\n  [data-slot=\"panel\"][data-frame=\"rail\"][data-tone=\"success\"] { border-inline-start-color: var(--success-border); }\n  [data-slot=\"panel\"][data-frame=\"rail\"][data-tone=\"warning\"] { border-inline-start-color: var(--warning-border); }\n  [data-slot=\"panel\"][data-frame=\"rail\"][data-tone=\"error\"]   { border-inline-start-color: var(--error-border); }\n  [data-slot=\"panel\"][data-frame=\"rail\"][data-tone=\"accent\"]  { border-inline-start-color: var(--action); }\n\n  /* Viewfinder frame: corner color. */\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"][data-tone=\"info\"]    { --vf-color: var(--info-fg); }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"][data-tone=\"success\"] { --vf-color: var(--success-fg); }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"][data-tone=\"warning\"] { --vf-color: var(--warning-fg); }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"][data-tone=\"error\"]   { --vf-color: var(--error-fg); }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"][data-tone=\"accent\"]  { --vf-color: var(--action); }\n\n  /* Surface frame: subtle header tint + colored bottom-border on header. */\n  [data-slot=\"panel\"][data-frame=\"surface\"][data-tone=\"info\"]    [data-slot=\"panel-header\"] {\n    background: color-mix(in oklab, var(--info-fg) 6%, transparent);\n    border-block-end-color: var(--info-border);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"][data-tone=\"success\"] [data-slot=\"panel-header\"] {\n    background: color-mix(in oklab, var(--success-fg) 6%, transparent);\n    border-block-end-color: var(--success-border);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"][data-tone=\"warning\"] [data-slot=\"panel-header\"] {\n    background: color-mix(in oklab, var(--warning-fg) 8%, transparent);\n    border-block-end-color: var(--warning-border);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"][data-tone=\"error\"]   [data-slot=\"panel-header\"] {\n    background: color-mix(in oklab, var(--error-fg) 8%, transparent);\n    border-block-end-color: var(--error-border);\n  }\n  [data-slot=\"panel\"][data-frame=\"surface\"][data-tone=\"accent\"]  [data-slot=\"panel-header\"] {\n    background: color-mix(in oklab, var(--action) 6%, transparent);\n    border-block-end-color: var(--action);\n  }\n}\n\n/* ============================================================\n   Forced-colors: keep panel boundaries legible in Windows HCM.\n   Rail and surface frames need an explicit perimeter cue.\n   ============================================================ */\n@media (forced-colors: active) {\n  [data-slot=\"panel\"][data-frame=\"hairline\"],\n  [data-slot=\"panel\"][data-frame=\"surface\"] {\n    border-color: CanvasText;\n  }\n  [data-slot=\"panel\"][data-frame=\"rail\"] {\n    border-inline-start-color: CanvasText;\n  }\n  [data-slot=\"panel\"][data-frame=\"viewfinder\"] [data-slot=\"panel-corners\"] > span {\n    border-color: CanvasText;\n  }\n  [data-slot=\"panel-header\"][data-marker=\"bar\"]::before {\n    background: CanvasText;\n  }\n}\n",
      "type": "registry:style",
      "target": "~/styles/panel.css"
    }
  ],
  "meta": {
    "client": true
  },
  "type": "registry:ui"
}
