{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "search",
  "title": "Search Utilities",
  "description": "Case-insensitive search matching utility for filtering items by text query.",
  "files": [
    {
      "path": "registry/lib/search.ts",
      "content": "/** Case-insensitive substring search using the host locale. */\nexport function matchesSearch(value: string, query: string): boolean {\n  return !query || value.toLocaleLowerCase().includes(query.toLocaleLowerCase());\n}\n",
      "type": "registry:lib"
    }
  ],
  "meta": {
    "client": false,
    "hidden": true
  },
  "type": "registry:lib"
}
