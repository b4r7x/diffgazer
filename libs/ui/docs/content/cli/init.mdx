---
title: dgadd init
description: Initialize installer configuration for @diffgazer/ui and @diffgazer/keys.
---

## Usage

```bash
npx @diffgazer/add init
```

Use this `npx` form only after `@diffgazer/add` is published. Before publication, install a locally packed tarball and run `pnpm exec dgadd init`.

## What It Does

1. Detects your project configuration:
   - Package manager
   - Tailwind CSS version
   - TypeScript path aliases
   - Source directory (`src/` or root)

2. Creates `diffgazer.json`.

3. Creates install directories:
   - `src/components/ui/` for UI components
   - `src/hooks/` for copied keys hooks
   - `src/lib/` for shared utilities

4. Installs required dependencies.

5. Copies `src/styles/theme.css` and `src/styles/styles.css`.

It does not edit `tsconfig.json`, Vite, Next, or your app CSS entrypoint. Configure `@/*` in the app before running `init`, or pass `--allow-missing-alias` only when your tooling already resolves that alias.

## Options

| Flag | Description | Default |
|------|-------------|---------|
| `--cwd <path>` | Working directory | `.` |
| `--components-dir <path>` | Component install directory | `src/components/ui` |
| `--allow-missing-alias` | Continue when no `@/*` alias is detected | `false` |
| `--yes` | Skip confirmation prompts | `false` |

## Example

```bash
$ npx @diffgazer/add init

  Creating diffgazer.json
  Creating src/components/ui/
  Creating src/hooks/
  Creating src/lib/utils.ts
  Copying src/styles/theme.css
  Copying src/styles/styles.css

  Done. Add components with: npx @diffgazer/add add ui/button
```
