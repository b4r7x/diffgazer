---
title: "Diff View"
description: "Diff viewer with unified and side-by-side modes. Accepts unified diff strings, before/after text, or pre-parsed diff data."
component: "diff-view"
---

> `mode="split"` renders two independently scrolling panes and needs roughly ≥640px of container width to stay readable. On narrow or coarse-pointer viewports prefer `mode="unified"` (or switch modes from a container query in the consumer).

<ComponentDocScaffold hero="diff-view-default" />
