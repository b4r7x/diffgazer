---
title: Theme Overview
description: How @diffgazer/ui's two-layer CSS variable system works with primitives and semantic tokens.
---

@diffgazer/ui uses a two-layer CSS variable system. Primitives define the raw palette. Semantic tokens reference primitives for functional meaning.

Override the primitives to change every component at once. Override semantic tokens for fine-grained control.

## Variable Architecture

<VariableDiagram />

## Theme Playground

<ThemePlayground />
