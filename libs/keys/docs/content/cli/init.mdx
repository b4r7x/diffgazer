---
title: dgadd init
description: Initialize installer configuration before adding @diffgazer/keys hooks.
---

```bash
npx @diffgazer/add init
```

This creates `diffgazer.json` with aliases for components, hooks, utilities, and styles. Hooks are installed into the configured hooks directory, usually `src/hooks`.
