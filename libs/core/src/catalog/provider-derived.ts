// GENERATED by libs/core/scripts/generate-catalog-snapshot.ts — DO NOT EDIT BY HAND.
// Client-safe provider display names + capability prose derived from the
// models.dev snapshot at generation time (design D6, F-225).
// Regenerate: pnpm --filter @diffgazer/core generate:catalog-snapshot
import type { AIProvider } from "../schemas/config/providers.js";
import type { ProviderCapabilities } from "./capabilities.js";

export const PROVIDER_DERIVED: Record<
  AIProvider,
  { displayName: string; capabilities: ProviderCapabilities }
> = {
  gemini: {
    displayName: "Google Gemini",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported (structured output / JSON schema)",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 1M tokens",
      tier: "mixed",
      tierBadge: "FREE",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription:
        "Free and paid tiers vary by model; live pricing drives the per-model badge.",
    },
  },
  zai: {
    displayName: "Z.AI",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported (structured output / JSON schema)",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 205K tokens",
      tier: "mixed",
      tierBadge: "FREE",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription:
        "Free and paid tiers vary by model; live pricing drives the per-model badge.",
    },
  },
  "zai-coding": {
    displayName: "Z.AI Coding Plan",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported (structured output / JSON schema)",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 205K tokens",
      tier: "paid",
      tierBadge: "PAID",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription: "Paid usage; live pricing drives the per-model badge.",
    },
  },
  openrouter: {
    displayName: "OpenRouter",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported (structured output / JSON schema)",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 2M tokens",
      tier: "mixed",
      tierBadge: "FREE",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription:
        "Free and paid tiers vary by model; live pricing drives the per-model badge.",
    },
  },
  groq: {
    displayName: "Groq",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported (structured output / JSON schema)",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 262K tokens",
      tier: "free",
      tierBadge: "FREE",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription:
        "Free and paid tiers vary by model; live pricing drives the per-model badge.",
    },
  },
  cerebras: {
    displayName: "Cerebras",
    capabilities: {
      toolCalling: "Supported (tool / function calling)",
      jsonMode: "Supported where the model offers JSON output",
      streaming: "Supported (model-dependent)",
      contextWindow: "Up to 131K tokens",
      tier: "free",
      tierBadge: "FREE",
      capabilities: ["TOOLS", "JSON", "REASONING"],
      costDescription: "Cerebras free tier: ~1M tokens/day. Live pricing per model.",
    },
  },
};
