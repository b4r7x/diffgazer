# Thermo-Nuclear Reaudit — F001–F019 remediation verification + fresh deep audit

- Date: 2026-06-01
- Repo: `/Users/voitz/Projects/diffgazer-workspace` (branch `main`, HEAD `a42db394`)
- Remediation under review: `c1dec6ee..HEAD` (167 files) — the F001–F019 fixes from `audits/2026-05-31/THERMO-NUCLEAR-AUDIT.md`
- Method: 168 Opus subagents via background workflow — 19 per-finding verifiers + loop-until-dry fresh audit (18 area/lens finders × 5 rounds, converged) with per-finding adversarial verification and a shared dedup ledger. Plus 12 empirical verification gates run by the orchestrator.
- Skills applied: code-audit, anti-slop, code-quality, clean-code, test-behavior-not-implementation, sota (+ AGENTS.md architecture/handoff contract).

## Verdict — "czy wszystko zrobione?"

**F001–F019: all 19 remediated and verified RESOLVED** (verifier confidence 0.93–0.98), and the build is functionally green: `type-check`, `test` (184 files), `test:types`, `smoke`, `bench`, and the F001 ESM direct-imports all PASS.

**But it is NOT release-ready as-is.** Three things stand between this branch and "done":

1. **Release gate is RED.** `pnpm run check` (biome) fails with **133 errors in apps/docs** — including `breadcrumbs.tsx:49` (an F017-touched file) and `vite.config.ts:5`. `release-check` and `test-ci` both run this, so the branch fails CI. *(Mostly auto-fixable: `biome check --write`.)*
2. **F018 is only half-done.** Graceful session teardown (`shutdownSessions`) is wired into the **dev** server but never into the **packaged `diffgazer` binary** — the actual user-facing path still does not abort in-flight reviews on Ctrl-C (cluster CL-SHUTDOWN: N01/N07/N34).
3. **F010 is dead plumbing.** Scoped active-session lookup was made consistent across schema/route/service/core, but **no consumer ever passes the scope**, so scoped reviews remain undiscoverable on resume — the new capability is unreachable, and two scope-key encoding bugs (CSV/lens-dedup) ride along (cluster CL-RESUME: N10/N19/N25/N27/N45).

Everything else is quality debt: 1 real Medium security gap (registry `file.target` not path-checked), a Safari download bug, a scroll-spy freeze edge case, and ~40 anti-slop / dead-code / DRY / doc-drift items consistent with the repo's own rules.

## Empirical verification gates (orchestrator-run ground truth)

| Gate | Result |
|---|---|
| prepare:artifacts | ✅ PASS |
| F001 core build + 3× ESM direct-import (dist/index, api, schemas/config) | ✅ PASS |
| validate:artifacts:check | ✅ PASS |
| verify:monorepo | ✅ PASS |
| test:scripts | ✅ PASS |
| **pnpm run check (biome)** | ❌ **FAIL — apps/docs, 133 errors** |
| turbo type-check | ✅ PASS |
| turbo test:types | ✅ PASS |
| turbo test (184 test files) | ✅ PASS |
| smoke (strict) | ✅ PASS |
| bench (strict) | ✅ PASS |
| git diff --check | ✅ PASS |

## F001–F019 remediation status

All resolved. "test=NONE" rows are config/Dockerfile/comment changes where a unit test is not the right tier (F009 comment removal, F011 dependabot, F016 Dockerfile, F019 package.json) — appropriate.

| ID | Status | Test | Conf | Remaining risk (verifier) |
|---|---|---|---|---|
| F001 | resolved | implementation | 0.95 | The guard only executes inside `pnpm --filter @diffgazer/core build` (tsc && verify:dist-esm). A future code path that emits dist without invoking that package  |
| F002 | resolved | behavior | 0.98 | None material for the scope of F002. The fix is correct for the reported case. Minor edge note (not a regression vs. the audit requirement): isAriaHidden only c |
| F003 | resolved | behavior | 0.96 | Low. The standalone-dev pass-through test asserts `not.toBe(401)` rather than a concrete 200, because /api/config can still fail downstream for unrelated reason |
| F004 | resolved | behavior | 0.93 | The test asserts on the session buffer (`getSession().events`) rather than driving `streamActiveSessionToSSE` end-to-end, so it verifies the buffer the replay r |
| F005 | resolved | behavior | 0.97 | None material for the SSR-crash fix. Minor: the SSR test asserts only that render does not throw and that the seeded first id appears; it does not assert that e |
| F006 | resolved | behavior | 0.97 | isOwnerHTMLElement returns false when doc.defaultView is null (detached/loading document), so headings are silently dropped in that edge case; this is acceptabl |
| F007 | resolved | behavior | 0.97 | Low. The F007-specific regression test (object-vs-positional first-arg) lives only in the libs/ui testing folder, not duplicated in apps/docs; however apps/docs |
| F008 | resolved | behavior | 0.95 | Low. The test exercises log() directly rather than driving a full HTTP request through requestLogger + createApp under DIFFGAZER_PACKAGED=1, so the end-to-end w |
| F009 | resolved | none | 0.95 | F009's documented scope (the two cli/server files) is fully resolved. However the same audit-ticket-comment anti-slop pattern still exists elsewhere in runtime/ |
| F010 | resolved | behavior | 0.95 | Minor coverage gap, not a correctness gap: there is no router-level integration test for GET /api/review/sessions/active (router.test.ts covers POST /reviews an |
| F011 | resolved | none | 0.97 | Because no test or schema validation guards .github/dependabot.yml, a future change could silently re-introduce an `allow: - dependency-type: "direct"` (or simi |
| F012 | resolved | behavior | 0.97 | The lock is a regex on the package.json script string, not an end-to-end run that boots the server, induces an SLO breach, and asserts exit code 1. The regex ma |
| F013 | resolved | behavior | 0.95 | The base RegistryItemSchema (libs/registry/src/registry-types.ts:4) still types files[].path as plain z.string(), so the base schema by itself still accepts uns |
| F014 | resolved | behavior | 0.97 | Minor coverage gap: tests exercise the pure benchmark-slo.mjs helpers directly; runScenario's wiring (`...summarizeStatuses(statuses)`, pushing each response st |
| F015 | resolved | behavior | 0.96 | Low. The fix is a removal, so the dead-surface failure cannot recur unless the env key is re-added (the regression test guards exactly that). The same commit op |
| F016 | resolved | none | 0.96 | No automated test or CI invariant guards the deploy fix; a future Dockerfile edit could silently regress to a shell-less container. Builder stage rebuilds dist  |
| F017 | resolved | behavior | 0.97 | Low. (1) The "still includes generated hook item routes" test asserts against hardcoded names (controllable-state, use-focus-trap) sourced from src/generated/*/ |
| F018 | resolved | behavior | 0.93 | dev.ts shutdown is fire-and-forget: if server.close() never invokes its callback (a hung/keep-alive socket), there is no in-process exit fallback in the standal |
| F019 | resolved | none | 0.95 | The fix is structurally correct but unguarded: no automated test/invariant pins the landing build script to include `tsc -b` or pins the Dockerfile to type-chec |

> Note: F010 and F018 are marked "resolved" by the per-finding verifiers because the *originally cited* failure path no longer reproduces — but the fresh audit found both fixes are incomplete at the integration/production level (clusters CL-RESUME, CL-SHUTDOWN). Treat them as **partially done**.

## Theme clusters (dedup map)

The 53 raw findings collapse to ~30 distinct root issues. Key clusters:

- **CL-RESUME — F010 scoped active-session resume is half-wired (dead plumbing, real resume gap)** → N10, N19, N25, N27, N45 (and partly N05)
- **CL-SHUTDOWN — F018 graceful session teardown not wired into the packaged binary** → N01, N07, N34 (env race: N03)
- **CL-FLOATING-DRY — F007 fix left single-use object-form wrappers + misleading comment** → N17, N18, N26
- **CL-FLOATING-REALM — F006 cross-realm fix applied to active-heading but not its useFloatingPosition sibling** → N08, N16, N32
- **CL-TICKET-IDS — audit/ticket-ID comments still present in source & newly added to tests** → N13, N15, N28, N29, N51
- **CL-HUB-PARITY — hub diverges from sibling-SPA tsconfig/script conventions** → N23, N40 (N02 refuted)
- **CL-DOCS-LINTGATE — F017 docs files sit outside the lint/format gate that is itself red** → seed (biome), N39, N47

## Refuted on orchestrator re-check

- **N02 (was high)** Hub tsconfig lacks vite/client types, so the F016 Dockerfile build (tsc -b) cannot resolve the CSS side-effect import and fails
  - ORCHESTRATOR: REFUTED — ran `pnpm --filter @diffgazer/hub build` (tsc -b && vite build) → exit 0, builds cleanly (30 modules, dist emitted). The vite/client-types claim is false. Excluded from the active list.

## High-severity findings

### N01 🟠 HIGH — Packaged embedded server never aborts active review sessions on shutdown, so Ctrl-C hangs ~3s and leaks in-flight LLM work while an SSE review stream is open
- **Area/category:** cli/diffgazer (web + TUI prod shutdown / embedded server) / correctness / devex (graceful shutdown)
- **Locations:** `cli/diffgazer/src/lib/servers/embedded-server.ts:156-170`, `cli/server/src/dev.ts:15-24`, `cli/server/src/index.ts:1-3`, `cli/server/src/features/review/sessions.ts:191-216`
- **Evidence:** The embedded server is what the packaged `diffgazer` binary actually runs (both web mode and TUI prod mode, via createProdServerFactories). Its stop() does only `closing.close(() => resolve())` (embedded-server.ts:166-168). `serve()` from @hono/node-server returns a Node http.Server (node_modules/@hono/node-server/dist/types.d.ts: ServerType = Server | Http2Server | ...), and Node's Server.close(callback) fires its callback only after ALL open connections drain. The review pipeline serves long-lived SSE streams (cli/server/src/features/review/session-resume.ts uses streamSSE; sse-replay.ts), so an in-progress review keeps a connection open and `close()`'s callback never fires — the stop() promise never resolves. The standalone dev server handles exactly this: dev.ts:15-22 calls `shutdownSessions()` BEFORE `server.close()`, with the comment 'Abort active reviews and close subscribers first so no in-flight work or SSE client keeps the server alive'. shutdownSessions() (sessions.ts:191-216) aborts each session's controller ('shutdown'), emits a terminal SSE error, and clears subscribers 'so no SSE client keeps the process alive'. The embedded server never calls it — `shutdownSessions` is not even exported from the @diffgazer/server entry (index.ts only re-exports createApp), and `rg shutdownSessions cli/diffgazer/src` returns nothing. Net effect on Ctrl-C during a streaming review: stop() hangs, the parent grace race (web-launcher.ts:35 stopWithTimeout(stop, gracefulMs=3000); use-exit.ts:17-22 same 3000ms) always elapses, then process.exit(0) force-quits at 3s. The user eats a guaranteed ~3s delay, the browser never receives the terminal 'aborted' SSE event, and the in-flight provider/LLM request is never AbortController-aborted (it dies only when the OS reaps the force-exited process), unlike the dev server which aborts it cleanly.
- **Fix:** Mirror the dev.ts shutdown sequence in the packaged path. Step 1: export shutdownSessions from the server public entry so the bundled binary can reach it. In /Users/voitz/Projects/diffgazer-workspace/cli/server/src/index.ts add `export { shutdownSessions } from "./features/review/sessions.js";` alongside the existing `export { createApp }`. (Safe: the sessions module is already in the app graph via service.ts/sse-replay.ts/review-routes.ts/session-resume.ts, so no new module-load side effects beyond what createApp already pulls in.) Step 2: in /Users/voitz/Projects/diffgazer-workspace/cli/diffgazer/src/lib/servers/embedded-server.ts stop() (lines 158-169), abort active sessions before closing so the open SSE streams resolve and Node's close() callback can fire. Because startServer already uses `await import("@diffgazer/server")` for createApp, keep that lazy style for consistency and make stop async:      stop: async () => {       state = "stopped";       const { shutdownSessions } = await import("@diffgazer/server");       shutdownSessions();       if (!server) {         return;       }       const closing = server;       server = null;       await new Promise<void>((resolve) => {         closing.close(() => resolve());       });     },  shutdownSessions() aborts each session.controller ('shutdown') — cancelling in-flight provider/LLM work via the threaded AbortSignal — stores a terminal error, and notifies subscribers/completion, which resolves the streamActiveSessionToSSE promise so the streamSSE handler returns and the connection drains. closing.close()'s callback then fires promptly, stop() resolves before the 3000ms grace race, and the browser receives the terminal 'aborted' SSE event. No public API or contract change; matches the existing dev.ts pattern. Add a focused test that an active session with a subscriber is aborted and subscribers cleared after stop() (the sessions.test.ts shutdownSessions tests already cover the abort/subscriber-clear behavior; assert the embedded stop() invokes it).
- **Verifier:** Verified against the real code. (1) The packaged `diffgazer` binary runs the embedded in-process server for BOTH prod paths: web mode via web-launcher.ts:24 -> server-factories.ts:91 createProdServerFactories -> embedded-server.ts createEmbeddedServer, and TUI prod via app/index.tsx:127 createServer
- **ORCHESTRATOR: CONFIRMED by reading embedded-server.ts:158-169 — stop() only calls server.close(); shutdownSessions is never imported (only createApp at :96). Real incomplete-remediation of F018 for the packaged binary. Impact is graceless shutdown (process still exits via parent force-kill), so effective severity ~Medium.**

## Medium-severity findings

### N03 🟡 MEDIUM — Env-overridable child force-kill delay can exceed the hardcoded parent grace timeout, orphaning the server process on shutdown
- **Area/category:** cli/diffgazer (web + TUI shutdown) / correctness
- **Locations:** `cli/diffgazer/src/config.ts:22-28`, `cli/diffgazer/src/lib/servers/create-process-server.ts:128-143`, `cli/diffgazer/src/web-launcher.ts:34-38`, `cli/diffgazer/src/web-launcher.ts:60-68`, `cli/diffgazer/src/hooks/use-exit.ts:17-23`
- **Evidence:** Shutdown is a two-timer race between parent and child. The parent ceiling is hardcoded: web-launcher `stopAndExit` (web-launcher.ts:35) and the TUI `shutdownAndExit` (use-exit.ts:19) both `Promise.race([stop(), timeout])` against `config.shutdown.gracefulMs` (3000) and then unconditionally `process.exit(0)`. The child's force-kill timer fires at `appConfig.shutdown.forceKillMs` (create-process-server.ts:132-134), and `forceKillMs` is read from the environment with no upper bound: `readPositiveIntEnv(process.env.DIFFGAZER_FORCE_KILL_DELAY_MS, 2000)` (config.ts:27). The comment at config.ts:25-26 explicitly invites raising it ('Overridable for slow shutdowns (e.g. CI)'). With defaults (3000 > 2000) the SIGKILL fires and `await child` resolves before the parent's 3000ms timeout, so ordering is correct. But the documented override breaks ordering: setting DIFFGAZER_FORCE_KILL_DELAY_MS to any value >= 3000 (e.g. 5000 for 'slow CI') means the parent's race resolves to 'timeout' at 3000ms and calls `process.exit(0)` BEFORE the child's force-kill timer at 5000ms ever runs. The child server process (dev: vite/api tsx; prod: nothing spawned, but dev mode spawns two) keeps running orphaned and holding ports 3000/3001, so the next launch hits EADDRINUSE (embedded-server.ts:142). gracefulMs is not env-overridable and has no relationship to forceKillMs, so the very use case the override advertises is the one it silently defeats.
- **Fix:** Make the parent grace ceiling strictly exceed the child force-kill delay so the SIGKILL path can always complete before the parent exits. Compute forceKillMs once and derive gracefulMs from it in cli/diffgazer/src/config.ts:    const forceKillMs = readPositiveIntEnv(process.env.DIFFGAZER_FORCE_KILL_DELAY_MS, 2000);   // ...   shutdown: {     forceKillMs,     // Parent must outlast the child SIGKILL escalation, or it exits while a     // SIGTERM-resistant dev child is still alive and orphans it.     gracefulMs: forceKillMs + 1000,   },  This keeps the default behavior effectively the same (forceKillMs 2000 -> gracefulMs 3000) and guarantees gracefulMs > forceKillMs for any override, so the override the code advertises no longer defeats cleanup. Add one focused test asserting that with DIFFGAZER_FORCE_KILL_DELAY_MS raised (e.g. 5000) the resolved config.shutdown.gracefulMs is strictly greater than config.shutdown.forceKillMs. (Equivalent alternative: clamp forceKillMs to be strictly less than the fixed gracefulMs, but deriving gracefulMs preserves the override's intent of allowing longer waits in CI.)
- **Verifier:** The two shutdown timers are coupled in purpose but uncoupled in code, and the env override the code advertises is the exact configuration that inverts their ordering.  - Child cleanup (cli/diffgazer/src/lib/servers/create-process-server.ts:128-142): stop() sends SIGTERM, schedules SIGKILL via setTim

### N04 🟡 MEDIUM — Web launch never prints the server URL, leaving no way to reach the app when browser auto-open does not surface a tab
- **Area/category:** cli/diffgazer (web launch devex) / correctness
- **Locations:** `cli/diffgazer/src/lib/servers/server-factories.ts:37-49`, `cli/diffgazer/src/lib/servers/server-factories.ts:55-83`, `cli/diffgazer/src/web-launcher.ts:43-47`, `cli/diffgazer/src/lib/servers/create-process-server.ts:88-108`, `cli/diffgazer/src/lib/servers/embedded-server.ts:130-138`
- **Evidence:** In the web flow (the default action for `diffgazer` with no flags; cli-options.ts:55, index.tsx:30-31) the only user-visible output is the figlet banner (web-launcher.ts:44 -> banner.ts:10). The resolved address is computed (create-process-server.ts:101, embedded-server.ts:136) and passed only to `createOpenBrowserHandler`, whose handler calls `openBrowserAddress` to spawn the OS browser (server-factories.ts:48-49). The address string is written to the console only on failure (`console.warn` at server-factories.ts:44); on the success path it is never printed. Child stdout is captured with `stdout: "pipe"` purely to detect the ready pattern and is never forwarded (create-process-server.ts:88-108), so even vite's own 'Local: http://...' banner is swallowed in dev web mode. Result: in headless/SSH/container/CI sessions, or whenever the user closes the auto-opened tab, there is no printed URL to navigate to, and `open` can resolve successfully without actually showing anything to the user. The grep across the launch path confirms no success-path URL print exists.
- **Fix:** Print the URL on the success path so it is always visible regardless of browser auto-open. Since both dev and prod use the single shared onReady from createOpenBrowserHandler (server-factories.ts:48-50), edit that handler to log the address before opening the browser, and emit it even when openBrowser is false so headless/TUI-adjacent runs still see it. Concretely, in cli/diffgazer/src/lib/servers/server-factories.ts replace createOpenBrowserHandler with a handler that always logs and conditionally opens:  function createOpenBrowserHandler(openBrowser = true): (address: string) => void {   return (address) => {     console.log(`Diffgazer is running at ${address}`);     if (openBrowser) openBrowserAddress(address);   }; }  Then pass it unconditionally to both createWebServer and createEmbeddedServer onReady (drop the `openBrowser ? ... : undefined` gating at server-factories.ts:55 and :81, passing options.openBrowser through to the handler instead). This fires for dev (createWebServer onReady) and prod (createEmbeddedServer onReady), shows the deterministic http://localhost:<port> on readiness, and preserves the existing open-on-success and console.warn-on-failure behavior. No new dependency; uses the address already computed at create-process-server.ts:101 and embedded-server.ts:136.
- **Verifier:** Confirmed from the actual launch path. Web is the default action with openBrowser:true (cli-options.ts:55, index.tsx:30-31). The only console output before/during launch is the figlet banner (web-launcher.ts:43-44 -> banner.ts:9-12), which contains no URL. The resolved address is computed at create-

### N05 🟡 MEDIUM — Registry path-traversal guard validates file.path but not file.target, the field that controls shadcn copy-install write location
- **Area/category:** libs/registry / security
- **Locations:** `libs/registry/src/shadcn/validate.ts:79`, `libs/registry/src/shadcn/validate.ts:149`, `libs/registry/src/shadcn/validate.ts:152`
- **Evidence:** The remediation added `ensureSafeFilePath` (validate.ts:79-86) to reject registry file entries whose path is absolute or contains `..` segments, and calls it for both source and public registries (lines 149-154). But it only guards `file.path`; it never guards `file.target`. In the keys public registry, `target` is the load-bearing placement field: e.g. libs/keys/public/r/focus-trap.json ships files with `path: "src/dom/dom.ts"` but `target: "src/hooks/utils/dom.ts"`, and shadcn/direct-copy installers write each file to its `target` (verified: 18 targets in libs/keys/registry/registry.json, 13 in libs/ui/registry/registry.json, e.g. `~/styles/dialog.css`, `~/components/button.tsx`). The traversal test (libs/registry/src/testing/shadcn.test.ts:414-433) only exercises an unsafe `path` (`../escape.tsx`); a file with a safe `path` but a `target` of `../../escape.ts` would pass `validatePublicRegistryFresh` and ship a registry item that writes outside a shadcn consumer's project. The guard's own stated intent ('Registry file paths must be relative and must not contain ..') is exactly what `target` needs, since `target` -- not `path` -- determines the consumer write destination. dgadd is unaffected (it ignores `target` and resolves via resolveInstallPath/ensureWithinDir), so the gap is specific to the direct-copy/shadcn path.
- **Fix:** In validatePublicRegistryFresh (libs/registry/src/shadcn/validate.ts), extend the two existing file loops (currently validate.ts:149-154) to also guard `target` when present, mirroring the existing `path` checks:    for (const file of expectedItem.files) {     ensureSafeFilePath(file.path, sourceItem.name, "source registry");     if (file.target) ensureSafeFilePath(file.target, sourceItem.name, "source registry");   }   for (const file of publicItemJson.files ?? []) {     ensureSafeFilePath(file.path, sourceItem.name, "public registry");     if (file.target) ensureSafeFilePath(file.target, sourceItem.name, "public registry");   }  isRelativeSubpath already accepts legitimate relative targets and the `~/...` form used by the real registries contains no `..` segment and is not OS-absolute, so it passes; only absolute or `..`-escaping targets are rejected. Add a test in libs/registry/src/testing/shadcn.test.ts mirroring the existing it.each "../escape.tsx" / "/etc/passwd" / "C:\\windows\\system32" path cases but supplying a safe `path` and an unsafe `target` (via publicItemFiles), asserting it throws /Unsafe registry file path/ — covering both the source-loop and public-only-artifact code paths as the two existing path tests already do.
- **Verifier:** Confirmed by reading the cited code. `ensureSafeFilePath` (libs/registry/src/shadcn/validate.ts:79-86) is only invoked on `file.path` in both the source and public loops (validate.ts:150 and :153); `file.target` is never traversal-checked. `target` is a real, load-bearing placement field: RegistryFi

### N06 🟡 MEDIUM — downloadAsFile revokes the blob object URL synchronously after click(), which can abort the download in Safari/WebKit
- **Area/category:** apps/web/src/utils (context snapshot export) / correctness / cross-browser
- **Locations:** `apps/web/src/utils/download.ts:7-12`, `apps/web/src/features/review/components/context-snapshot-preview.tsx:35`
- **Evidence:** downloadAsFile creates a blob URL, sets it on an anchor, calls anchor.click(), then immediately calls URL.revokeObjectURL(url) on the next synchronous line (download.ts:11-12). The anchor is also never appended to the document. anchor.click() only *initiates* the download; the browser reads the blob URL asynchronously after the current task. Revoking the URL synchronously in the same task invalidates it before WebKit/Safari (and some older Chromium/Firefox builds) have fetched the blob, so the download silently produces an empty/failed file. This is the only blob-download path in the app and backs three user-facing export buttons in ContextSnapshotPreview (Download .txt / .md / .json at context-snapshot-preview.tsx:35,43,52), so the bug is directly reachable by users on non-Chromium browsers. No test pins the synchronous-revoke behavior, so the fix is safe.
- **Fix:** Defer the revoke past the current task so the browser can fetch the blob first, and append/remove the anchor for older Firefox. In apps/web/src/utils/download.ts, replace the body after `anchor.download = filename;` with:    document.body.appendChild(anchor);   anchor.click();   anchor.remove();   setTimeout(() => URL.revokeObjectURL(url), 0);  This matches the standard file-saver pattern, keeps the function synchronous-looking, and requires no API/signature changes or new dependencies. No test asserts the current synchronous-revoke timing, so no test update is needed.
- **Verifier:** Confirmed in apps/web/src/utils/download.ts:6-12: the function creates a blob URL (line 7), assigns it to a detached anchor (lines 8-10), calls anchor.click() (line 11), then synchronously calls URL.revokeObjectURL(url) on the very next line (line 12). anchor.click() only initiates the download; the

### N07 🟡 MEDIUM — shutdownSessions() is wired into the dev server but not the packaged diffgazer binary, so in-flight reviews are never aborted on shutdown
- **Area/category:** cli/diffgazer (web mode) + cli/server (review sessions) / correctness / incomplete remediation
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/cli/diffgazer/src/lib/servers/embedded-server.ts:156-170`, `/Users/voitz/Projects/diffgazer-workspace/cli/server/src/dev.ts:15-22`, `/Users/voitz/Projects/diffgazer-workspace/cli/server/src/features/review/sessions.ts:191-216`, `/Users/voitz/Projects/diffgazer-workspace/cli/server/src/index.ts:3`
- **Evidence:** The remediation expanded shutdownSessions() (sessions.ts:191-216) to abort every active session's AbortController, emit a terminal SESSION_STALE error to SSE subscribers, and clear subscribers/listeners, and wired it into the standalone dev server (dev.ts:18 calls shutdownSessions() before server.close()). But `rg -ln shutdownSessions` shows the only callers are dev.ts and its test — the packaged binary never calls it. The packaged embedded server's stop() (embedded-server.ts:158-169) only does `closing.close()` and never imports/calls shutdownSessions(); cli/server/src/index.ts (the @diffgazer/server entry the embedded server imports from) exports only createApp, not shutdownSessions. Concrete impact in the shipped product (web mode is the primary path): on SIGINT/SIGTERM with a review streaming, runReviewSession's AbortController (service.ts:137-144 passes session.controller.signal into the AI/git pipeline) is never aborted, so the in-flight model/git work keeps running; SSE clients never get the clean terminal error; and because the live-streaming promise in sse-replay.ts:105-204 only resolves on a terminal event / completion / client-abort, server.close()'s callback cannot fire while an SSE connection is open. web-launcher.ts:34-38 only avoids a permanent hang via the gracefulMs(3000ms) timeout that then process.exit(0)'s, i.e. the process is brute-force killed rather than draining gracefully. The dev server gets the new behavior; the actual binary does not.
- **Fix:** Wire shutdownSessions() into the in-process packaged path, mirroring dev.ts. 1) Export it from cli/server/src/index.ts (the @diffgazer/server entry the embedded server imports): add `export { shutdownSessions } from "./features/review/sessions.js";` (keep the existing createApp export). 2) In cli/diffgazer/src/lib/servers/embedded-server.ts, capture shutdownSessions from the dynamic import where createApp is already imported (startServer, line 96): `const { createApp, shutdownSessions } = await import("@diffgazer/server");` and hold it in a closure-scoped variable (e.g. `let shutdownSessions: (() => void) | null = null;` set after import). Then in stop() (embedded-server.ts:158-169), call `shutdownSessions?.();` before `closing.close(() => resolve())`. This aborts in-flight reviews, emits the terminal SESSION_STALE SSE error, clears subscribers so the open SSE socket ends, lets close()'s callback fire, and resolves stop() before the gracefulMs fallback hard-kills. Add a behavior test asserting embedded-server stop() drains an active session (terminal SSE error emitted, stop() promise resolves) to cover the shipped path the way sessions.test.ts already covers shutdownSessions directly. Keep severity medium: the OS reclaims the process on exit, so the harm is the lost graceful drain plus SSE clients missing the clean terminal error, not a leaked/zombie process.
- **Verifier:** Verified the full execution path. In packaged/web mode the only server is createEmbeddedServer (server-factories.ts:72-84 createProdServerFactories), which runs the Hono app in-process via @hono/node-server serve() (embedded-server.ts:96-138). serve() returns a Node http.Server (confirmed in @hono/n

### N08 🟡 MEDIUM — Floating-position cross-realm test cannot regression-guard the host-realm `instanceof HTMLElement` scroll-ancestor bug (asymmetric with the active-heading test that got a real cross-realm JSDOM)
- **Area/category:** libs/ui/registry/hooks (use-floating-position) — test coverage / test-behavior-not-implementation / missing-regression-coverage / sibling-module-inconsistency
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/testing/use-floating-position.test.ts:361`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/testing/use-floating-position.test.ts:515`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-floating-position.ts:80`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/testing/use-active-heading.test.ts:270`
- **Evidence:** The remediation cross-realm-hardened both sibling hooks, but only the active-heading test got a true cross-realm regression guard. use-active-heading.test.ts:266-319 spins up a SEPARATE realm via `new JSDOM(...)` whose elements fail the host `instanceof HTMLElement` (asserted at line 282: `expect(cross("x1") instanceof HTMLElement).toBe(false)`), which is the only thing that exercises the realm-correct filter and would catch a regression to a host-realm check.  use-floating-position.ts:80 still relies on a host-realm `parent instanceof HTMLElement` inside `getNearestOverflowAncestor` (flagged separately as the F006-area correctness defect). The floating-position test suite cannot guard a fix for it, because neither test that touches that code path uses a true cross-realm document: - The 'derives viewport and listener target from the trigger's ownerDocument' test (use-floating-position.test.ts:361-426) builds `altDoc` via `document.implementation.createHTMLDocument("alt")`, which is SAME-realm — its elements ARE host `HTMLElement` instances (the active-heading test even documents this at its line 213-215). Moreover the trigger's parent is `altDoc.body`, so `getNearestOverflowAncestor` returns at use-floating-position.ts:78 (`isLastTraversableNode` branch) and never reaches the `instanceof HTMLElement` check at line 80. - The 'scroll-parent discovery' tests (use-floating-position.test.ts:515-549) DO reach line 80, but they render into the host `document` (same realm), so `instanceof HTMLElement` passes trivially.  Result: `rg` confirms `use-floating-position.test.ts` is the only file exercising overflow-ancestor discovery, and `new JSDOM` appears only in the active-heading test. There is no test in which `getNearestOverflowAncestor` walks a cross-realm overflow ancestor. A fix at line 80 would ship with zero regression coverage, and the suite gives a false sense of cross-realm parity between the two hooks.
- **Fix:** Pair the correctness fix with its regression guard.  Fix the realm-unsafe check at use-floating-position.ts:80 to mirror the F006 fix already applied in use-active-heading.ts:39-42. Replace the host-realm `instanceof HTMLElement` with a realm-derived constructor (the file already routes view via ownerDocument.defaultView):    const view = parent.ownerDocument?.defaultView;   const HTMLElementCtor = view?.HTMLElement;   if (HTMLElementCtor && parent instanceof HTMLElementCtor && isOverflowElement(parent as Element)) {     return parent as HTMLElement;   }  (getParentNode returns a Node, so keep the existing narrowing intent; an equivalent already-present-in-file approach is to gate on getNodeName/Element rather than the host HTMLElement, but deriving HTMLElement from defaultView matches the sibling hook exactly and keeps the two hooks consistent.)  Then add a cross-realm regression test mirroring use-active-heading.test.ts:266-319: build the trigger, content, and an intermediate scrollable wrapper inside a separate `new JSDOM(...)` realm (so `wrapper instanceof HTMLElement` is false in the host realm — assert that premise as the active-heading test does at line 282), give the wrapper overflow-y: auto via the realm's getComputedStyle, drive the realm's requestAnimationFrame synchronously, render useFloatingPosition with that cross-realm trigger ref, and assert the wrapper receives the passive `scroll` listener (mirroring the existing scroll-parent discovery assertion at use-floating-position.test.ts:543-545). With the current line-80 check this test fails (ancestor dropped); after the realm-derived fix it passes — restoring cross-realm parity with the active-heading suite. Keep the scope to this one file pair; do not broaden to unrelated hooks.
- **Verifier:** Both halves of the finding verify against the code as written.  (1) The underlying defect is real. libs/ui/registry/hooks/use-floating-position.ts:80 uses a host-realm check `parent instanceof HTMLElement` inside getNearestOverflowAncestor. The rest of the file is deliberately realm-safe — isShadowR

### N09 🟡 MEDIUM — useActiveHeading scroll-spy permanently freezes when scrollTo targets an already-in-view heading
- **Area/category:** libs/ui/registry/hooks/use-active-heading / correctness / effect lifecycle
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-active-heading.ts:225`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-active-heading.ts:122`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-active-heading.ts:172`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-active-heading.ts:192`
- **Evidence:** scrollTo sets scrollingToRef.current = id (line 225) as a guard so the scroll handler does not fight the programmatic scroll. The ref is ONLY ever cleared in two places: handleScrollEnd (line 194, fired by the native 'scrollend' event) and the settleDelay fallback timer inside scheduleUpdate (lines 176-178), which is only armed when a 'scroll' event actually fires (scheduleUpdate is a scroll/resize/mutation listener). If scrollTo targets a heading that is already at the resolved scroll position, view.scrollTo({top, behavior}) (lines 236/238) produces no scroll movement, so the browser fires neither 'scroll' nor 'scrollend'. Concrete path: user clicks a TOC entry whose section is already in view (Math.max(0, ...) clamps to the current scrollTop) -> no scroll occurs -> handleScrollEnd never runs and scheduleUpdate is never invoked -> scrollingToRef.current stays set to that id permanently. From then on every update() call short-circuits at line 122 (`if (doc === null || scrollingToRef.current !== null) return;`), so activeId never tracks subsequent user scrolling again until the component remounts. The displayed activeId is correct on click (line 224 sets it), so the regression is silent: only auto-tracking dies. This is unguarded by tests — use-active-heading.test.ts:180-191 asserts the click sets activeId='h2' but never asserts that scroll-spy resumes afterward, and the cross-realm/F006 tests do not exercise scrollTo at all.
- **Fix:** Make scrollTo arm a self-clearing settle timer so a no-movement programmatic scroll can never strand the guard, reusing the existing settleTimerRef/settleDelay machinery and the defaultView already captured at line 228. Critically the timer must be armed for BOTH the container and window scroll branches (either can be a no-op), not only the window branch, and it must clear any prior settleTimerRef before re-arming to avoid duplicate timers. Concretely, after the if (container) {...} else if (view) {...} block in scrollTo (lines 232-242), add:    if (view) {     if (settleTimerRef.current !== 0) view.clearTimeout(settleTimerRef.current);     settleTimerRef.current = view.setTimeout(() => {       settleTimerRef.current = 0;       scrollingToRef.current = null;       update();     }, settleDelay);   }  Then add settleDelay (and, because the callback calls update via useEffectEvent which is stable, no extra dep) to the scrollTo useCallback dependency list at line 243: [doc, containerId, scrollOffset, settleDelay]. This guarantees the guard is released after settleDelay even when no scroll/scrollend fires; when a real scroll/scrollend DOES occur first, handleScrollEnd/the existing fallback clears the ref and (for handleScrollEnd) the effect cleanup at line 216 already clears settleTimerRef, while a redundant timer firing afterward is harmless because it only re-nulls an already-null ref and re-runs update(). Note that update() is a useEffectEvent and is callable from scrollTo; no behavior in the existing settle/scrollend paths changes.
- **Verifier:** Confirmed by reading /Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-active-heading.ts. The scroll-spy guard scrollingToRef is the only thing referenced at 7 sites (verified via rg): it is SET only at line 225 inside scrollTo, and CLEARED only at line 178 (the settle fallback) a

## Low-severity findings

### N10 ⚪ LOW — getActiveReviewSession scope parameter has no caller (F010 fix wired into client/server but not into the app)
- **Area/category:** libs/core/src/api / yagni-dead-code
- **Locations:** `libs/core/src/api/review.ts:108-131`, `libs/core/src/api/review.ts:175-176`, `libs/core/src/api/hooks/review.ts:16-19`, `libs/core/src/api/hooks/queries/review.ts:22-27`
- **Evidence:** The F010 remediation added `ActiveReviewSessionOptions` and a third `scope` parameter to `getActiveReviewSession(client, mode, scope = {})` (review.ts:108-131), threaded it through `bindReview` (review.ts:175-176), and the server route now validates `profile`/`lenses`/`files` and builds a `scopeKey` for discovery (cli/server/src/features/review/review-routes.ts:115-124, schemas.ts:33-46). However NO caller ever passes `scope`. The React Query hook only forwards `mode`: `activeSession: (api, mode?) => queryOptions({ queryKey: [...all(), "active-session", mode], queryFn: () => api.getActiveReviewSession(mode) })` (queries/review.ts:22-27) and `useActiveReviewSession(mode?)` (hooks/review.ts:16-19). The only direct callers, apps/web/src/features/home/components/home-presentation.tsx:146,151 and page.tsx:55, call `getActiveReviewSession(mode)` / `getActiveReviewSession("unstaged")` with no scope. `rg ActiveReviewSessionOptions` shows it is referenced only inside review.ts; the only invocation passing the third arg is review.test.ts. Because the web app currently creates only mode-only reviews (`createReview({ mode })`, home-presentation.tsx:132), the F010 symptom does not manifest today, so the scope branches (review.ts:124-126) are unreachable in production. This is speculative/incomplete API: either dead code (no consumer ever supplies scope) or an incompletely-wired F010 fix (the app's active-session lookup would still send an empty scopeKey and fail to discover a scoped review if the app ever creates one), contradicting the in-source comment 'Scope must mirror createReview so a scoped review is discoverable on resume.'
- **Fix:** The claimed "low" severity is correct; this is a yagni/dead-surface issue, not a correctness bug. Per AGENTS.md ("no abstractions without real consumers", "Prefer deleting dead code over preserving compatibility wrappers", "No deprecated aliases before first public release"), pick the deletion option rather than building out scoped discovery for a feature the app does not have. Concretely: in libs/core/src/api/review.ts remove the `scope`/`ActiveReviewSessionOptions` surface — delete the `ActiveReviewSessionOptions` interface (108-112), revert `getActiveReviewSession` to `(client, mode?)` dropping the scope branches (117, 119-126), and revert `bindReview`'s entry to `getActiveReviewSession: (mode?: ReviewMode) => getActiveReviewSession(client, mode)` (175-176). Delete the two scope-specific tests in review.test.ts (145-167), keeping the mode/no-mode cases. Leave the server-side F010 fix (schemas.ts ActiveSessionQuerySchema, review-routes.ts buildScopeKey/getActiveSessionForProject scopeKey) intact: it makes create-side and lookup-side scope keys consistent and is harmless for mode-only clients (absent params yield the empty scope key). If, instead, scoped reviews are an imminent product feature, do not stop at the client helper: add the scope inputs to `reviewQueries.activeSession` and `useActiveReviewSession` (including scope in the query key so distinct scopes cache separately), pass the same scope used at create time from the app callers, and add an end-to-end test for scoped create-review followed by active-session lookup — but absent a scoped-create producer in the app, deletion is the minimal correct change.
- **Verifier:** Confirmed by reading the cited files. The F010 remediation added a third `scope` parameter and `ActiveReviewSessionOptions` to the imperative client helper (libs/core/src/api/review.ts:108-131) and threaded `scope?` through `bindReview` (review.ts:175-176), but no production caller ever passes it. `

### N11 ⚪ LOW — Domain error schemas widen `code` to `string`, discarding the literal error-code union
- **Area/category:** libs/core/src/schemas (errors) / type safety
- **Locations:** `libs/core/src/schemas/errors.ts:35-39`, `libs/core/src/schemas/errors.ts:45-46`, `libs/core/src/schemas/review/issues.ts:127`
- **Evidence:** `createDomainErrorCodes` returns `[...SHARED_ERROR_CODES, ...specificCodes] as [string, ...string[]]` (errors.ts:38). The explicit cast to `[string, ...string[]]` throws away the literal element types, so `z.enum(allCodes)` (errors.ts:46) produces a `z.ZodEnum` whose inferred value type is `string`, not the intended union (`"INTERNAL_ERROR" | "API_KEY_MISSING" | "RATE_LIMITED" | "NO_DIFF" | "AI_ERROR" | "GENERATION_FAILED" | "SESSION_NOT_FOUND" | "SESSION_STALE"`). Consequently `ReviewError` (issues.ts:129) and every domain error produced by this factory has `code: string`. Consumers that switch on the code — e.g. `use-review-start.ts:59-61` and `use-review-stream.ts:110-111` comparing `result.error.code === ReviewErrorCode.SESSION_STALE` / `SESSION_NOT_FOUND` — get no compile-time guarantee: a typo in the compared constant, a stale code, or a missing branch is silently accepted because both sides are just `string`. The generic factory already captures the literals via `<const T extends readonly string[]>`, so they are available; only the manual cast destroys them. Runtime parsing/validation is correct, so this is a type-safety regression in the public error contract rather than a runtime bug. (Pre-existing relative to commit c1dec6ee; the remediation only rewrote the import specifier.)
- **Fix:** The finding's proposed fix is INCOMPLETE: changing only `createDomainErrorCodes` leaves the second widening cast at libs/core/src/schemas/review/issues.ts:125 (`const REVIEW_SPECIFIC_CODES = Object.values(ReviewErrorCode) as [string, ...string[]]`), so the generic `T` the factory receives is already `[string, ...string[]]` and `T[number]` stays `string` — the union is still lost. Remove BOTH casts: (A) In errors.ts:35-39, return the literal tuple: `function createDomainErrorCodes<const T extends readonly string[]>(specificCodes: T) { return [...SHARED_ERROR_CODES, ...specificCodes] as const; }` (drop the `: [string, ...string[]]` return annotation and the cast). (B) In issues.ts:125, build a literal tuple instead of the widened cast, e.g. `const REVIEW_SPECIFIC_CODES = [ReviewErrorCode.NO_DIFF, ReviewErrorCode.AI_ERROR, ReviewErrorCode.GENERATION_FAILED, ReviewErrorCode.SESSION_NOT_FOUND, ReviewErrorCode.SESSION_STALE] as const;` (or `Object.values(ReviewErrorCode) as readonly ReviewErrorCode[]` — but a literal tuple is what `z.enum`'s `const T` overload needs to infer the union). With both casts removed, `z.enum` infers the literal union, `ReviewError["code"]` becomes `"INTERNAL_ERROR" | "API_KEY_MISSING" | "RATE_LIMITED" | "NO_DIFF" | "AI_ERROR" | "GENERATION_FAILED" | "SESSION_NOT_FOUND" | "SESSION_STALE"`, and the hook comparisons regain compile-time checking. Verify via `pnpm --filter @diffgazer/core type-check` (not run here per static-analysis constraint).
- **Verifier:** Confirmed by reading the real code and the installed zod v4 type definitions. (1) `createDomainErrorCodes` casts its return to `[string, ...string[]]` (libs/core/src/schemas/errors.ts:37-38) and `createDomainErrorSchema` passes that to `z.enum(allCodes)` (errors.ts:45-46). (2) The installed zod v4.3

### N12 ⚪ LOW — `AgentMetaSchema.emoji` is dead schema surface — accepted on input, stripped by the transform, never produced or consumed
- **Area/category:** libs/core/src/schemas (events) / dead code / anti-slop
- **Locations:** `libs/core/src/schemas/events/agent.ts:30`, `libs/core/src/schemas/events/agent.ts:32-39`
- **Evidence:** `AgentMetaSchema` declares `emoji: z.string().optional()` (agent.ts:30) but its `.transform` (agent.ts:32-39) returns an object containing only `id, lens, name, badgeLabel, badgeVariant, description` — `emoji` is intentionally omitted from the output. `rg emoji` across `libs/core`, `cli`, and `apps` finds no producer that sets `emoji` and no consumer that reads it (only an unrelated UI callout example mentions the word). The field therefore can never round-trip: it is silently dropped from every parsed `AgentMeta`. It is pure unused schema surface that misleads readers into thinking agent metadata carries an emoji.
- **Fix:** Delete the `emoji: z.string().optional(),` line (libs/core/src/schemas/events/agent.ts:30) from AgentMetaSchema. The transform already discards it and nothing produces or reads it, so removing it makes the input object's accepted shape match the transform's emitted contract. No other change is needed — AGENT_METADATA, the AgentMeta type, and all event schemas referencing AgentMetaSchema continue to type-check unchanged.
- **Verifier:** Confirmed by reading libs/core/src/schemas/events/agent.ts. AgentMetaSchema declares `emoji: z.string().optional()` on input (line 30), but the `.transform` at lines 32-39 returns an object with only `id, lens, name, badgeLabel, badgeVariant, description` — `emoji` is omitted, so it is stripped from

### N13 ⚪ LOW — Audit/ticket-id comment left in runtime source (`slot-07 F10`)
- **Area/category:** libs/core/src/schemas (presentation) / anti-slop
- **Locations:** `libs/core/src/schemas/presentation/shortcuts.ts:25-27`
- **Evidence:** `// Wording chosen per slot-07 F10: "Navigate" / "Select" / "Quit" matches the rest of the keyboard model ...` embeds an internal slot/ticket identifier (`slot-07 F10`) in shipped library source. This is exactly the audit/ticket-id-in-runtime-source pattern the anti-slop checklist calls out (e.g. "F148", "STRUCTURE.md §6"). The reference is meaningless to any external/copy consumer of `MAIN_MENU_SHORTCUTS` and ties the source to a private planning artifact.
- **Fix:** In libs/core/src/schemas/presentation/shortcuts.ts:25, drop the `slot-07 F10` ticket reference while keeping the non-obvious single-source rationale. Replace the comment with intent-only wording, e.g.: `// "Navigate"/"Select"/"Quit" matches the rest of the keyboard model (j/k Navigate in review, Enter Select elsewhere). Web and CLI share this one constant.` Do not delete the comment entirely — the single-source-of-truth note is useful; only the slot id must go.
- **Verifier:** libs/core/src/schemas/presentation/shortcuts.ts:25 contains `// Wording chosen per slot-07 F10: "Navigate" / "Select" / "Quit" matches the ...`. The token `slot-07 F10` is an internal slot/ticket identifier embedded in shipped private-library runtime source. It is the exact pattern the anti-slop che

### N14 ⚪ LOW — navigation-items.ts uses global Node constants, contradicting the codebase's own realm-correctness convention (the exact pattern F002-area code was fixed to avoid)
- **Area/category:** libs/keys/src/dom (navigation) / keyboard/a11y correctness; ownerDocument/realm consistency
- **Locations:** `libs/keys/src/dom/navigation-items.ts:56`, `libs/keys/src/dom/navigation-items.ts:57`, `libs/keys/public/r/navigation.json (embedded copy of src/dom/navigation-items.ts, same two lines)`
- **Evidence:** queryAllMatchingGroups sorts merged multi-selector results by DOM order using `if (pos & Node.DOCUMENT_POSITION_FOLLOWING) return -1; if (pos & Node.DOCUMENT_POSITION_PRECEDING) return 1;` — both reference the GLOBAL `Node` constructor. This is the precise anti-pattern the sibling file libs/keys/src/dom/focusable.ts:67-76 was written to avoid: focusable.ts defines local numeric literals `const DOCUMENT_POSITION_PRECEDING = 0x02; const DOCUMENT_POSITION_FOLLOWING = 0x04;` with a comment 'jsdom returns compound-selector matches out of tree order; normalize' and a dedicated regression test focusable.test.ts:55-64 ('sorts in document order without relying on the global Node constructor', exercised via withGlobalNodeUnavailable). dom.ts:21-23 likewise threads an `ownerView` into `isNode(value, ownerView)` rather than touching global `Node`. navigation-items operates on cross-realm elements (it is reachable from the same focus/navigation stack as focusable), so when the global `Node` is absent (the scenario already guarded against and tested in focusable), `getNavigationItems`/`findNavigationItemByValue` throw a ReferenceError at the sort step whenever a query merges >1 selector group (merged.length > 1 at line 53). AGENTS.md keys rule: 'Focus utilities must respect the element ownerDocument; do not assume global document' — the same realm-correctness expectation applies to the Node constructor. Not a regression from this remediation (file predates c1dec6ee), but it is an in-scope correctness/consistency gap that the F002-area fix left unaddressed.
- **Fix:** In libs/keys/src/dom/navigation-items.ts, stop reading the global Node constructor and reuse the realm-independent comparator the repo already established in focusable.ts. focusable.ts already exports documentOrder logic inline; the cleanest minimal change is to export `documentOrder` from focusable.ts and import it here (navigation-items.ts already imports `containsActiveElement` from "./focusable.js"), then replace the inline sort body at lines 54-59 with `merged.sort(documentOrder);`. Concretely: in focusable.ts change `function documentOrder` to `export function documentOrder`; in navigation-items.ts change the import to `import { containsActiveElement, documentOrder } from "./focusable.js";` and replace the `merged.sort((a, b) => { ... })` block with `merged.sort(documentOrder);`. This removes the duplicated comparator (now 2 occurrences -> 1 shared helper, satisfying the realm-correctness convention without inventing a new abstraction). Then regenerate the public registry so libs/keys/public/r/navigation.json (content at line 23) reflects the updated source via the repo's artifact pipeline (pnpm run prepare:artifacts) and verify with pnpm run validate:artifacts:check; the copy-mode import specifier in the JSON is the bare "./focusable" form, which the existing rewrite already produces, so adding the named import keeps copy/shadcn consumers building. Do not introduce new local 0x02/0x04 literals here when an existing helper can be shared.
- **Verifier:** Confirmed by reading the source. libs/keys/src/dom/navigation-items.ts:56-57 reads the global Node constructor: `if (pos & Node.DOCUMENT_POSITION_FOLLOWING) return -1; if (pos & Node.DOCUMENT_POSITION_PRECEDING) return 1;`. The sibling file libs/keys/src/dom/focusable.ts:67-76 was deliberately writt

### N15 ⚪ LOW — Runtime source comment carries internal ticket IDs (KEY-019/KEY-020) and 'known limitation' language, violating the repo anti-slop rule against audit/ticket-id comments and future-work hedging in shipped source
- **Area/category:** libs/keys/src/hooks (focus restore) / anti-slop (ticket-id comments / known-limitation hedging in runtime source)
- **Locations:** `libs/keys/src/hooks/use-focus-restore.ts:26-30`, `libs/keys/src/hooks/use-focus-restore.ts:30`
- **Evidence:** Lines 26-30 read: `// KEY-020: The restore stack is global (module-scoped). In multi-document // scenarios (iframes, multi-window), entries from different documents share // this stack. A per-document WeakMap<Document, FocusRestoreEntry[]> would // isolate them, but requires threading ownerDocument through capture/restore // calls. This is a known limitation paired with KEY-019.` AGENTS.md anti-slop checklist explicitly forbids 'audit/ticket-id comments in runtime source (e.g. "F148", "STRUCTURE.md §6")' and the global CLAUDE.md forbids marking issues as a 'known limitation' for 'future work'. This is a public, copy/shadcn-distributed registry hook (libs/keys/public/r/focus-restore.json embeds this file), so the ticket-ID/limitation comment ships verbatim to external copy-mode consumers who have no access to the KEY-019/KEY-020 tracker. (Same pattern also at libs/keys/src/providers/keyboard-provider.tsx:171 'KEY-019', just outside this scope.) Not introduced by this remediation, but it is an in-scope reviewable-handoff artifact.
- **Fix:** In libs/keys/src/hooks/use-focus-restore.ts:26-30, replace the comment with a ticket-free, hedge-free description of the actual behavior, e.g.: "// The restore stack is module-scoped, so every document shares one stack; entries from different documents (iframes/multi-window) are not isolated." Drop the KEY-019/KEY-020 references and the "known limitation" wording. For consistency in the same anti-slop pass, apply the same treatment to libs/keys/src/providers/keyboard-provider.tsx:171-175 (remove "KEY-019", "future API", and "known single-document limitation"; state plainly that the keydown listener is on window and only captures events from the provider's own document). Then regenerate the public registry via the existing build script (libs/keys/scripts/build-shadcn-registry.ts) so libs/keys/public/r/focus-restore.json and libs/keys/public/r/focus-trap.json pick up the new comment, and run the registry verification gate (pnpm run prepare:artifacts + pnpm run validate:artifacts:check) so the committed handoff contract matches source. Do not implement the per-document WeakMap — that would be unrequested scope; only the comment is the issue.
- **Verifier:** Confirmed by reading the source. libs/keys/src/hooks/use-focus-restore.ts:26-30 contains a runtime comment referencing internal tracker IDs "KEY-020" and "KEY-019" plus the phrase "This is a known limitation". This violates two cited rules: (a) the orchestrator's anti-slop checklist forbids "audit/t

### N16 ⚪ LOW — useFloatingPosition keeps a host-realm `instanceof HTMLElement` check that the sibling F006 fix removed, breaking scroll-ancestor discovery for cross-realm triggers
- **Area/category:** libs/ui/registry/hooks/use-floating-position / correctness/cross-realm
- **Locations:** `libs/ui/registry/hooks/use-floating-position.ts:80`
- **Evidence:** The F006 remediation hardened use-active-heading against cross-realm documents by deriving HTMLElement from `doc.defaultView` (use-active-heading.ts:39-42 `isOwnerHTMLElement`). use-floating-position.ts already follows the same cross-realm-aware pattern in its other helpers — `isShadowRoot` reads `value.ownerDocument?.defaultView` (line 48) and `isOverflowElement` reads `element.ownerDocument?.defaultView` (line 66) — but `getNearestOverflowAncestor` still gates on the module's host-realm constructor: `if (parent instanceof HTMLElement && isOverflowElement(parent))` (line 80). `getParentNode` walks `node.parentNode`/`assignedSlot` in the trigger's own realm, so when the trigger lives in a different realm (an iframe whose elements are not the host realm's HTMLElement instances), this check is always false. The recursion then skips every real overflow ancestor and walks up to body, so the hook never attaches `scroll` listeners to the inner scroll containers (lines 165-168) and the floating element drifts when those containers scroll. The file's own comment (lines 86-87) advertises iframe/document-boundary awareness, and the existing cross-doc test (use-floating-position.test.ts:361) uses `document.implementation.createHTMLDocument`, which is same-realm, so its elements ARE host HTMLElement and the gap is never exercised.
- **Fix:** In libs/ui/registry/hooks/use-floating-position.ts, change getNearestOverflowAncestor to resolve HTMLElement from the parent's own view, mirroring isShadowRoot/use-active-heading's isOwnerHTMLElement:    function getNearestOverflowAncestor(node: Node): HTMLElement | null {     const parent = getParentNode(node);     if (isLastTraversableNode(parent)) {       return node.ownerDocument?.body ?? null;     }     const HE = parent.ownerDocument?.defaultView?.HTMLElement;     if (HE && parent instanceof HE && isOverflowElement(parent)) {       return parent;     }     return getNearestOverflowAncestor(parent);   }  Then regenerate the UI public registry so libs/ui/public/r/floating-position.json matches source (registry must mirror source per the handoff contract). Add a true cross-realm behavior test: build a second realm via an iframe/JSDOM document (not document.implementation.createHTMLDocument, which is same-realm), give an ancestor overflow-y:auto in that realm, mount the hook on a trigger inside it, and assert a scroll listener is attached to that ancestor — currently this fails because the host-realm instanceof check rejects the foreign-realm element.
- **Verifier:** Confirmed in the code as written. libs/ui/registry/hooks/use-floating-position.ts:80 gates overflow-ancestor discovery on the module's host-realm constructor: `if (parent instanceof HTMLElement && isOverflowElement(parent))`. This is the one spot in the file that does NOT resolve the constructor/vie

### N17 ⚪ LOW — F007 fix leaves single-use object-form wrappers (`shiftFrom`, `resolveCollisionPositionFrom`) behind the public positional helpers, and the justifying comment is inaccurate
- **Area/category:** libs/ui/registry/hooks/compute-floating-position / code-quality (DRY/KISS)
- **Locations:** `libs/ui/registry/hooks/compute-floating-position.ts:83`, `libs/ui/registry/hooks/compute-floating-position.ts:97`, `libs/ui/registry/hooks/compute-floating-position.ts:133`, `libs/ui/registry/hooks/compute-floating-position.ts:157`, `libs/ui/registry/hooks/compute-floating-position.ts:167`
- **Evidence:** The remediation renamed the four exported object-form helpers to private `*From` functions and added positional public wrappers. The comment at lines 133-135 states the `*From` internals 'are used by callers that benefit from named arguments.' That is only true for `computePositionFrom` and `wouldOverflowFrom`, which are reused inside the collision loop (lines 116-117, 122). `shiftFrom` (line 83) is called only by the public `shift` wrapper (line 164) and `resolveCollisionPositionFrom` (line 97) only by the public `resolveCollisionPosition` wrapper (line 177) — verified by ripgrep: each `*From` of these two has exactly one call site, the matching positional wrapper. So two of the four are pure pass-through duplicates whose body must be kept in sync with nothing, adding maintenance surface with no caller benefit, and the comment overstates the reuse.
- **Fix:** Inline the two single use helpers into their wrappers, keep the others, fix the comment, resync the json.
- **Verifier:** Two helpers shiftFrom and resolveCollisionPositionFrom each have one caller. The other two are reused. Comment overstates reuse.

### N18 ⚪ LOW — Remediation introduced a 1:1 object-form internal API layer with a misleading justification comment in compute-floating-position
- **Area/category:** libs/ui registry hooks (floating-position) / anti-slop / code-quality (YAGNI, dead indirection, misleading comment)
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/compute-floating-position.ts:9`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/compute-floating-position.ts:83`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/compute-floating-position.ts:97`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/compute-floating-position.ts:133-187`
- **Evidence:** The F007 remediation kept the public positional signatures (computePosition/wouldOverflow/shift/resolveCollisionPosition) by wrapping a new set of object-form internals (computePositionFrom/wouldOverflowFrom/shiftFrom/resolveCollisionPositionFrom). The comment at lines 133-135 states: "The object-form internals above are used by callers that benefit from named arguments." No such callers exist. `rg` across the repo (excluding dist) shows the only importers of compute-floating-position are use-floating-position.ts and the test file, both of which call the POSITIONAL public API exclusively. Of the four internals: computePositionFrom and wouldOverflowFrom are called internally by resolveCollisionPositionFrom (lines 116-117, 122) AND by their positional wrappers; but shiftFrom (line 83) is called only by the public shift wrapper (line 164), and resolveCollisionPositionFrom (line 97) is called only by the public resolveCollisionPosition wrapper (line 177). Those two internals are pure positional->object->destructure pass-throughs with a single caller and zero named-argument consumers, so the justifying comment is false. The whole file ships to public copy/package consumers (libs/ui/public/r/floating-position.json files = use-floating-position.ts, compute-floating-position.ts, floating-position-constants.ts), so the parallel-API duplication and the misleading comment land in the handoff source that shadcn/copy consumers read. Behavior is correct and the public positional signatures (the actual F007 fix) are preserved; this is a maintainability/anti-slop issue only, not a correctness or public-contract regression.
- **Fix:** Fix only the inaccurate comment; keep the wrappers (they are the F007 remediation). In libs/ui/registry/hooks/compute-floating-position.ts, replace the three-line comment at lines 133-135 with an accurate one that does not invoke non-existent external callers, e.g.:  // Public helpers keep the positional call shape so existing copy/package consumers // (including plain JS) keep working; the object-form internals are used internally // by resolveCollisionPosition for readable multi-argument calls.  Then regenerate the public registry so libs/ui/public/r/floating-position.json carries the corrected comment (run the repo's prepare:artifacts + validate:registry per the F007 verification gate). Do NOT collapse the wrappers or convert the internals to positional — that would regress the F007 intent of preserving the positional public API while keeping object-form internals.
- **Verifier:** The misleading-comment portion of the finding is verified true; the dead-indirection/"collapse the wrappers" portion is not actionable and is partly wrong. Verified facts: the c1dec6ee..HEAD diff for /Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/compute-floating-position.ts rename

### N19 ⚪ LOW — Active-session scope-key lookup does not dedup lenses the way review creation does, so a duplicate-lens resume query can miss its own session
- **Area/category:** cli/server/src/features/review / public-contract-consistency
- **Locations:** `cli/server/src/features/review/schemas.ts:36-46`, `cli/server/src/features/review/schemas.ts:51-55`, `cli/server/src/features/review/sessions.ts:21-37`, `libs/core/src/api/review.ts:114-131`
- **Evidence:** The remediation added scope-aware active-session lookup so a scoped review is discoverable on resume (schemas.ts ActiveSessionQuerySchema + review-routes.ts getActiveSessionHandler -> buildScopeKey). But the two scope-key inputs normalize lenses differently. CreateReviewBodySchema (schemas.ts:51-55) deduplicates lenses: `z.array(LensIdSchema).transform((arr) => [...new Set(arr)])`, so a create body of lenses ['a','a'] produces scopeKey `l:a` (buildScopeKey sorts but does NOT dedup, sessions.ts:30-31). ActiveSessionQuerySchema (schemas.ts:36-40) parses lenses via `csvToArray` with no Set dedup, so the query `?lenses=a,a` yields ['a','a'] and scopeKey `l:a,a`. getActiveSessionForProject (sessions.ts:373) compares scopeKey by strict equality, so the lookup misses a session it should match. The shared client helpers compound this: getActiveReviewSession (review.ts:125) sends `scope.lenses.join(',')` raw, and createReview likewise sends the raw array, so a caller passing duplicate lenses gets a create scopeKey deduped server-side but an active-session scopeKey that is not, yielding a spurious cache miss (a redundant duplicate review is created instead of resuming the in-flight one). Files are symmetric (neither path dedups them and buildScopeKey sorts), so only lenses are affected.
- **Fix:** Make the query path dedup lenses identically to creation, and do it before the `.max()` check so duplicates do not count toward MAX_LENSES (matching CreateReviewBodySchema, which dedups before max). In cli/server/src/features/review/schemas.ts, change the `csvToArray` transform on the `lenses` field of ActiveSessionQuerySchema to dedup, while leaving `files` (which is intentionally not deduped, symmetric with creation) using the plain `csvToArray`. Minimal change: add a lens-specific transform, e.g.    lenses: z     .string()     .optional()     .transform((value) => {       const items = csvToArray(value);       return items && [...new Set(items)];     })     .pipe(z.array(LensIdSchema).max(MAX_LENSES).optional()),  This yields ['a'] for `?lenses=a,a`, so buildScopeKey produces `l:a` on both the create and resume paths and getActiveSessionForProject matches. Keep `files` on the existing `csvToArray` transform unchanged (no dedup, mirroring CreateReviewBodySchema.files). No client change is needed in libs/core/src/api/review.ts since the server now normalizes; the comment there at review.ts:119-121 already documents the mirroring intent. Do not add a deprecated alias or extra config — this is a single in-place schema correction.
- **Verifier:** low is correct. The defect is real but currently unreachable from any in-repo consumer: every production caller of getActiveReviewSession passes mode only and no scope (apps/web/src/features/home/components/page.tsx:55 `api.getActiveReviewSession(mode)`; home-presentation.tsx:146,151 call with "unst

### N20 ⚪ LOW — Unreachable keys fallback in ownedFileHash (dead code)
- **Area/category:** cli/add remove command / anti-slop/dead-code
- **Locations:** `cli/add/src/commands/remove.ts:26-28`
- **Evidence:** ownedFileHash resolves the manifest record as `manifest[parsed.publicName] ?? (getKeysHookNames().has(parsed.name) ? manifest[`keys/${parsed.name}`] : undefined)`. `parseInstallName` always sets `publicName = `${namespace}/${name}``, so for any keys item `parsed.publicName === `keys/${parsed.name}``. The `??` fallback therefore re-reads the exact same manifest key (`keys/${parsed.name}`) that the left-hand side already evaluated to `undefined`. The branch can never return a value the first lookup did not, and the only caller (canRemoveFile -> item.name from getNamespacedItem) always passes a namespaced name, so the `getKeysHookNames().has(parsed.name)` guard is also moot. It is unreachable dead code that also pays the cost of loading/hashing the copy bundle (getKeysHookNames -> loadCopyBundle) for no effect.
- **Fix:** In cli/add/src/commands/remove.ts collapse the redundant lookup at lines 26-28 to a single read: replace the multi-line `const record = manifest[parsed.publicName] ?? (getKeysHookNames().has(parsed.name) ? manifest[`keys/${parsed.name}`] : undefined);` with `const record = manifest[parsed.publicName];`. Do NOT remove the `getKeysHookNames` import on line 5 — it is still used by resolveKeyName at line 215. No other changes needed.
- **Verifier:** Confirmed by reading the code. In ownedFileHash (cli/add/src/commands/remove.ts:26-28) the record is `manifest[parsed.publicName] ?? (getKeysHookNames().has(parsed.name) ? manifest[`keys/${parsed.name}`] : undefined)`. parseInstallName (cli/add/src/utils/namespaces.ts:17-30) sets publicName = `${nam

### N21 ⚪ LOW — `isApiError` type guard routed through two layers of app-local re-export indirection in the review feature
- **Area/category:** apps/web/src/features/review / code-quality (DRY / re-export indirection)
- **Locations:** `apps/web/src/features/review/hooks/use-review-error-handler.ts:6`, `apps/web/src/features/review/hooks/index.ts:1-2`, `apps/web/src/features/review/components/page.tsx:12`
- **Evidence:** `isApiError` is a pure type guard publicly exported from `@diffgazer/core/api` (libs/core/src/api/index.ts:3) and `@diffgazer/core/api/types` (libs/core/src/api/types.ts:15). In the review feature it is re-exported through two app-local layers solely to reach a consumer: `use-review-error-handler.ts:6` does `export { isApiError };` (re-exporting it from the hook module although the hook itself only uses it internally), then `hooks/index.ts:2` re-exports it again with a self-flagging comment on line 1 (`// isApiError is a type guard, not a hook`), and `page.tsx:12` imports it from `../hooks`. Meanwhile the identical symbol is imported directly from `@diffgazer/core/api` in `home-presentation.tsx:5`, so the same type guard now has two divergent import paths and the review path adds two pointless indirection hops. This matches the code-quality rule to kill re-export indirection; the in-source comment admits the symbol does not belong in the hooks barrel.
- **Fix:** In page.tsx change the import on line 12 to import the guard directly from core, matching home-presentation.tsx: keep `import { useReviewErrorHandler } from "../hooks";` and add `import { isApiError } from "@diffgazer/core/api";`. In use-review-error-handler.ts delete line 6 (`export { isApiError };`); the hook retains its own internal `import { isApiError } from "@diffgazer/core/api/types";` on line 4 (untouched to keep the change minimal). In hooks/index.ts delete line 1 (the self-flagging comment) and change line 2 to `export { useReviewErrorHandler } from "./use-review-error-handler";` so the barrel only re-exports real hooks. This removes both indirection hops and the slop comment while preserving behavior; no tests assert these import paths.
- **Verifier:** All cited facts verified against the actual code. `isApiError` is a pure type guard publicly exported from both `@diffgazer/core/api` (libs/core/src/api/index.ts:3) and `@diffgazer/core/api/types` (libs/core/src/api/types.ts:15), and both are valid public subpath exports (libs/core/package.json:80,8

### N22 ⚪ LOW — Stale comment in breadcrumbs contract test claims it skips when content is absent, but it hard-asserts presence
- **Area/category:** apps/docs / anti-slop
- **Locations:** `apps/docs/src/components/breadcrumbs.test.ts:35-37`
- **Evidence:** The comment reads: "Generated by `prepare:generated` (run by pretest). Skip if absent so the assertion never passes vacuously on a clean checkout." The very next line does the opposite of skipping: `expect(existsSync(CONTENT_DOCS)).toBe(true)`. There is no `it.skip`, no conditional `return`, and no `this.skip()` — the test fails hard when `content/docs` is missing. content/docs/ui/** and content/docs/keys/** are entirely gitignored (only content/docs/meta.json is committed; confirmed via `git check-ignore content/docs/ui/hooks/index.mdx`), so the directory only exists after `prepare:generated`. The chosen fail-loud behavior is actually correct, but the comment describes a 'skip' design that the code does not implement, so a maintainer reading it will misunderstand what happens on a clean checkout (it errors, it does not skip). This is a comment that contradicts the code it documents.
- **Fix:** Replace the misleading two-line comment at apps/docs/src/components/breadcrumbs.test.ts:35-36 with one that matches the implemented fail-loud behavior, e.g.: "// Generated by `prepare:generated` (run by pretest). Assert the directory exists // so the set comparison below can never pass vacuously on a clean checkout." Leave line 37 (`expect(existsSync(CONTENT_DOCS)).toBe(true)`) unchanged.
- **Verifier:** apps/docs/src/components/breadcrumbs.test.ts:35-37 — the comment reads "Skip if absent so the assertion never passes vacuously on a clean checkout," but line 37 is `expect(existsSync(CONTENT_DOCS)).toBe(true)`, a hard assertion. The file contains no `it.skip`, `this.skip()`, or conditional `return` 

### N23 ⚪ LOW — Hub tsconfig lacks noEmit, so `tsc -b` emits throwaway JS into dist during the Docker build (diverges from every sibling SPA)
- **Area/category:** apps/hub / build-script parity / correctness
- **Locations:** `apps/hub/tsconfig.json:10`, `apps/hub/tsconfig.json:11`, `apps/hub/package.json:9`, `deploy/hub.Dockerfile:20`, `deploy/hub.Dockerfile:25`
- **Evidence:** apps/hub/package.json:9 runs `build`: "tsc -b && vite build". But apps/hub/tsconfig.json sets `"outDir": "dist"` + `"rootDir": "src"` and does NOT set `noEmit`. Every other SPA in the repo uses `noEmit: true` for its build/type-check config: apps/web/tsconfig.json:` "noEmit": true`, apps/landing/tsconfig.json:15 `"noEmit": true`, apps/docs/tsconfig.json:16 `"noEmit": true`. Because hub omits it, `tsc -b` actually emits compiled `.js` (App.js, main.js, siteLinks.js, styles/) into apps/hub/dist before `vite build` runs. Vite 7 then empties dist (emptyOutDir defaults true because dist is inside root, and apps/hub/vite.config.ts sets no `root`), so the tsc emit is immediately discarded — wasteful, and the dual-output `dist` is confusing/inconsistent. The shipped artifact (deploy/hub.Dockerfile:25 COPYs apps/hub/dist) is the vite bundle, so it is not currently broken, but this is the one SPA whose `build` step is not type-check-only. If vite's empty-dir behavior is ever disabled or outDir is moved outside root, stale tsc `.js` would leak into the served SPA. This predates the remediation in tsconfig but the new hub.Dockerfile (F016) is what now ships `dist`, making the emit-vs-bundle ambiguity a live deploy concern.
- **Fix:** In apps/hub/tsconfig.json, replace the emitting config with the type-check-only contract used by sibling SPAs: remove "outDir": "dist" and "rootDir": "src" (lines 10-11) and add "noEmit": true. Resulting compilerOptions keep target/module/moduleResolution/jsx/strict/esModuleInterop/skipLibCheck and gain "noEmit": true. This makes `tsc -b` (build script line in apps/hub/package.json:9) type-check only and write just tsconfig.tsbuildinfo, leaving `vite build` as the sole producer of apps/hub/dist — matching apps/landing/tsconfig.json and removing the wasted emit and the latent stale-.js risk if emptyOutDir is ever disabled or outDir moves outside root. Scope the change to the noEmit swap only; do not pull in the siblings' extra lint/types flags, which are outside this finding.
- **Verifier:** Verified by reading the cited files and reproducing the mechanic in an isolated /tmp dir (no repo build run). apps/hub/tsconfig.json:10-11 sets "outDir": "dist" + "rootDir": "src" with NO "noEmit", while all three sibling SPAs set "noEmit": true (apps/landing/tsconfig.json:15, apps/web/tsconfig.json

### N24 ⚪ LOW — VITE_DOCS_ORIGIN is documented and read at runtime but never wired into the landing deploy path
- **Area/category:** apps/landing / deploy / dead config / deploy correctness
- **Locations:** `apps/landing/.env.example:6`, `apps/landing/src/content.ts:8`, `deploy/landing.Dockerfile:16`, `docker-compose.yml:61`
- **Evidence:** apps/landing/src/content.ts:8 reads `import.meta.env.VITE_DOCS_ORIGIN ?? "https://docs.b4r7.dev"` and apps/landing/.env.example:6 documents `VITE_DOCS_ORIGIN=https://docs.b4r7.dev`. Vite inlines VITE_-prefixed vars at build time. But deploy/landing.Dockerfile declares no `ARG VITE_DOCS_ORIGIN` / `ENV` (rg for VITE_/ARG in that file returns nothing), and the `landing` service in docker-compose.yml:61-66 has no `args:` block. Contrast the docs service: Dockerfile:20-24 declares `ARG VITE_PUBLIC_ORIGIN` / `ARG VITE_REGISTRY_ORIGIN` and docker-compose.yml:3-9 passes them through. So in any container build the env var is never present and the landing build always uses the hardcoded fallback. The deployed link still works only because the fallback string equals the production value — the documented `VITE_DOCS_ORIGIN` override is silently inert. This is misleading config (a knob that does nothing) and a latent break if the docs origin ever changes and someone relies on the env var instead of editing the source fallback.
- **Fix:** Wire the override through the deploy path to match the existing docs pattern, rather than deleting it (the env var was intentionally added per the 2026-05-28 audit). In deploy/landing.Dockerfile, before the `pnpm --filter @diffgazer/landing build` step (around line 16-20), add:    ARG VITE_DOCS_ORIGIN=https://docs.b4r7.dev   ENV VITE_DOCS_ORIGIN=${VITE_DOCS_ORIGIN}  In docker-compose.yml, add an `args:` block to the `landing` service `build:` (lines 62-64), mirroring the docs service:    build:     context: .     dockerfile: deploy/landing.Dockerfile     args:       VITE_DOCS_ORIGIN: ${VITE_DOCS_ORIGIN:-https://docs.b4r7.dev}  This makes the documented .env.example override take effect in the containerized build and removes the latent foot-gun if the docs origin changes. Leave content.ts, vite-env.d.ts, and .env.example as-is (they are correct and intentional).
- **Verifier:** Verified against the actual files. apps/landing/src/content.ts:8 reads `import.meta.env.VITE_DOCS_ORIGIN ?? "https://docs.b4r7.dev"` and apps/landing/.env.example:2,6 documents the var while explicitly noting "Vite inlines VITE_-prefixed values into the static client build at build time". deploy/lan

### N25 ⚪ LOW — Scoped active-session resume is wired only halfway: client scope param + server query parsing are unreachable from the React Query hook layer (dead capability + unfixed resume)
- **Area/category:** libs/core (api hooks) + cli/server (review) / correctness / incomplete-remediation / dead-code
- **Locations:** `libs/core/src/api/hooks/queries/review.ts:22-27`, `libs/core/src/api/hooks/review.ts:16-19`, `libs/core/src/api/review.ts:108-131`, `libs/core/src/api/review.ts:175-176`, `cli/server/src/features/review/schemas.ts:21-47`, `cli/server/src/features/review/review-routes.ts:91-127`, `libs/core/src/api/review.test.ts:145-161`
- **Evidence:** The remediation extended the active-session resume path to be scope-aware. On the server, `ActiveSessionQuerySchema` (cli/server/src/features/review/schemas.ts) now parses `profile`/`lenses`/`files` query params and `getActiveSessionHandler` (review-routes.ts:112) feeds them through `buildScopeKey(...)` into `getActiveSessionForProject({..., scopeKey})`. On the client, `getActiveReviewSession` (libs/core/src/api/review.ts:114-131) gained a `scope` arg and the bound API exposes `getActiveReviewSession: (mode?, scope?)` (review.ts:175-176). But the only production consumer — the React Query layer at libs/core/src/api/hooks/queries/review.ts:25 — calls `api.getActiveReviewSession(mode)` with NO scope, and `useActiveReviewSession(mode)` (hooks/review.ts:16) only forwards `mode`. Every app/CLI caller confirms this: apps/web/src/features/home/components/home-presentation.tsx:146,151 and cli/diffgazer/src/app/screens/home-screen.tsx:40 call it with mode only (or nothing). Reviews are also only ever CREATED mode-only: apps/web/.../home-presentation.tsx:132 (`createReview({ mode })`) and cli/diffgazer/.../use-review-lifecycle.ts:90 (`createReview.mutateAsync({ mode: selectedMode })`). Net effect: the new server query-param scope parsing and the client `scope` parameter are unreachable dead code; and the stated goal (a scoped review discoverable on resume) is still not achievable through the public hooks, because a scoped session (non-empty `scopeKey` from buildScopeKey at service.ts:122) would be looked up with the empty scope key (`getActiveSessionForProject` defaults `scopeKey ?? ""` at sessions.ts:367) and never match (sessions.ts:374). The test at review.test.ts:145 ('sends scope params so a scoped review is discoverable on resume') asserts a code path no integrated consumer exercises, giving false confidence.
- **Fix:** No consumer creates scoped reviews and none resumes with scope, so per AGENTS.md ("prefer deleting dead code over preserving fallback branches before first public release"; "no abstractions without real consumers") delete the unreachable scope capability rather than thread it through: (1) libs/core/src/api/review.ts — drop the `scope: ActiveReviewSessionOptions` param and its serialization block from getActiveReviewSession (114-131), remove the ActiveReviewSessionOptions interface (108-112), and revert the bound wrapper to getActiveReviewSession: (mode?) => getActiveReviewSession(client, mode) (175-176); (2) cli/server/src/features/review/schemas.ts — remove profile/lenses/files (and csvToArray, MAX_LENSES if now unused) from ActiveSessionQuerySchema (21-46), leaving { mode }; (3) cli/server/src/features/review/review-routes.ts — drop profile/lenses/files from getActiveSessionHandler's query type and the buildScopeKey call (92-125), calling getActiveSessionForProject without scopeKey (the ?? '' default already matches mode-only sessions); (4) delete the two scope tests in libs/core/src/api/review.test.ts:145-172. If scoped resume is actually a planned product feature, instead make it end-to-end: add an optional scope to reviewQueries.activeSession and useActiveReviewSession, include it in the queryKey, forward it to api.getActiveReviewSession(mode, scope), and have the home/TUI resume callers pass the same scope they use for createReview (which today is none) — but only do this once a consumer creates scoped reviews.
- **Verifier:** Verified end-to-end against the code. CLIENT: getActiveReviewSession gained a scope arg (libs/core/src/api/review.ts:114-131) and the bound wrapper exposes (mode?, scope?) (review.ts:175-176). HOOK CHOKEPOINT: reviewQueries.activeSession calls api.getActiveReviewSession(mode) with no scope (libs/cor

### N26 ⚪ LOW — Floating-position helpers duplicated into private object-arg twins forwarded by thin positional wrappers (no consumer uses the object form)
- **Area/category:** libs/ui (registry hooks) / anti-slop / DRY / KISS over-engineering
- **Locations:** `libs/ui/registry/hooks/compute-floating-position.ts:9-187`, `libs/ui/registry/hooks/use-floating-position.ts:120-127`, `libs/ui/public/r/floating-position.json`
- **Evidence:** The remediation (F007: restore positional public signatures) split each helper into a private object-arg `*From` twin plus a public positional wrapper that does nothing but re-pack the args and call the twin: `computePosition` -> `computePositionFrom`, `wouldOverflow` -> `wouldOverflowFrom`, `shift` -> `shiftFrom`, `resolveCollisionPosition` -> `resolveCollisionPositionFrom` (compute-floating-position.ts:136-187). The justifying comment (lines 133-135) claims 'object-form internals above are used by callers that benefit from named arguments,' but a repo-wide search shows the `*From` functions are called ONLY by their own wrappers and by `resolveCollisionPositionFrom` (which itself is only reached via its wrapper). The sole external consumer, use-floating-position.ts:123-127, calls the positional public functions. So three of the four object-form twins exist purely as a redundant indirection layer; the public registry JSON (floating-position.json) ships this duplicated source to copy/package consumers.
- **Fix:** Collapse the parallel object-form layer in libs/ui/registry/hooks/compute-floating-position.ts while keeping the F007-mandated positional public signatures (pinned by testing/compute-floating-position.test.ts:64-71). Concretely: (1) Delete the four `*From` functions (computePositionFrom, wouldOverflowFrom, shiftFrom, resolveCollisionPositionFrom). (2) Move each function body directly into the public positional function (computePosition, wouldOverflow, shift, resolveCollisionPosition) by destructuring the positional params instead of an options object. (3) Inside resolveCollisionPosition's loop, call the now-positional computePosition(triggerRect, contentRect, side, preferredAlign, sideOffset, alignOffset) and wouldOverflow(pos.x, pos.y, contentRect, collisionPadding, vp) directly. (4) Remove the misleading comment at lines 133-135. (5) Regenerate libs/ui/public/r/floating-position.json via the repo's artifact pipeline (pnpm run prepare:artifacts) so the committed registry contract stays in sync with the source, then pnpm run validate:artifacts:check. No docs/examples/exports change: hook-docs/floating-position.ts documents only useFloatingPosition, and no file imports the helper functions besides the hook and its tests.
- **Verifier:** Confirmed by reading libs/ui/registry/hooks/compute-floating-position.ts and a repo-wide rg. Each of the four helpers exists twice: a private object-arg `*From` (computePositionFrom:9, wouldOverflowFrom:67, shiftFrom:83, resolveCollisionPositionFrom:97) and a public positional wrapper that only re-p

### N27 ⚪ LOW — Active-session scope lookup is half-wired: no consumer passes scope, so scoped reviews remain undiscoverable on resume despite the new plumbing
- **Area/category:** cli/server + libs/core review API / correctness / dead-capability (incomplete remediation, YAGNI)
- **Locations:** `libs/core/src/api/review.ts:108-131`, `libs/core/src/api/review.ts:175-176`, `libs/core/src/api/hooks/queries/review.ts:22-26`, `libs/core/src/api/hooks/review.ts:16-19`, `apps/web/src/features/home/components/page.tsx:55`, `apps/web/src/features/home/components/home-presentation.tsx:146-151`, `cli/server/src/features/review/schemas.ts:29-46`, `cli/server/src/features/review/review-routes.ts:92-124`
- **Evidence:** The remediation added end-to-end scope support to the active-session LOOKUP: getActiveReviewSession(client, mode, scope) now serializes profile/lenses/files into CSV query params (libs/core/src/api/review.ts:114-131), ActiveSessionQuerySchema parses them (schemas.ts:33-46), and getActiveSessionHandler now builds a scopeKey via buildScopeKey and passes it to getActiveSessionForProject (review-routes.ts:115-124). The schema/API comments and the new test (review.test.ts: 'sends scope params so a scoped review is discoverable on resume') state the goal is that a scoped review is 'discoverable by resume'. But NO consumer ever passes scope: the React Query layer is reviewQueries.activeSession(api, mode) -> api.getActiveReviewSession(mode) (queries/review.ts:25) and useActiveReviewSession(mode) (hooks/review.ts:18) have no scope parameter at all; the only two direct callers, page.tsx:55 and home-presentation.tsx:146/151, call getActiveReviewSession('unstaged'/'staged') with no scope. Since getActiveSessionForProject already matched on scopeKey (default '') before the remediation, and createSession already stores a non-empty scopeKey for scoped reviews (service.ts:122-134), a review created with files/lenses/profile has scopeKey 'f:...'/'l:...'/'p:...', while every real lookup sends an empty scope -> empty scopeKey '' -> the session is never found. The added scope arg on getActiveReviewSession, the ActiveReviewSessionOptions interface, and the new lenses/files/profile fields on ActiveSessionQuerySchema are therefore unreachable in production, and the documented 'discoverable by resume' behavior does not hold through any path. The review.test.ts case only asserts request serialization (toHaveBeenCalledWith), giving false confidence in a feature no caller uses.
- **Fix:** Treat this as dead-capability cleanup, not a correctness fix. Since scoped reviews cannot be created through any production path either, scoped resume is out of scope for this release: revert the unreachable lookup-side surface. Restore getActiveReviewSession(client, mode?) to its prior mode-only signature and remove the scope arg + ActiveReviewSessionOptions interface (libs/core/src/api/review.ts:108-131) and the bindReview scope param (review.ts:175-176); remove the profile/lenses/files fields and csvToArray + the "discoverable by resume" comment from ActiveSessionQuerySchema (cli/server/src/features/review/schemas.ts:29-46), reverting getActiveSessionHandler to take mode only and drop the buildScopeKey call there (review-routes.ts:92-124) so the lookup uses the empty scopeKey consistent with how all sessions are created; and delete the "sends scope params so a scoped review is discoverable on resume" / "omits empty scope arrays" tests (libs/core/src/api/review.test.ts:145-172) plus the matching schema comment, since they assert a behavior no path delivers. Only if scoped resume is actually intended for this release should you instead complete BOTH ends: thread scope through useActiveReviewSession(mode, scope) -> reviewQueries.activeSession(api, mode, scope) (include scope in the query key) -> api.getActiveReviewSession(mode, scope), AND wire scoped createReview callers so a session with a non-empty scopeKey can be created, then have the resume flow pass the identical profile/lenses/files. Do not half-wire only the lookup side.
- **Verifier:** The lookup-side scope plumbing added by the remediation is genuinely unreachable in production. The React Query layer carries no scope: reviewQueries.activeSession(api, mode) calls api.getActiveReviewSession(mode) (libs/core/src/api/hooks/queries/review.ts:22-25) and useActiveReviewSession(mode) (li

### N28 ⚪ LOW — Remediation introduced new audit/ticket-ID comments (F005/F006/F007/F012/F015) into test source, the same anti-slop pattern the audit removed from runtime source
- **Area/category:** changed test files (libs/ui registry hooks, scripts/monorepo) / anti-slop (audit/ticket-id comments in source)
- **Locations:** `libs/ui/registry/hooks/testing/use-active-heading.test.ts:268`, `libs/ui/registry/hooks/testing/use-active-heading.test.ts:313`, `libs/ui/registry/hooks/testing/use-active-heading.ssr.test.tsx:6`, `libs/ui/registry/hooks/testing/compute-floating-position.test.ts:51`, `scripts/monorepo/package-scripts.test.mjs:11`, `scripts/monorepo/package-scripts.test.mjs:28`
- **Evidence:** git diff c1dec6ee..HEAD shows these comments are newly ADDED by this remediation: 'F006: filtering must derive the constructor from ownerDocument.defaultView' and 'Before the F006 fix ...' (use-active-heading.test.ts), 'F005: the hook must not read browser globals during render' (use-active-heading.ssr.test.tsx), 'F007: the public helper must keep the positional signature' (compute-floating-position.test.ts), and 'F012: ...' / 'F015: ...' (package-scripts.test.mjs). The same remediation deleted F148/F155/F100/§6 ticket-ID comments from runtime source (app.ts, sessions.ts, config/store.ts), so it is internally inconsistent: it strips ticket IDs from src while seeding new ones into tests. Audit/ticket-ID references rot once the audit closes and add no information a reader needs to understand the test.
- **Fix:** Strip only the `F0xx` / "Before the F0xx fix" tokens from these six comments; keep the behavior rationale (the test names already describe what is under test). Concretely: (1) use-active-heading.test.ts:268 -> "...are NOT `instanceof` the host realm's HTMLElement, so filtering must derive the constructor from `ownerDocument.defaultView`, not the host."; (2) use-active-heading.test.ts:313 -> "A host-realm `instanceof HTMLElement` filter would drop both cross-realm headings and activeId would fall back to the first id (\"x1\")."; (3) use-active-heading.ssr.test.tsx:6 -> "...`document` and `window` are undefined here, so the hook must not read browser globals during render."; (4) compute-floating-position.test.ts:64 -> "The public helper must keep the positional signature so plain-JS copy/package consumers (no TS checking) keep getting correct results."; (5) package-scripts.test.mjs:11 -> "The benchmark only gates latency/throughput SLOs under strict mode, so the CI/release chains must run `pnpm run bench` with the strict env var or breaches silently pass."; (6) package-scripts.test.mjs:28 -> "The dead review opt-in contract was removed; no script env name should reintroduce it." No code/logic changes, comments only.
- **Verifier:** Confirmed by `git diff c1dec6ee..HEAD` and current-tree `rg`: the remediation added six audit/ticket-ID comments into test source that did not exist at the base commit (`git grep` for F-ID comments in *.test.* at c1dec6ee returns nothing). Exact in-code occurrences: libs/ui/registry/hooks/testing/us

### N29 ⚪ LOW — Audit ticket IDs (F005/F006/F007) left in committed registry test comments
- **Area/category:** libs/ui / anti-slop
- **Locations:** `libs/ui/registry/hooks/testing/use-active-heading.ssr.test.tsx:6`, `libs/ui/registry/hooks/testing/use-active-heading.test.ts:268`, `libs/ui/registry/hooks/testing/use-active-heading.test.ts:313`, `libs/ui/registry/hooks/testing/compute-floating-position.test.ts:64`
- **Evidence:** Four committed test comments embed remediation ticket IDs that mean nothing to a future maintainer: `// ... F005: the hook must not read browser globals during render.` (use-active-heading.ssr.test.tsx:6); `// ... F006: filtering must derive the constructor from ownerDocument.defaultView, not the host.` (use-active-heading.test.ts:268); `// Before the F006 fix the host-realm instanceof HTMLElement filter dropped ...` (use-active-heading.test.ts:313); `// F007: the public helper must keep the positional signature so existing plain-JS copy/package consumers ...` (compute-floating-position.test.ts:64). The anti-slop checklist explicitly flags audit/ticket-id comments in committed source (e.g. 'F148'). The surrounding prose is genuinely useful; only the bare audit IDs are slop -- they reference an audit document that won't travel with the code.
- **Fix:** Strip the audit ticket-id prefixes while keeping the explanatory sentences. (1) use-active-heading.ssr.test.tsx:5-7 -> `// Runs in the "ssr" vitest project (node environment, no jsdom), so \`document\`\n// and \`window\` are undefined here. The hook must not read browser globals\n// during render.` (2) use-active-heading.test.ts:267-269 -> `// A second JSDOM realm has its own HTMLElement constructor, so its elements\n// are NOT \`instanceof\` the host realm's HTMLElement. Filtering must\n// derive the constructor from \`ownerDocument.defaultView\`, not the host.` (3) use-active-heading.test.ts:313-314 -> `// With a host-realm \`instanceof HTMLElement\` filter, both cross-realm headings\n// would be dropped and activeId would fall back to the first id ("x1").` (4) compute-floating-position.test.ts:64-65 -> `// The public helper must keep the positional signature so existing plain-JS\n// copy/package consumers (no TS checking) keep getting correct results.`
- **Verifier:** All four cited comment lines exist verbatim and were added by this remediation (git diff c1dec6ee..HEAD shows them as net-new `+` lines): libs/ui/registry/hooks/testing/use-active-heading.ssr.test.tsx:6 (`F005: the hook must not read browser globals`), libs/ui/registry/hooks/testing/use-active-headi

### N30 ⚪ LOW — useReviewStream.resume leaves isStreaming=true on SESSION_STALE / SESSION_NOT_FOUND, leaving the hook's state machine stuck mid-stream
- **Area/category:** libs/core/src/api/hooks (review stream lifecycle) / react-state-machine / correctness (incomplete terminal transition)
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/core/src/api/hooks/use-review-stream.ts:84`, `/Users/voitz/Projects/diffgazer-workspace/libs/core/src/api/hooks/use-review-stream.ts:109-114`, `/Users/voitz/Projects/diffgazer-workspace/cli/diffgazer/src/features/review/hooks/use-review-lifecycle.ts:63-69`
- **Evidence:** resume() dispatches START at line 84 (reducer sets isStreaming=true, see review-state.ts:307-308). On every other terminal outcome resume dispatches a state-clearing action: ok -> COMPLETE_WITH_RESULT (isStreaming=false), generic error -> ERROR (isStreaming=false), abort -> RESET. But on the SESSION_STALE / SESSION_NOT_FOUND branch (lines 109-114) it returns err(result.error) WITHOUT dispatching any action, so state.isStreaming stays true and state.error stays null. The hook hands the error to the caller while its own observable state remains 'streaming'. The web consumer hides this by navigating away in onStaleSession/onNotFoundInSession, but the CLI consumer (cli/diffgazer use-review-lifecycle.ts:63-69) wires neither callback, so if a freshly created session is reported stale/not-found when the stream opens, the hook is left permanently reporting isStreaming=true with error=null (perpetual loading, no error surfaced). The committed test 'returns SESSION_STALE to the caller without surfacing it as a stream error' only asserts state.error===null and never checks isStreaming, so the inconsistency is untested.
- **Fix:** In libs/core/src/api/hooks/use-review-stream.ts, dispatch a terminal action on the stale/not-found branch before returning so the hook's own state is self-consistent. Change lines 109-114 to dispatch RESET (matching abort()'s treatment of an unrecoverable stream and clearing the dangling reviewId) immediately before returning:        if (         result.error.code === ReviewErrorCode.SESSION_STALE ||         result.error.code === ReviewErrorCode.SESSION_NOT_FOUND       ) {         dispatch({ type: "RESET" });         return err(result.error);       }  This keeps the caller-facing contract intact (still returns err(result.error) so useReviewStart fires onStaleSession/onNotFoundInSession, and state.error stays null per the existing test) while leaving isStreaming=false. Then extend the existing "returns SESSION_STALE to the caller without surfacing it as a stream error" test (use-review-stream.test.ts:247-271) to assert `expect(result.current.state.isStreaming).toBe(false)` after resume resolves, so the terminal transition is covered.
- **Verifier:** Verified against source. In libs/core/src/api/hooks/use-review-stream.ts, resume() dispatches START at line 84, which the reducer (libs/core/src/review/review-state.ts:307-308) turns into { isStreaming: true }. Every terminal outcome in resume() then dispatches a state-clearing action: result.ok -> 

### N31 ⚪ LOW — isStepEvent's hardcoded type Set duplicates the StepEventSchema discriminant literals, creating an untested misrouting drift hazard
- **Area/category:** libs/core/src/schemas/events / DRY / source-of-truth duplication; reducer-routing correctness hazard
- **Locations:** `libs/core/src/schemas/events/step.ts:48-53`, `libs/core/src/schemas/events/step.ts:82-86`, `libs/core/src/review/review-state.ts:272-303`
- **Evidence:** STEP_EVENT_TYPES = new Set(["review_started","step_start","step_complete","step_error"]) (step.ts:82) re-types by hand the same four discriminants already declared as z.literal(...) in StepEventSchema.options (ReviewStartedEventSchema/StepStartEventSchema/StepCompleteEventSchema/StepErrorEventSchema, step.ts:48-53). isStepEvent (step.ts:84) is the sole router predicate in dispatchEvent (review-state.ts:273): step events go to handleStepEvent, everything else falls through to the agent/issue handler (review-state.ts:297-302). The two lists are not derived from one another and nothing ties them together — there is no test file under schemas/events/ and no test references STEP_EVENT_TYPES or isStepEvent. If a fifth step event is later added to the discriminated union but not to the Set, isStepEvent returns false for it and dispatchEvent silently misroutes it through updateAgents/updateIssues instead of handleStepEvent, with no type error and no failing test.
- **Fix:** Derive the Set from the schema so StepEventSchema is the single source of truth. The finding's proposed `option.shape.type.value` works but uses zod v4's @legacy accessor — /Users/voitz/node_modules/zod/v4/classic/schemas.d.ts:581-582 marks `ZodLiteral.value` @legacy and says "Use `.values` instead." The repo pins zod ^4.3.6, so prefer the non-legacy `.values` Set (which also future-proofs multi-value literals): in libs/core/src/schemas/events/step.ts replace line 82 with `const STEP_EVENT_TYPES = new Set<string>(StepEventSchema.options.flatMap((option) => [...option.shape.type.values]));`. This keeps the existing isStepEvent/StepEvent contract unchanged and matches the established StepEventSchema.options pattern at stream.ts:17-20, so adding or renaming a step event can no longer drift the router. Optionally add one regression test under libs/core/src/schemas/events/ asserting isStepEvent returns true for every StepEventSchema.options discriminant, but the schema-derived Set already removes the drift hazard.
- **Verifier:** The duplication and drift hazard are verifiable in the source. libs/core/src/schemas/events/step.ts:48-53 declares StepEventSchema as a discriminatedUnion over four named member schemas whose discriminants are z.literal("review_started"|"step_start"|"step_complete"|"step_error") (step.ts:22,28,34,41

### N32 ⚪ LOW — useFloatingPosition observes resize with the host realm's ResizeObserver, not the trigger's own view, so cross-realm (iframe) repositioning misses size changes
- **Area/category:** libs/ui/registry/hooks/use-floating-position / cross-realm / F006 incomplete-remediation / correctness
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/ui/registry/hooks/use-floating-position.ts:160-163`, `/Users/voitz/Projects/diffgazer-workspace/libs/ui/public/r/floating-position.json:11`
- **Evidence:** The F006 remediation made this hook explicitly cross-realm aware: the viewport, scroll/resize listeners, and rAF scheduling are all derived from the trigger's own `view = trigger.ownerDocument?.defaultView ?? window` (lines 117, 141, 169, 181), and a dedicated test (use-floating-position.test.ts:361-426 "derives viewport and listener target from the trigger's ownerDocument") asserts listeners attach on the trigger's view and never on the host window. The sibling hook use-active-heading.ts:203-204 was fixed in the same remediation to read the observer constructor from the trigger's realm: `if (observe && typeof view.MutationObserver === "function") { mutationObs = new view.MutationObserver(...) }`. But useFloatingPosition still reads the host realm's constructor: line 160 `const ResizeObserverCtor = globalThis.ResizeObserver;` then observes the cross-realm `trigger` and `content` nodes (lines 162-163). A ResizeObserver instantiated from the host document does not deliver resize notifications for elements that live in a different document; per-realm observers only report on their own document's layout. So when the trigger/content are inside an iframe document, size-driven repositioning (content growing/shrinking, trigger resizing) never reschedules `update()` — the floating element drifts out of alignment until an unrelated scroll/resize event fires. This is a distinct defect from the host-realm `instanceof HTMLElement` at line 80 (which breaks scroll-ancestor discovery): this one breaks resize-driven repositioning, and it is wholly untested — the cross-doc test never defines `ResizeObserver` on `altView` (use-floating-position.test.ts:364-377) and the only ResizeObserver test (lines 325-359) runs in the same realm via `globalThis`.
- **Fix:** In libs/ui/registry/hooks/use-floating-position.ts, hoist the existing view computation above the observer setup and read the ResizeObserver constructor from it, mirroring the use-active-heading fix. After line 158 (the `if (!trigger || !content) return;` guard) add `const view = trigger.ownerDocument?.defaultView ?? window;`, change line 160 to `const ResizeObserverCtor = view.ResizeObserver;`, and remove the now-duplicate `const view = trigger.ownerDocument?.defaultView ?? window;` that currently sits at line 169 (reuse the hoisted one for the scroll/resize listeners and cleanup). Apply the identical edit to the mirrored source string in libs/ui/public/r/floating-position.json:11 so source and public registry stay byte-identical. Extend the cross-doc test (libs/ui/registry/hooks/testing/use-floating-position.test.ts:361-426) by adding a ResizeObserver class to altView whose observe()/disconnect() are spies, and assert observe is invoked for the trigger and content from the alt view's constructor (and that globalThis.ResizeObserver is not used) — confirming resize observation runs against the trigger's own realm.
- **Verifier:** Confirmed real and consistent with the code as written. use-floating-position.ts was made cross-realm aware by F006: it derives the viewport (line 120), rAF scheduler (line 141), and scroll/resize listeners (line 169) from the trigger's own view = trigger.ownerDocument?.defaultView ?? window. But li

### N33 ⚪ LOW — Public registry INDEX files[] are never validated (no safety check, no source comparison) — F013 path-safety coverage gap
- **Area/category:** libs/registry/src/shadcn (public registry freshness/path-safety validation) / security / incomplete-remediation (path safety + source<->generated drift)
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/registry/src/shadcn/validate.ts:139`, `/Users/voitz/Projects/diffgazer-workspace/libs/registry/src/shadcn/validate.ts:54`, `/Users/voitz/Projects/diffgazer-workspace/libs/registry/src/shadcn/validate.ts:152`
- **Evidence:** The F013 remediation added ensureSafeFilePath(...) only to two collections: the (transformed) source item files (validate.ts:149-151) and the per-item public JSON `<name>.json` files (validate.ts:152-154). The public registry INDEX item (`public/r/registry.json` -> `publicByName.get(name)`, validate.ts:129) is passed only to compareItemFields (validate.ts:139), and compareItemFields iterates `itemFields`, which is explicitly built with `.filter((field) => field !== "files")` (validate.ts:54-56). I confirmed via rg that `publicItem` is referenced nowhere else in the file. Net result: the index `files` array — every `files[].path` (and `files[].target`) in `public/r/registry.json` — is never run through ensureSafeFilePath and is never compared against the source registry. The published index does carry real install paths (e.g. libs/ui/public/r/registry.json items each have a populated `files[].path`). In the normal pipeline runner.ts:45 produces the index by copyFileSync of the safety-checked source registry, so in practice the paths match; but validatePublicRegistryFresh provides no guarantee of that. A stale, hand-edited, or drifted index whose `files[].path` contains an absolute or `..`-escaping segment (or simply diverges from source) passes validation cleanly, even though the equivalent unsafe path in the per-item JSON or source is now correctly rejected. This is an asymmetry within the very lens (F013) the remediation targeted: the index is part of the public registry contract but is exempt from both the new path-safety check and any source-equality check.
- **Fix:** In validatePublicRegistryFresh, inside the `if (!isHidden)` block after the `publicItem` null check (validate.ts:128-139), validate and compare the index files. Because `publicRegistry` is parsed with RegistrySchema, `publicItem.files` is non-optional `RegistryFile[]`, so no `?? []` guard is needed (unlike the per-item JSON which uses PublicItemSchema with optional files). Add:    for (const file of publicItem.files) {     ensureSafeFilePath(file.path, sourceItem.name, "public registry index");   }   ensureSameValue({     label: "index files",     a: expectedItem.files.map((file) => file.path),     b: publicItem.files.map((file) => file.path),     itemName: sourceItem.name,     fixCommand,   });  Compare against `expectedItem.files` (the transformed source) — matching the per-item JSON comparison at validate.ts:160 — so transformSourceItem rewrites (e.g. target/type) stay consistent and path is the equality basis. This closes the asymmetry with helpers already in the file and adds no new abstraction. Add one behavior test mirroring the existing 'rejects an unsafe file path that only appears in the public registry artifact' test (shadcn.test.ts:426) but injecting the unsafe path into the index via setupRegistry's `publicItems` argument (e.g. publicItems = [{ name: 'button', files: [{ path: '../escape.tsx' }] }] with safe source + safe per-item JSON), asserting it throws /Unsafe registry file path/; optionally a second test where the index path merely diverges from source (safe but different) asserting the new 'index files' mismatch is thrown.
- **Verifier:** Verified against the actual code. (1) The public index item (`publicItem`, validate.ts:129) is consumed only at validate.ts:139 via compareItemFields; `rg` confirms no other reference. (2) compareItemFields iterates `itemFields`, which is explicitly built as `Object.keys(RegistryItemSchema.shape).fi

### N34 ⚪ LOW — F018 graceful session teardown (shutdownSessions) is never invoked in the production binary; only the dev server wires it
- **Area/category:** cli/server review sessions / cli/diffgazer embedded server lifecycle / correctness / incomplete-remediation
- **Locations:** `cli/server/src/features/review/sessions.ts:191`, `cli/server/src/dev.ts:18`, `cli/server/src/index.ts:3`, `cli/diffgazer/src/lib/servers/embedded-server.ts:96`, `cli/diffgazer/src/lib/servers/embedded-server.ts:158`
- **Evidence:** The F018 remediation rewrote shutdownSessions() (sessions.ts:191) to abort every active session's AbortController, store a terminal SESSION_STALE error, notify subscribers, run completion listeners, and clear subscribers/listeners so 'no SSE client keeps the process alive' (per its JSDoc and the spec). However, a clean grep (`grep -rn shutdownSessions cli/server/src cli/diffgazer/src --include='*.ts*' | grep -v test`) shows it is imported and called in exactly one place: cli/server/src/dev.ts:18 — the DEV server, which spawns the API as a separate child process via createApiServer. The shipped PRODUCTION path is cli/diffgazer createProdServerFactories -> createEmbeddedServer, which runs the Hono app IN-PROCESS by calling `createApp()` (embedded-server.ts:96-101) and whose stop() (embedded-server.ts:158-169) only calls `server.close()`. It never calls shutdownSessions(). Moreover @diffgazer/server only re-exports createApp (index.ts:3) — shutdownSessions is not part of the package's public surface, so the embedded server cannot even import it as written. app.ts registers no SIGTERM/SIGINT handler and no shutdown hook. Net effect in the shipped binary: on shutdown, in-flight reviews' AbortControllers are never aborted (AI provider requests and spawned git subprocesses are abandoned rather than cancelled) and active SSE subscribers never receive the terminal SESSION_STALE event. The cli/diffgazer web-launcher's stopWithTimeout + process.exit(0) (web-launcher.ts:35-36) is the only thing that prevents an indefinite hang on an open SSE connection, so the entire careful F018 teardown is dead code in production and is replaced by a brute-force process kill. The dev-only sessions.test.ts coverage of shutdownSessions gives a false impression that production shutdown is covered.
- **Fix:** Wire shutdownSessions into the production embedded-server lifecycle, statically (matching how dev.ts already imports it) rather than via dynamic import. 1) Re-export it from the package entry: in cli/server/src/index.ts add `export { shutdownSessions } from "./features/review/sessions.js";` alongside the existing createApp export. 2) In cli/diffgazer/src/lib/servers/embedded-server.ts, import it at top (`import { shutdownSessions } from "@diffgazer/server";`) and call it in stop() before closing the server so active AbortControllers are aborted and SSE subscribers are drained, letting server.close() resolve before the 3000ms graceful timeout: change stop() to set `state = "stopped"; shutdownSessions(); if (!server) return Promise.resolve(); const closing = server; server = null; return new Promise<void>((resolve) => closing.close(() => resolve()));`. This makes the careful F018 teardown actually run in the shipped binary instead of relying on the process.exit(0) backstop, and avoids the ~3s shutdown delay on an open SSE connection. Note for severity context: the audit's F018 (THERMO-NUCLEAR-AUDIT.md:178-185) was explicitly scoped to the standalone/dev server, which the remediation fixed correctly; this is a real but bounded production gap, not a botched F018 fix.
- **Verifier:** duplicate-not-allowed

### N35 ⚪ LOW — Unhandled-error path double-logs every crashed request and the onError header-set is dead code (justifying comment is factually wrong)
- **Area/category:** cli/server/src/app.ts / correctness / anti-slop
- **Locations:** `cli/server/src/app.ts:141-157`, `cli/server/src/app.ts:153-155`, `cli/server/src/shared/middlewares/request-logger.ts:28-38`
- **Evidence:** The remediation reworded the comment block above app.onError, whose final line still asserts: "A thrown error skips the request-logger tail, so set the id header here too." That premise is false for Hono 4.12. Hono composes the error handler INSIDE the middleware chain: hono-base #dispatch calls compose(matchResult[0], this.errorHandler, ...), and compose.js catches the throw at the dispatch level where it occurs, runs onError, sets context.res, and RESOLVES the chain. The upstream requestLogger's `await next()` therefore resolves normally and its tail (request-logger.ts:30-38) runs. I confirmed this empirically against the installed hono@4.12.19: an upstream `app.use('*')` middleware that sets X-Request-Id and logs after `await next()`, plus a route that throws and an onError that returns a 500 and sets its own X-Request-Id, produced: status 500, final X-Request-Id = the logger-tail value (NOT the onError value), and BOTH log entries fired (["onError ran","TAIL ran, status=500"]). Consequences in app.ts: (1) every unhandled error emits TWO error-level log lines for the same request — `unhandled_error` from onError (app.ts:144) and `request` with status 500 from the requestLogger tail (levelForStatus(500)='error'); in packaged mode both pass the F008 `warn` threshold, so the very terminal-noise reduction F008 targeted is doubled on each crash. (2) The manual `response.headers.set(REQUEST_ID_HEADER, requestId)` in onError (app.ts:154-155) is dead: the requestLogger tail unconditionally re-sets the identical X-Request-Id afterward and wins.
- **Fix:** In cli/server/src/app.ts onError (lines 141-157): (a) delete the false comment at line 153; (b) delete the dead manual header write at line 155 (`if (requestId) response.headers.set(REQUEST_ID_HEADER, requestId);`) and just `return errorResponse(c, "Internal Server Error", ErrorCode.INTERNAL_ERROR, 500);`; (c) since the requestLogger tail already logs the completed request with method/path/status:500/durationMs, trim those duplicated fields from the `unhandled_error` log call and keep only the diagnostic the tail cannot provide — requestId plus error message and stack: `log("error", "unhandled_error", { requestId, error: err instanceof Error ? err.message : String(err), stack: err instanceof Error ? err.stack : undefined });`. Then drop the now-unused `startTime`/`durationMs` computation and the `REQUEST_ID_HEADER` import if it becomes unused in app.ts. This corrects the false premise, removes the dead header write, and removes the duplicate request-shaped log fields without changing client-visible behavior (the 500 body and X-Request-Id header are still set by errorResponse + requestLogger tail).
- **Verifier:** Confirmed against installed hono@4.12.19 source. node_modules/.pnpm/hono@4.12.19/.../dist/hono-base.js:298 composes the error handler INSIDE the middleware chain: `compose(matchResult[0], this.errorHandler, this.#notFoundHandler)`. In compose.js:20-40 each `dispatch(i)` wraps `handler(context, () =>

### N36 ⚪ LOW — readOrCreateProjectFile is a re-export alias with no runtime consumer (test-only dead export)
- **Area/category:** cli/server/src/shared/lib/config/state.ts / code-quality (DRY / dead code)
- **Locations:** `cli/server/src/shared/lib/config/state.ts:190`, `cli/server/src/shared/lib/config/state.test.ts:138-151`
- **Evidence:** `export const readOrCreateProjectFile = createProjectFile;` is a pure alias. rg across the repo shows the only importers are state.test.ts (lines 138, 148, 151). No production code references it; createProjectFile already documents create-or-return semantics (state.ts:176-188). This is the re-export-indirection anti-slop pattern the repo rules call out (kill re-export indirection / delete dead code). Pre-existing (untouched by the c1dec6ee..HEAD remediation) but within the config/store-adjacent scope.
- **Fix:** Delete the alias line at cli/server/src/shared/lib/config/state.ts:190 (`export const readOrCreateProjectFile = createProjectFile;`). In cli/server/src/shared/lib/config/state.test.ts, change the destructure at line 138 to `const { createProjectFile } = await loadState();` and replace the two call sites at lines 148 and 151 to call `createProjectFile(...)`. The existing test description ("reads an existing project file or creates one under the project .diffgazer directory") stays accurate because createProjectFile already has read-or-create semantics, so no test name or assertion changes are needed.
- **Verifier:** Confirmed by reading the cited files and rg across the whole repo. cli/server/src/shared/lib/config/state.ts:190 is a pure alias: `export const readOrCreateProjectFile = createProjectFile;`. rg shows the ONLY references to `readOrCreateProjectFile` are the alias definition (state.ts:190) and three t

### N37 ⚪ LOW — buildHtmlShell XSS test asserts a false security property — JSON.stringify does not neutralize a </script> breakout, leaving a latent injection sink and a misleading test
- **Area/category:** cli/diffgazer (embedded server HTML shell injection) / security defense-in-depth / misleading test (test-behavior)
- **Locations:** `cli/diffgazer/src/lib/servers/embedded-server.ts:22-24`, `cli/diffgazer/src/lib/servers/embedded-server.test.ts:42-48`
- **Evidence:** buildHtmlShell injects the token via `window.__DIFFGAZER_SHUTDOWN_TOKEN__=${JSON.stringify(token)}` inside an inline <script>. The test 'JSON-escapes the token to prevent script injection' (embedded-server.test.ts:42-48) and the framing imply JSON.stringify sanitizes against script injection. It does not: `JSON.stringify('</script><script>alert("xss")')` yields `"</script><script>..."` — the `</script>` sequence is passed through unescaped (verified: node prints it verbatim), which the HTML parser treats as a script-element terminator regardless of JS string context. The test only passes because it checks for the quote-escaped raw form (with backslashes the assertion substring is absent), so it accidentally green-lights a mechanism that does NOT stop a </script> breakout. This is currently NOT exploitable because the injected token is always `randomBytes(32).toString('hex')` and ensureShutdownToken() overwrites any shell-supplied DIFFGAZER_SHUTDOWN_TOKEN (cli-options.test.ts:154 even asserts `token).not.toBe('shell-token')`), so the value can never contain '<' or quotes. The hazard is a maintainer one: the test documents JSON.stringify as injection-safe, which would mislead anyone who later makes the token (or any other value passed through this shell) user/config-derived.
- **Fix:** Make the escaping actually breakout-safe at the single sink and assert the real property in the test. In cli/diffgazer/src/lib/servers/embedded-server.ts:24, escape the HTML-significant characters so the value cannot terminate the script element: `const script = `<script nonce="${nonce}">window.__DIFFGAZER_SHUTDOWN_TOKEN__=${JSON.stringify(token).replace(/</g, "\\u003c").replace(/>/g, "\\u003e")};</script>`;`. Then fix the misleading test in embedded-server.test.ts:42-48: rename it to describe breakout-safety, drop the incorrect "escapes the angle brackets" comment, and assert the rendered body does not contain the literal breakout sequence produced by the payload, e.g. `const { body } = buildHtmlShell(MINIMAL_HTML, '</script><script>alert("xss")'); expect(body).not.toContain("</script><script>"); expect(body).toContain("\\u003c/script\\u003e"); expect(body).toContain("window.__DIFFGAZER_SHUTDOWN_TOKEN__=");`. This keeps the existing helper/contract and CSP nonce defenses intact while closing the latent sink and making the test verify the property it claims.
- **Verifier:** Both technical claims are verified against the code as written. (1) JSON.stringify does NOT neutralize a </script> breakout: at cli/diffgazer/src/lib/servers/embedded-server.ts:24 the token is interpolated via `window.__DIFFGAZER_SHUTDOWN_TOKEN__=${JSON.stringify(token)}` inside an inline <script>; 

### N38 ⚪ LOW — clearScopedRouteState("/review", "highlighted") in the resume/start-review flow targets a key no component ever writes and a scope that does not match the real /review/<id> pathname
- **Area/category:** apps/web/src/features/home (navigation reset) / dead code / incorrect scope key
- **Locations:** `apps/web/src/features/home/components/home-presentation.tsx:119-126`
- **Evidence:** navigateToReview() calls clearScopedRouteState("/review", "highlighted") before navigating. useScopedRouteState namespaces storage keys by useLocation().pathname (use-scoped-route-state.ts:50-51, 24-26). The review route is path "/review/{-$reviewId}" (router.tsx:98), so its runtime pathname is "/review/<uuid>", never "/review". Separately, an exhaustive scan shows the only useScopedRouteState("highlighted", ...) writers are the home page (pathname "/") and the settings hub (pathname "/settings"); no review-route component uses scoped "highlighted" state at all (it uses local useState). The clear at line 120 therefore deletes the key "/review:highlighted", which is doubly unreachable: nothing writes it, and even if it did the scope string would not match. The sibling clears at home-presentation.tsx:192 and hub/page.tsx:59 use the destination route as the scope and are meaningful (e.g. resetting /settings:highlighted), which makes this one read as intentional but it is a silent no-op that misleads maintainers into thinking review entry resets a highlight.
- **Fix:** Delete the inert line at apps/web/src/features/home/components/home-presentation.tsx:120 (clearScopedRouteState("/review", "highlighted");) so navigateToReview only performs the navigate(). It has no runtime effect: nothing writes a "highlighted" scoped key under any "/review..." scope (the review page uses local useState, not scoped route state), and the literal "/review" scope can never equal the real "/review/<uuid>" pathname anyway. Removing it also eliminates the misleading impression that review entry resets a review-list highlight. No other change is needed; the clearScopedRouteState prop on the HomePresentation contract stays since it is still used at line 192 for the menu-route navigation.
- **Verifier:** Confirmed from the actual code. home-presentation.tsx:119-120 — navigateToReview() calls clearScopedRouteState("/review", "highlighted") before navigate({ to: "/review/{-$reviewId}", ... }). clearScopedRouteState builds the storage key via createStorageKey(key, scope) => `${scope}:${key}` = "/review

### N39 ⚪ LOW — F017-modified docs scripts (generate-sitemap.mjs + test) are outside every lint/format gate and carry a real biome violation that `pnpm run check` cannot detect
- **Area/category:** apps/docs / lint/format gate integrity
- **Locations:** `apps/docs/biome.json:10-18`, `apps/docs/scripts/generate-sitemap.mjs:1-3`, `apps/docs/scripts/generate-sitemap.test.ts:1-90`, `package.json:22`
- **Evidence:** The F017 remediation added/modified code in apps/docs/scripts/generate-sitemap.mjs (new pushSectionIndexPage + index-route emission) and apps/docs/scripts/generate-sitemap.test.ts (new index-route assertions), but neither file is reachable by any lint/format gate.  1. The docs biome `files.includes` (apps/docs/biome.json:10-18) is an allowlist of `src/**/*`, `.vscode/**/*`, `index.html`, `vite.config.ts` only. `scripts/**` is not listed, so `biome check` skips it. Verified directly: `pnpm exec biome check scripts/generate-sitemap.mjs` reports `Checked 0 files ... These paths were provided but ignored: - scripts/generate-sitemap.mjs`. 2. The root `check` script (package.json:22) is `biome lint --config-path=biome.root.json scripts/monorepo && turbo run check`. It only lints `scripts/monorepo`, then delegates per-package to the docs `biome check`, which (per #1) excludes `apps/docs/scripts`. So the directory is covered by neither gate. 3. The gap hides a real violation, not just theoretical drift. Running biome against the file with the root config surfaces: `scripts/generate-sitemap.mjs:1:1 assist/source/organizeImports ... The imports and exports are not sorted` (biome wants the node:child_process/node:fs/node:path imports reordered). The docs biome.json has `assist.actions.source.organizeImports: "on"`, so this exact rule would fire if the file were in scope. Additionally the changed files use 2-space indentation (verified lines 132-141 of the .mjs and lines 52-54 of the .test.ts) while the docs formatter mandates `indentStyle: "tab"`, so `biome check` would also report formatting diffs.  Impact: The F017 lens requires `pnpm run check` cleanliness, but check passes only because these remediation-touched files are skipped, not because they are clean. A maintainer editing the sitemap generator or its tests gets no lint/format feedback, and unsorted imports / wrong indentation accumulate silently. The newly added test logic in particular escapes the linter entirely (it is type-checked via tsconfig.test.json `scripts/**/*.ts`, but biome never sees it).
- **Fix:** Add "scripts/**/*" to files.includes in apps/docs/biome.json so the docs lint/format gate covers the scripts directory (the existing src-scoped negations remain unchanged and unaffected). Then run `pnpm --filter @diffgazer/docs exec biome check --write` once to apply tab indentation and import-sort to generate-sitemap.mjs and generate-sitemap.test.ts. Note that adding scripts/**/* also pulls the other pre-existing scripts (generate-logo-ascii.mjs, generate-route-tree.mjs, prepare-generated.mjs, sync-artifacts.mjs, and artifacts/*.ts) under the gate, so the same `biome check --write` pass will reformat those too; this is the desired outcome (it closes the whole hole), but apply and review that wider reformat in the same change rather than only the two F017 files. No runtime behavior changes.
- **Verifier:** Verified directly from the cited files. (1) apps/docs/biome.json:10-18 sets files.includes to an allowlist of "src/**/*", ".vscode/**/*", "index.html", "vite.config.ts" plus src-scoped negations; "scripts/**" is NOT listed. The docs "check" script is plain `biome check` with no path arg (apps/docs/p

### N40 ⚪ LOW — Hub test files are never type-checked: missing tsconfig.test.json + test:types script that every sibling SPA has
- **Area/category:** apps/hub / build-script parity / verification gate
- **Locations:** `apps/hub/package.json:7-13`, `apps/hub/tsconfig.json:14`, `apps/hub/src/App.test.tsx:1-27`, `apps/web/package.json:13`, `apps/landing/package.json:11`, `apps/landing/tsconfig.test.json:1-8`, `turbo.json:33-34`
- **Evidence:** The remediation promoted hub from "static HTML" to a real built-and-served SPA (deploy/hub.Dockerfile now runs `pnpm --filter @diffgazer/hub build` and ships `apps/hub/dist`), and hub now carries a Vitest suite (apps/hub/src/App.test.tsx) using jest-dom matchers like `.toBeInTheDocument()`. But hub's type-check coverage is missing in every gate:   1. apps/hub/package.json defines `type-check: tsc --noEmit`, and apps/hub/tsconfig.json:14 has `"exclude": ["**/*.test.ts", "**/*.test.tsx"]`, so the type-check never sees test files.   2. `build: tsc -b && vite build` uses the same exclude, so the build never type-checks tests either.   3. Hub has NO `tsconfig.test.json` and NO `test:types` script. The repo's verification pipeline runs `turbo run test:types` (package.json:19,23,24,25), but turbo only runs the task for packages that DEFINE it, so hub is silently skipped. Every sibling SPA closes this: apps/web and apps/landing both ship `tsconfig.test.json` (which includes `src/**/*.tsx` and adds vitest/jest-dom types) plus `"test:types": "vitest --typecheck --typecheck.only --run"`. As a result, a type error in apps/hub/src/App.test.tsx (e.g. a wrong `siteLinks` key or a misused jest-dom matcher) ships completely undetected, unlike in web/landing.
- **Fix:** Bring hub to sibling test:types parity. The finding's two-part fix (add tsconfig.test.json + test:types script) is necessary but INCOMPLETE — `vitest --typecheck.only` only consults the test tsconfig and jest-dom/vitest-globals types when vitest.config.ts has a `typecheck` block (web and landing both have it; hub does not). Apply all three: 1) Add apps/hub/tsconfig.test.json mirroring apps/landing/tsconfig.test.json:    { "extends": "./tsconfig.json", "compilerOptions": { "types": ["vite/client", "vitest/globals", "@testing-library/jest-dom"] }, "include": ["src/**/*.ts", "src/**/*.tsx"], "exclude": ["node_modules", "dist"] } 2) In apps/hub/vitest.config.ts, add inside `test`:    typecheck: { enabled: false, tsconfig: "./tsconfig.test.json", include: ["src/**/*.test.ts", "src/**/*.test.tsx"] }    (matches apps/web/vitest.config.ts and apps/landing/vitest.config.ts exactly). 3) Add `"test:types": "vitest --typecheck --typecheck.only --run"` to apps/hub/package.json scripts. Separately (correctness, not type-check coverage): add `"noEmit": true` to apps/hub/tsconfig.json compilerOptions so `tsc -b` in the build script stops emitting throwaway JS into dist before `vite build` overwrites it — apps/landing/tsconfig.json:15 already sets this. Do NOT also copy landing's isolatedModules/verbatimModuleSyntax/noUncheckedIndexedAccess unless you intend to tighten hub's compiler config; those are stricter flags beyond the parity gap and could surface unrelated errors, so keep them out of the minimal fix. After the change, hub's test files participate in `turbo run test:types`, closing the gate.
- **Verifier:** Confirmed by reading the actual files. apps/hub/package.json:7-13 defines only `type-check: "tsc --noEmit"` and `test: "vitest run"` — there is NO `test:types` script, unlike apps/web/package.json:13 and apps/landing/package.json:11 which both have `"test:types": "vitest --typecheck --typecheck.only

### N41 ⚪ LOW — REVERSE_PROXY.md is stale after hub became a lib-consuming SPA: wrong content type and incomplete Coolify watch paths for landing/hub
- **Area/category:** deploy / deploy correctness / documentation drift
- **Locations:** `deploy/REVERSE_PROXY.md:46`, `deploy/REVERSE_PROXY.md:79`, `deploy/REVERSE_PROXY.md:86`, `deploy/hub.Dockerfile:16-25`, `apps/hub/src/styles/index.css:2-4`, `apps/landing/src/styles/index.css:5-7`
- **Evidence:** The remediation changed deploy/hub.Dockerfile from copying `apps/hub/public/` to running a full Vite build (`pnpm --filter @diffgazer/ui build && pnpm --filter @diffgazer/hub build`) and shipping `apps/hub/dist`. Two facts in deploy/REVERSE_PROXY.md are now wrong:   1. Line 46 (subdomain table) still lists hub Content as "Static HTML (single page)". It is now a built Vite + React SPA that bundles `@diffgazer/ui` theme CSS (apps/hub/src/styles/index.css imports @diffgazer/ui/theme-base.css, theme.css, styles.css), identical in kind to the landing SPA.   2. The documented Coolify Watch Paths are `apps/landing/**` (line 79) and `apps/hub/**` (line 86) only. But both apps' BUILD OUTPUT now depends on `libs/ui` (theme tokens are inlined into the bundled CSS via @import of @diffgazer/ui/theme.css, which resolves to libs/ui/dist/theme.css). A change to the shared theme tokens in libs/ui therefore changes the landing/hub bundles, yet the watch paths will not trigger a Coolify rebuild — the deployed CSS silently drifts from source. The Dockerfiles rebuild @diffgazer/ui first so a manual/triggered rebuild is correct; the gap is purely the change-detection watch paths.
- **Fix:** In deploy/REVERSE_PROXY.md: (1) Change the hub Content cell at line 46 from "Static HTML (single page)" to "Static SPA (Vite + React)" so it matches the landing row (line 45) and the actual hub.Dockerfile build. (2) Broaden the Watch Paths to include the shared build input, mirroring the registry/docs entries that already list lib paths: line 79 → `apps/landing/**,libs/ui/**` and line 86 → `apps/hub/**,libs/ui/**`. This is documentation-only and respects the existing watch-path convention in the same file; no source or Dockerfile change is required since the Dockerfiles already rebuild @diffgazer/ui before each app.
- **Verifier:** Both sub-claims verify against the actual files. (1) Content-type mislabel: deploy/REVERSE_PROXY.md:46 describes hub as "Static HTML (single page)", but hub is now a Vite + React SPA — apps/hub/package.json build is "tsc -b && vite build" with @diffgazer/ui (workspace:*) as a dependency, apps/hub/sr

### N42 ⚪ LOW — F012 bench-strict regression test guards a CI-unused script and leaves the real per-PR gate (`verify`) untested
- **Area/category:** scripts/monorepo + .github/workflows / test-completeness / CI-gate fragility
- **Locations:** `scripts/monorepo/package-scripts.test.mjs:14-26`, `package.json:23`, `package.json:24`, `.github/workflows/release-readiness.yml:58`
- **Evidence:** The F012 remediation moved the strict flag into `test-ci` and `release-check` (`... && DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run bench && ...`) and added package-scripts.test.mjs assertions for exactly those two scripts. But `test-ci` is invoked by NO workflow (grep of .github finds only `pnpm run verify` at release-readiness.yml:58 and `pnpm run release-check` at release.yml:53; `test-ci` appears only in scripts/README.md, audits/, and this test). The actual per-PR/push gate is `pnpm run verify`, whose script body (package.json:23) runs a bare `pnpm run bench` with NO inline strict flag — its strict mode comes solely from the workflow YAML env prefix `DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run verify` at release-readiness.yml:58. Nothing in the test suite asserts that prefix exists, so dropping it (e.g. during a workflow refactor) would silently disable bench latency/throughput gating on every PR while the F012 regression test stays green and one of its two guarded scripts (`test-ci`) is effectively dead as a CI gate.
- **Fix:** Keep severity low: the env prefix currently exists and works, so there is no live failure — only missing regression protection. Add a test that parses .github/workflows/release-readiness.yml and asserts the "Verify" step invokes `pnpm run verify` with `DIFFGAZER_SMOKE_STRICT_SKIPS=1` in its `run:` line. In scripts/monorepo/package-scripts.test.mjs, read the YAML as text (readFileSync of the workflow file relative to import.meta.url) and `assert.match(yaml, /DIFFGAZER_SMOKE_STRICT_SKIPS=1[^\n]*pnpm run verify/)`. Do NOT take the finding's alternative of inlining `DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run bench` into the root `verify` script: `verify` is the documented LOCAL dev command (README.md:36, TESTING.md:130, PACKAGE_GOVERNANCE.md:58), and the repo deliberately keeps its bench/smoke non-strict so machine-sensitive latency SLO breaches only warn locally and gate only in CI via the workflow env prefix; inlining strict would make every local `pnpm run verify` hard-fail on slow dev machines. Optionally also drop the dead `test-ci` assertion (package-scripts.test.mjs:14-19) or note that `test-ci` is a local/sota-verify gate, not a GitHub Actions gate, so its coverage does not protect the per-PR pipeline.
- **Verifier:** Verified against the actual code. (1) benchmark-server.mjs:203-208 confirms latency/throughput SLO breaches only hard-fail when DIFFGAZER_SMOKE_STRICT_SKIPS=1; otherwise they report-and-warn, so strict mode is what makes the benchmark a real gate. (2) The F012 regression test (scripts/monorepo/packa

### N43 ⚪ LOW — `release-check` runs `smoke:packages` twice — once strict inside `smoke`, once redundantly in weaker non-strict mode
- **Area/category:** package.json scripts / code-quality (DRY / redundant + weaker duplicate gate)
- **Locations:** `package.json:25`, `package.json:31`
- **Evidence:** release-check (package.json:25) contains `... DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run smoke && pnpm run smoke:packages && DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run bench ...`. The `smoke` script (package.json:31) is `pnpm run smoke:cli && pnpm run smoke:packages && pnpm run smoke:shadcn`, so `smoke:packages` already runs inside the strict `smoke` invocation. The trailing standalone `pnpm run smoke:packages` re-runs the identical scripts/monorepo/smoke-package-install.mjs a second time but WITHOUT `DIFFGAZER_SMOKE_STRICT_SKIPS=1`, so the duplicate run can SKIP fixtures (via skipMissingSmokeDeps) that the strict pass would hard-fail on. It adds zero gating value, doubles the slowest smoke pass on every release gate, and misleads readers into thinking package smokes gate strictly on their own. (Pre-existing token, but the same release-check line was edited by the F012 remediation.)
- **Fix:** In package.json:25 (`release-check`), delete the redundant standalone `&& pnpm run smoke:packages` immediately after `DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run smoke`, since the strict `smoke` already runs `smoke:packages` strictly. Resulting fragment: `... && DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run smoke && DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run bench && ...`. (If a separate standalone strict package smoke were ever wanted, prefix it with `DIFFGAZER_SMOKE_STRICT_SKIPS=1` rather than leaving a weaker duplicate — but it would still be redundant here.) No test changes needed; package-scripts.test.mjs only checks the bench strict pattern.
- **Verifier:** Confirmed from source. package.json:31 `smoke` = `pnpm run smoke:cli && pnpm run smoke:packages && pnpm run smoke:shadcn`, so `smoke:packages` (smoke-package-install.mjs) already runs inside the strict `DIFFGAZER_SMOKE_STRICT_SKIPS=1 pnpm run smoke` invocation in release-check (package.json:25). The

### N44 ⚪ LOW — Stale DRY comment in core review display documents a deleted web file and a divergence that no longer exists
- **Area/category:** libs/core/src/review (display) / DRY / documentation accuracy
- **Locations:** `libs/core/src/review/display.ts:1-12`, `libs/core/src/review/progress-mapping.ts:17-20`, `apps/web/src/features/review/components/review-container.tsx:6`
- **Evidence:** display.ts:4-11 asserts: "The web app has its own `mapStepStatus` in `apps/web/src/features/review/components/review-container.utils.ts` ... This is intentional divergence — the web and CLI have different UI status models, not a duplication bug." That file does not exist (`ls` errors; it was deleted in pre-remediation commit 2af9c4d7 'cleanup'). `rg mapStepStatus apps/web/src` returns zero hits. The step->ProgressStatus mapping the comment claims lives in web is in fact consolidated into core: libs/core/src/review/progress-mapping.ts:17 `mapStepStatusToProgress` calls display.ts `mapStepStatus`, and apps/web/src/features/review/components/review-container.tsx:49 consumes the core-exported `mapStepsToProgressData` (libs/core/src/review/index.ts:46). So the only step-status mapping is centralized in core; the comment documents a parallel web implementation that no longer exists and frames a no-longer-real DRY decision as 'not a duplication bug', actively misleading any maintainer who later edits the mapping.
- **Fix:** Rewrite the display.ts:1-12 doc comment to reflect the single source of truth. Replace the false "intentional divergence"/deleted-file reference with an accurate note, e.g.:  /**  * CLI-oriented display helpers for review progress steps.  *  * `mapStepStatus` maps schema step statuses to the 4-value CLI `UIStepStatus`  * ("pending"/"running"/"complete"/"error"). The web progress view derives its  * status from the same function via `mapStepsToProgressData` (progress-mapping.ts),  * which collapses "error" to "pending" because the web `ProgressStatus` has no  * "error" variant.  */  This keeps the only accurate distinction (CLI UIStepStatus has an "error" value; web ProgressStatus maps it to "pending"), both derived from this one function, and drops the reference to the deleted review-container.utils.ts and the no-longer-real DRY decision.
- **Verifier:** Confirmed from source. libs/core/src/review/display.ts:5-11 documents a parallel web `mapStepStatus` at `apps/web/src/features/review/components/review-container.utils.ts` and frames it as "intentional divergence — not a duplication bug." That file does not exist: `ls` errors and was deleted in comm

### N45 ⚪ LOW — Active-session resume encodes file scope as comma-joined CSV, silently corrupting any file path that contains a comma or surrounding whitespace
- **Area/category:** cli/server review API + libs/core api client / correctness (latent scope-key mismatch, distinct from lens dedup)
- **Locations:** `cli/server/src/features/review/schemas.ts:23-46`, `libs/core/src/api/review.ts:124-126`, `cli/server/src/features/review/sessions.ts:21-37`
- **Evidence:** On create, files arrive as a JSON array (CreateReviewBodySchema.files = z.array(z.string().max(500)...) in schemas.ts:56), preserving each path verbatim including commas/spaces, and buildScopeKey (sessions.ts:27-29) sorts+joins the exact strings. On resume, the client serializes files with `scope.files.join(",")` (review.ts:126) and the server's ActiveSessionQuerySchema.files reparses with csvToArray which does `value.split(",").map(item => item.trim())` (schemas.ts:25,41-45). A POSIX-legal path such as `src/a,b.ts` becomes the two entries `["src/a","b.ts"]`, and a path with leading/trailing spaces is trimmed. Either case yields a different buildScopeKey than the creating session, so getActiveSessionForProject (sessions.ts:358-382) never matches and the scoped review is undiscoverable on resume. This is a different root cause than the already-reported lens-dedup mismatch (lenses are a constrained LensId enum and cannot contain commas; the lossy CSV encoding only bites the free-form `files` field). It is latent today because the React Query layer (libs/core/src/api/hooks/queries/review.ts:22) never passes scope, but it becomes a live resume bug the moment that already-flagged wiring is completed.
- **Fix:** Stop flattening the file-scope array through a comma-delimited query param; commas and spaces are legal path characters, so any reversible-on-its-own delimiter must survive arbitrary path content. Lenses are safe (constrained LensId enum) and can keep csvToArray, but files must not. Minimal fix that preserves the existing client params contract (Record<string,string> serialized via URLSearchParams in libs/core/src/api/client.ts:63): JSON-encode the files array on the client and JSON.parse it on the server. In libs/core/src/api/review.ts:126, replace `params.files = scope.files.join(",")` with `params.files = JSON.stringify(scope.files)`. In cli/server/src/features/review/schemas.ts, change the files field transform from csvToArray to a JSON parse that tolerates absent/invalid input, e.g. `.transform((v) => { if (v === undefined) return undefined; try { const parsed = JSON.parse(v); return Array.isArray(parsed) && parsed.length > 0 ? parsed : undefined; } catch { return undefined; } }).pipe(z.array(z.string().max(500).regex(/^[^\0]+$/)).max(200).optional())`. This keeps create and resume feeding buildScopeKey byte-identical file arrays. Update the libs/core/src/api/review.test.ts scope-param assertions (currently expecting the comma-joined value) to the JSON-encoded form, and add a test covering a path containing a comma so the round-trip is locked in. (Alternative if repeated query params are later preferred: widen the client params type to allow string[] and serialize each file as its own `files` entry, parsing via c.req.queries("files") on the server; this is a larger change to the shared client signature and not required for the fix.)
- **Verifier:** Confirmed against the code as written.

### N46 ⚪ LOW — Unused useViewportBreakpoint hook and dead-on-arrival hooks barrel (apps/web/src/hooks/index.ts)
- **Area/category:** apps/web/src/hooks / dead code / anti-slop
- **Locations:** `apps/web/src/hooks/use-viewport-breakpoint.ts:36`, `apps/web/src/hooks/index.ts:1-3`
- **Evidence:** A repo-wide search (rg over all .ts/.tsx, node_modules excluded) shows `useViewportBreakpoint` is referenced in exactly two places: its own definition at apps/web/src/hooks/use-viewport-breakpoint.ts:36 and the barrel re-export at apps/web/src/hooks/index.ts:3. There is no consumer anywhere. apps/web is a private application (not a published library), so the export has no external customer either. Separately, the `@/hooks` barrel itself is never imported: searching for `'@/hooks'` / `"@/hooks"` across apps/web returns no matches (rg exit code 1). The two other hooks it re-exports (useTheme, useScopedRouteState) are consumed only through deep paths (e.g. apps/web/src/features/settings/components/theme/page.tsx:11 imports `@/hooks/use-theme`; apps/web/src/features/home/components/page.tsx:6 imports `@/hooks/use-scoped-route-state`). So index.ts is a re-export barrel that no module imports, and useViewportBreakpoint is a 39-line hook (incl. its matchMedia subscribe/getSnapshot plumbing) that nothing calls. This violates the repo rules: AGENTS.md 'Prefer deleting dead code over preserving aliases', code-quality 'delete dead code completely / kill re-export indirection', and anti-slop 'dead code / unused exports'.
- **Fix:** Delete apps/web/src/hooks/use-viewport-breakpoint.ts. Delete apps/web/src/hooks/index.ts entirely, since the @/hooks barrel is never imported and the surviving hooks (useTheme, useScopedRouteState, clearScopedRouteState) are already consumed only through their deep @/hooks/use-* paths. Then check libs/core: getBreakpointTierFromPx in libs/core/src/layout/breakpoints.ts:15 is called ONLY by this web hook (cli/diffgazer uses the column-based getBreakpointTier instead), so removing the hook orphans that core export — either remove getBreakpointTierFromPx from breakpoints.ts and its barrel export too, or leave it if core intends it as part of its public layout API; verify with `rg getBreakpointTierFromPx` before deciding. Do not reintroduce a hooks barrel; if viewport breakpoints are needed later, add the hook at that point (YAGNI). Because both files are outside the c1dec6ee..HEAD remediation diff, this is optional pre-existing cleanup rather than a remediation defect — fold it into a dead-code sweep, not a blocker for this PR.
- **Verifier:** Verified by reading the files and rg over all .ts/.tsx (node_modules excluded). useViewportBreakpoint is referenced in exactly two places: its definition at apps/web/src/hooks/use-viewport-breakpoint.ts:36 and the barrel re-export at apps/web/src/hooks/index.ts:3 — no consumer exists. The @/hooks ba

### N47 ⚪ LOW — Sitemap script's hand-maintained type declaration (.d.mts) drifted from the implementation: writeSitemap return type omits robotsTarget
- **Area/category:** apps/docs / type safety / dead-and-incorrect type surface (anti-slop drift)
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/apps/docs/scripts/generate-sitemap.d.mts:7`, `/Users/voitz/Projects/diffgazer-workspace/apps/docs/scripts/generate-sitemap.mjs:155-176`
- **Evidence:** generate-sitemap.d.mts:7 declares `export function writeSitemap(outDir?: string): { target: string; count: number };`. The actual implementation in generate-sitemap.mjs:155-170 returns `{ target: sitemapTarget, robotsTarget, count: pages.length }` (three fields), and the script's own CLI entry block at line 176 destructures all three: `const { target, robotsTarget, count } = writeSitemap(outDir);`. The `robotsTarget` field was added to the .mjs when robots.txt generation landed (commit 30936cb6, predating the F017 remediation), but the sibling .d.mts declaration was never updated. The .d.mts is the hand-maintained typed contract for this script: TypeScript resolves it as the declaration for the `./scripts/generate-sitemap.mjs` import used in vite.config.ts:12 and generate-sitemap.test.ts:4. The F017 remediation touched generate-sitemap.mjs (adding pushSectionIndexPage) and its test, but left this sibling declaration inaccurate. No current TypeScript consumer imports `writeSitemap` (only `getPreRenderPages` is imported), so the wrong type is latent: any future typed caller of `writeSitemap` would receive a return type missing the real `robotsTarget` field, and the declaration is simultaneously dead type surface (declared but consumed by nothing) and factually wrong.
- **Fix:** In /Users/voitz/Projects/diffgazer-workspace/apps/docs/scripts/generate-sitemap.d.mts:7, update the return type to match the implementation: `export function writeSitemap(outDir?: string): { target: string; robotsTarget: string; count: number };`. This keeps the file's existing intent (declaring both exported functions of the script) while making the contract accurate. Deleting the `writeSitemap` line entirely is an acceptable alternative since no TypeScript consumer imports it today, but correcting it is lower-churn and avoids re-introducing the same drift if a typed caller is added later.
- **Verifier:** The hand-maintained declaration at /Users/voitz/Projects/diffgazer-workspace/apps/docs/scripts/generate-sitemap.d.mts:7 declares `writeSitemap(outDir?: string): { target: string; count: number }` (2 fields), but the implementation at /Users/voitz/Projects/diffgazer-workspace/apps/docs/scripts/genera

### N48 ⚪ LOW — Extracted benchmark gate (checkSlo) has no test for the clean-pass branch — the path that runs on every healthy benchmark
- **Area/category:** scripts/monorepo / test-behavior-not-implementation / missing test for changed logic
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/scripts/monorepo/benchmark-slo.test.mjs:65-81`, `/Users/voitz/Projects/diffgazer-workspace/scripts/monorepo/artifacts/benchmark-slo.mjs:31-50`
- **Evidence:** The F014 remediation extracted the benchmark gating logic into `checkSlo`/`summarizeStatuses` in scripts/monorepo/artifacts/benchmark-slo.mjs and added benchmark-slo.test.mjs. The three checkSlo tests cover only failure/partial-failure branches: a non-200 functional failure (line 26), skip-latency-on-functional-failure (line 47), and the all-three-latency-breaches case (line 65). Grepping the test file shows assertions only ever pin ONE collector to empty: line 44 `deepEqual(latencyBreaches, [])` (functionalFailures non-empty), line 62 `deepEqual(latencyBreaches, [])` (functionalFailures non-empty), line 79 `deepEqual(functionalFailures, [])` (latencyBreaches has 3). There is no test where a clean result (all 200s, p95/p99/throughput within SLO) leaves BOTH `functionalFailures` and `latencyBreaches` empty. That clean-pass branch is exactly the path checkSlo takes on every healthy run in `pnpm run bench` and is what keeps the CI/release/sota-verify gate green. A regression that made checkSlo erroneously push to functionalFailures (turning a healthy benchmark into a hard exit-1 in test-ci/release-check/verify) or spuriously push a latencyBreach on a within-SLO result would not be caught by the current suite.
- **Fix:** Add one behavior test pinning the clean-pass contract, matching the existing node:test + node:assert/strict style and reusing the public helpers:  test("checkSlo records nothing when an all-200 run is within every SLO", () => {   const functionalFailures = [];   const latencyBreaches = [];   const result = {     ...summarizeStatuses([200, 200]),     p95: 10,     p99: 20,     requestsPerSecond: 10000,   };   checkSlo({ functionalFailures, latencyBreaches }, "GET /health", result, {     p95Ms: 50,     p99Ms: 100,     minRequestsPerSecond: 500,   });   assert.deepEqual(functionalFailures, []);   assert.deepEqual(latencyBreaches, []); });  This pins the only branch where checkSlo records nothing — the path benchmark-server.mjs relies on to keep the CI/release/verify gate green — and would catch a comparison-direction regression in the p95/p99/throughput checks that no current test covers.
- **Verifier:** Confirmed by reading both cited files. checkSlo (scripts/monorepo/artifacts/benchmark-slo.mjs:31-50) has exactly one branch that records nothing to either collector: nonOkCount===0 AND p95<=p95Ms AND p99<=p99Ms AND requestsPerSecond>=minRequestsPerSecond (the clean-pass path). The three checkSlo tes

### N49 ⚪ LOW — dist ESM guard's .json exclusion is a latent over-permissive branch that defeats the guard's own purpose
- **Area/category:** libs/core/scripts/verify-dist-esm-imports.ts / correctness / dead-code
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/core/scripts/verify-dist-esm-imports.ts:32`
- **Evidence:** The F001 dist guard exists to fail the build when a relative specifier in the emitted dist cannot be resolved under Node ESM. Its offender filter is `if (specifier && !specifier.endsWith(".js") && !specifier.endsWith(".json"))`. The `.json` exclusion is structurally wrong for this guard: `tsc` (the only emitter here; build is `tsc && pnpm verify:dist-esm`) does NOT copy `.json` assets into `dist`, so any relative `.json` import would emit a dist `.js` referencing a non-existent sibling `.json` and throw `ERR_MODULE_NOT_FOUND` at runtime in exactly the Node-ESM consumers this guard protects. The exclusion waves that case through. It is currently inert (verified zero relative `.json` imports in libs/core/src), so it is a latent gap, but it is also a real divergence: the sibling guard libs/keys/scripts/verify-dist-esm-imports.ts:35 uses `!specifier.endsWith(".js")` with no `.json` branch, so the two copies of the same guard disagree on what counts as a safe dist import.
- **Fix:** In /Users/voitz/Projects/diffgazer-workspace/libs/core/scripts/verify-dist-esm-imports.ts:32, drop the `&& !specifier.endsWith(".json")` clause so the condition reads `if (specifier && !specifier.endsWith(".js"))`, matching the keys guard. This makes the core guard flag any relative `.json` specifier in emitted dist (tsc never copies JSON to dist, so such a specifier is always unresolvable under Node ESM). No `dist/artifacts` skip is needed here — unlike keys, libs/core has no copied shadcn payload tree. If a future module legitimately needs a JSON asset, the correct response is to add a build step that copies that JSON into dist, not to re-exempt `.json` in the guard.
- **Verifier:** Verified against the actual code. The core guard at /Users/voitz/Projects/diffgazer-workspace/libs/core/scripts/verify-dist-esm-imports.ts:32 uses `if (specifier && !specifier.endsWith(".js") && !specifier.endsWith(".json"))`, while the sibling keys guard at /Users/voitz/Projects/diffgazer-workspace

### N50 ⚪ LOW — Embedded server serveStatic rewriteRequestPath SPA-fallback branch is unreachable dead code
- **Area/category:** cli/diffgazer (web mode / embedded server) / anti-slop / dead-code
- **Locations:** `cli/diffgazer/src/lib/servers/embedded-server.ts:120-126`, `cli/diffgazer/src/lib/servers/embedded-server.ts:102-119`
- **Evidence:** The SPA shell is served by the GET `/*` handler: it computes `const pathname = new URL(c.req.url).pathname` (line 103) and only calls `await next()` (falling through to the serveStatic middleware) when `isSpaNavigationRequest(c, pathname)` is FALSE (line 104). The serveStatic middleware then declares `rewriteRequestPath: (path, c) => isSpaNavigationRequest(c, path) ? "/index.html" : path` (lines 124-125). Hono passes the same request pathname as `path`, and `c` is the same context, so `isSpaNavigationRequest(c, path)` re-evaluates the exact predicate that must have already returned false for control to reach serveStatic at all. The `"/index.html"` branch is therefore unreachable and `rewriteRequestPath` is a pure identity function. It also misleadingly implies SPA navigation fallback is handled at the static layer, when in reality every SPA navigation (text/html accept) is fully served by the token-injecting GET `/*` shell handler above; if this branch ever DID fire it would serve the raw, un-injected index.html (no shutdown token), so the misleading code is a latent footgun for anyone who later reorders the handlers.
- **Fix:** In cli/diffgazer/src/lib/servers/embedded-server.ts, drop the dead rewriteRequestPath callback and register the static middleware as serveStatic({ root: webRoot }) (lines 120-127). The GET "/*" handler already fully serves SPA navigation with token injection and CSP, and real static assets resolve by their actual path, so behavior is unchanged. This removes the unreachable "/index.html" branch and the misleading implication that static-layer SPA fallback exists. Note this is pre-existing code outside the c1dec6ee..HEAD remediation; treat it as an opportunistic low-priority cleanup, not a remediation regression.
- **Verifier:** Technically accurate dead-code finding, verified against source and Hono internals. In cli/diffgazer/src/lib/servers/embedded-server.ts the GET "/*" handler (lines 102-119) owns SPA-shell serving and only calls next() (line 104-107) to fall through to the serveStatic middleware when isSpaNavigationR

### N51 ⚪ LOW — Orphaned audit ticket-ID comments (F151, F155) left in remediation-touched test source
- **Area/category:** cli/server/src/features/review (session tests) / anti-slop
- **Locations:** `cli/server/src/features/review/sessions.test.ts:73`, `cli/server/src/features/review/sessions.test.ts:296`
- **Evidence:** Two comments carry internal audit ticket IDs from a prior (2026-05-28) audit:  - Line 73: `// Tear down the module-level stale-cleanup interval so it never outlives the\n// suite (F151). Idempotent, so it is safe even if a test already called it.` - Line 296: `// test is the client-facing notice, not the log line. (F155)`  The repo anti-slop rule explicitly bans audit/ticket-id comments in source ("e.g. 'F148', 'STRUCTURE.md §6'"), and the F009 remediation removed exactly these from cli/server runtime source. `rg '\bF151\b|\bF155\b'` shows F151/F155 are orphaned tags whose only other appearance is the older `audits/2026-05-28/` tree (they are not in the F001-F019 scope and not documented anywhere a maintainer reading this test would find). This remediation modified this exact file (`git diff --stat c1dec6ee..HEAD` = 78 insertions, 0 deletions) and the newly-added test block it introduced (the 'preserves the cap warning' test, diff lines 9-35) was correctly written WITHOUT a ticket ID, yet the anti-slop pass left the two pre-existing F151/F155 IDs untouched. This is a distinct location and distinct ticket set from the already-flagged F005/F006/F007 (libs/ui registry tests) and F012/F015 (scripts/monorepo) ticket-ID findings — it is in a different package (cli/server) and references an entirely different, older audit cycle.
- **Fix:** Strip only the orphaned ticket IDs, preserving the durable rationale. Line 72-73: change `// Tear down the module-level stale-cleanup interval so it never outlives the\n// suite (F151). Idempotent, so it is safe even if a test already called it.` to `// Tear down the module-level stale-cleanup interval so it never outlives the\n// suite. Idempotent, so it is safe even if a test already called it.` (delete only ` (F151)`). Line 295-296: change `// Suppress the expected server-side cap console.warn; the contract under\n// test is the client-facing notice, not the log line. (F155)` to `// Suppress the expected server-side cap console.warn; the contract under\n// test is the client-facing notice, not the log line.` (delete only ` (F155)`). No other lines change; the surrounding tests and assertions stay intact.
- **Verifier:** Confirmed by reading the file: cli/server/src/features/review/sessions.test.ts:73 ends `...outlives the suite (F151). Idempotent...` and line 296 reads `// test is the client-facing notice, not the log line. (F155)`. `rg '\bF151\b|\bF155\b'` proves these IDs are orphaned — they exist nowhere else ex

### N52 ⚪ LOW — Dead realm-safe helper isNode is exported only to its own test (and duplicated inline at its one natural call site)
- **Area/category:** libs/keys/src/dom (focus trap / dom realm helpers) / dead code / anti-slop (YAGNI, DRY)
- **Locations:** `/Users/voitz/Projects/diffgazer-workspace/libs/keys/src/dom/dom.ts:21`, `/Users/voitz/Projects/diffgazer-workspace/libs/keys/src/hooks/use-focus-trap.ts:84`, `/Users/voitz/Projects/diffgazer-workspace/libs/keys/public/r/focusable.json:17`, `/Users/voitz/Projects/diffgazer-workspace/libs/keys/public/r/focus-trap.json:35`, `/Users/voitz/Projects/diffgazer-workspace/libs/keys/public/r/navigation.json:47`
- **Evidence:** `isNode(value, ownerView)` in libs/keys/src/dom/dom.ts:21 is the realm-correct Node check. A repo-wide search (excluding dist/node_modules) finds it imported by exactly one file: its own unit test libs/keys/src/dom/dom.test.ts. It is NOT re-exported from the public package barrel libs/keys/src/index.ts (index.ts re-exports only navigation-items, focusable, keyboard-utils, and focus-restore symbols), so it is not public package API either. Meanwhile its one natural consumer, isInsideContainer in libs/keys/src/hooks/use-focus-trap.ts:84-88, reimplements the exact same logic inline (`const View = getOwnerView(container); ... target instanceof View.Node`) instead of calling isNode. The helper is also shipped to copy/shadcn consumers inside every registry dom.ts blob (focusable.json, focus-trap.json, navigation.json), where it is equally unreferenced. Net: a function that exists only to be tested, while the place that should use it duplicates its body, and the codebase's other Node check (navigation-items.ts) uses the non-realm-safe global `Node.DOCUMENT_POSITION_*` instead.
- **Fix:** Wire isNode into its real call sites (preferred over deletion, since the realm-safe primitive is correct and two sites duplicate it). 1) In libs/keys/src/hooks/use-focus-trap.ts: import isNode alongside getOwnerView from "../dom/dom.js" (line 4) and rewrite isInsideContainer (lines 84-88) to `const View = getOwnerView(container); return isNode(target, View) && container.contains(target);`. 2) In libs/keys/src/providers/keyboard-provider.tsx: import isNode from "../dom/dom.js" (line 15) and rewrite isEventWithinContainer (lines 92-94) to `const View = getOwnerView(container); if (!isNode(eventTarget, View)) return false; return container.contains(eventTarget);`. 3) Regenerate the public registry so the four dom.ts blobs (focusable.json:17, focus-trap.json:35, navigation.json:47, focus-restore.json:23) stay byte-identical to source via `pnpm run prepare:artifacts` then `pnpm run validate:artifacts:check` — do not hand-edit the JSON. Note isNode stays unexported from index.ts (internal helper), so no public-API/barrel change is needed. (Alternative: delete isNode + its describe block in dom.test.ts:49-62 and regenerate the blobs — but that discards the better realm-safe abstraction the two sites already need, so wiring it in is the cleaner DRY fix.)
- **Verifier:** Confirmed by reading source. isNode is defined at libs/keys/src/dom/dom.ts:21-23 as the realm-safe Node check (Boolean(ownerView && value instanceof ownerView.Node)). A scoped search (excluding dist/node_modules) shows its only import is its own unit test libs/keys/src/dom/dom.test.ts:3,49-62 — the 

### N53 ⚪ LOW — Settings hub clears the highlighted scoped key on the wrong (destination) scope, so the call is dead and the hub's menu highlight is never reset
- **Area/category:** apps/web/src/features/settings (settings hub navigation) / dead code / incorrect scope key (React state-store correctness)
- **Locations:** `apps/web/src/features/settings/components/hub/page.tsx:56-61`, `apps/web/src/hooks/use-scoped-route-state.ts:24-26`, `apps/web/src/hooks/use-scoped-route-state.ts:71-75`
- **Evidence:** useScopedRouteState keys every entry by the current pathname: createStorageKey(key, scope) = `${scope}:${key}` and useScopedRouteState reads `${pathname}:${key}` (use-scoped-route-state.ts:24-26,50-51). A repo-wide search shows the `"highlighted"` scoped key is written by exactly two components: home/page.tsx:37-38 (pathname `/`) and settings/components/hub/page.tsx:42-43 (pathname `/settings`). In SettingsHubPage.handleActivate (hub/page.tsx:56-61) the code does `clearScopedRouteState(route, "highlighted")` where `route` is the navigation DESTINATION sub-route (e.g. `/settings/theme`, `/settings/providers`, ... from SETTINGS_ROUTES). None of the seven settings sub-pages ever call `useScopedRouteState("highlighted", ...)` (verified: they only pass a local `highlighted` prop to UI components), so `clearScopedRouteState("/settings/theme", "highlighted")` deletes a store key that never exists — a guaranteed no-op on every settings menu activation. This is the same root mismatch as the already-known `/review` finding (home-presentation.tsx:120) but at a distinct, uncovered location, and it hides a second defect: the hub's own highlight state lives at scope `/settings`, so if the intent was to reset the hub menu position, the call should target `"/settings"`, not the destination route.
- **Fix:** Delete the dead no-op rather than rewriting its scope (changing the scope to "/settings" would alter behavior and contradict the destination-clear pattern at home-presentation.tsx:192). In apps/web/src/features/settings/components/hub/page.tsx, remove line 59 so handleActivate becomes:    const handleActivate = (id: string) => {     const route = SETTINGS_ROUTES[id as SettingsAction];     if (route) {       navigate({ to: route });     }   };  Then drop the now-unused `clearScopedRouteState` from the import on line 7 (keep useScopedRouteState). For consistency, also remove the equally-dead `clearScopedRouteState("/review", "highlighted");` at apps/web/src/features/home/components/home-presentation.tsx:120 (no component writes `/review:highlighted`); the `clearScopedRouteState(route.to, "highlighted")` at line 192 is real (its `/settings` destination is a real writer) and must stay. No tests assert the clear-on-activate behavior (home-presentation.test.tsx:47 only mocks it), so removal is safe. Severity remains low: pure dead-code cleanup with zero functional impact, and it is outside the remediation diff under review.
- **Verifier:** Verified against the real code. The scoped store keys by `${scope}:${key}` (apps/web/src/hooks/use-scoped-route-state.ts:24-26), where useScopedRouteState uses `scope = pathname` (line 50-51) and clearScopedRouteState takes scope explicitly (line 71-72). A repo-wide search (`rg '"highlighted"'` and 

## Scorecard

| Dimension | Score | Notes |
|---|---|---|
| F001–F019 remediation | 4/5 | 17/19 fully done; F010 & F018 only partially wired |
| CI / release gates | 2/5 | `pnpm run check` red (apps/docs 133); docs scripts outside lint gate |
| Correctness | 3/5 | shutdown teardown, scoped-resume gap, Safari download, scroll-spy freeze, cross-realm floating |
| Security | 4/5 | registry `file.target` path-traversal gap (N05); INDEX files unvalidated (N33) |
| Anti-slop | 3/5 | ticket-ID comments re-introduced into tests; dead code; misleading comments |
| DRY / KISS / YAGNI | 3/5 | object-form floating wrappers, re-export indirection, dead capability |
| Test-behavior | 4/5 | strong behavior tests for fixes; gaps: cross-realm floating, checkSlo clean-pass, hub test:types |
| Type safety | 4/5 | error-code union widened to string; sitemap .d.mts drift |
| Architecture boundaries | 5/5 | no core→app/cli or ui→app violations found |

_Generated from workflow `wf_85ac51af-a0a` (168 agents, 11.2M tokens, 5 rounds) + orchestrator gates. Full per-agent evidence in the workflow transcript._
