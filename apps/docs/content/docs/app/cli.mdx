---
title: CLI reference
description: Command-line options for the diffgazer binary.
---

# CLI reference

```text
Usage: diffgazer [options]

Product CLI for reviewing code changes with Diffgazer.

Options:
  --tui              Start the beta terminal UI (incomplete; not recommended)
  --theme <theme>    Start TUI with a specific theme (only with --tui)
  -V, --version      Display version
  -h, --help         Display help
```

Running `diffgazer` with no options starts the default web environment and opens your browser.
