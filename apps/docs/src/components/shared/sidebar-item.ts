/**
 * Inactive-link dimming for every chrome tree sidebar (Docs/Home/Legal), so the
 * three trees share one set of foreground/hover stops instead of drifting apart.
 */
export const CHROME_SIDEBAR_ITEM_CLASS =
  "text-foreground/52 transition-colors hover:bg-foreground/[0.04] hover:text-foreground data-[selected]:text-foreground";
