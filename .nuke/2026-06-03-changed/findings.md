# Findings Ledger
scope: changed-vs-HEAD (82 files) | started: 2026-06-03 | status: in-progress
counts: 0 critical / 0 high / 5 medium / 10 low / 3 info

## Confirmed

### F-001 · medium · correctness · apps/docs/content/docs/app/getting-started/first-review.mdx:64-68, apps/docs/content/docs/app/getting-started/first-review.mdx:102, apps/docs/src/components/docs-mdx/blocks/steps.tsx:16-22, apps/docs/src/components/docs-mdx/blocks/steps.tsx:50, apps/docs/src/components/toc.tsx:50-72, apps/docs/registry/hooks/use-active-heading.ts:126, apps/docs/registry/hooks/use-active-heading.ts:147, apps/docs/registry/hooks/use-active-heading.ts:222
This change set converts the numbered step list in first-review.mdx into a <Steps>/<Step> block. <Step title="Read the findings"> (line 64) renders <Typography as="h2" id={slugify(title)}> => <h2 id="read-the-findings"> (steps.tsx:16-22,50). The page also still contains a separate substantive markdown section ## Read the findings (line 102, the two-pane review-layout explanation), which fumadocs' rehype-slug / github-slugger pipeline slugs to the identical id "read-the-findings". The page therefore renders two elements with id="read-the-findings" (invalid duplicate HTML id), the Step one appearing first in document order. Because Step ids are produced at React render time, the compile-time slugger never sees them and cannot deduplicate against them. The new DOM-derived TOC (entriesFromDom, toc.tsx:50-72) dedupes by id via a `seen` Set, keeping only the first occurrence (the Step) and silently dropping the real ## Read the findings content section from the runtime 'On this page' TOC after hydration. use-active-heading resolves targets via doc.getElementById(id) for both scroll-spy and click scrollTo, returning the first match (the Step), so the TOC entry / #read-the-findings anchor scrolls into the wizard step list instead of the dedicated section, and the genuine section is unreachable. Before this change the step was a plain list item, so read-the-findings was unique; the collision is newly introduced here, and it is a structural gap (any future Step title matching a sibling markdown heading collides the same way).
Evidence: slugify() in steps.tsx (trim/lowercase/spaces->'-'/strip non [a-z0-9-]) yields 'read-the-findings'; fumadocs-core remark-heading uses github-slugger and yields the same for '## Read the findings' (markdown-renderers.tsx h2 forwards the injected id). Built output confirms fumadocs assigns id:'read-the-findings' to the markdown heading (.output/server/_ssr/first-review-*.mjs headings/toc arrays). In toc.tsx entriesFromDom() iterates `h2[id],h3[id]` and skips duplicate ids via `if (!id || seen.has(id)) continue;` (lines 60-61); the Step occurs first in document order so the line-102 section is dropped. The compile-time seed entriesFromToc maps read-the-findings to the markdown section, so the anchor target also shifts/flashes between SSR/first-paint and post-mount DOM re-derivation via the MutationObserver. use-active-heading.ts getElementById returns the first match for scroll-spy (126/147) and scrollTo (222). Surveyed every Step-using content page (rg over the four changed getting-started MDX plus libs/keys, libs/ui docs): only first-review.mdx has a Step-vs-markdown slug collision; no Step-vs-Step collisions. Duplicate ids also violate the unique-id a11y contract in quality-bar.md.
Fix: Rename the colliding Step title (e.g. <Step title="Read your findings"> / "Open the review") or the markdown ## Read the findings heading so the two slugs differ, or have the Steps block derive ids with the same github-slugger and de-duplicate against existing page heading ids (or namespace Step ids).
round: 1 · skeptic: confirmed (Verified end-to-end against the actual files. (1) The change set converts step 7 from a plain <li> into <Step title="Read the findings"> (first-review.mdx:64; git diff HEAD confirms the prior version was a numbered list item with no id, so the collision is genuinely NEW in this change set). (2) steps.tsx:38-54 renders the Step as <Typography as="h2" id={slugify(title)}>; slugify (steps.tsx:16-22) yields 'read-the-findings'. (3) The page also still contains a substantive markdown section ## Read the findings (first-review.mdx:102, the two-pane review-layout explanation). fumadocs-core 16.7.5 remark-heading uses github-slugger (node_modules/fumadocs-core/dist/mdx-plugins/remark-heading.js:2,12-13,31-33) and sets hProperties.id; markdown-renderers.tsx:72 forwards that id onto the <h2>. Since the file has exactly ONE markdown 'Read the findings' heading (verified via rg over all h2/Step titles — only this one pair collides; 'Trust the repository'/'Trust the directory' and 'Start a review'/'Run a review' differ), github-slugger produces 'read-the-findings' with no -1 suffix — identical to the Step id. (4) Both <h2> render inside the same #main-content container (docs-content-layout.tsx:107-121 wraps the MDX body), so the runtime DOM has two elements with id="read-the-findings" — invalid duplicate HTML id, violating the unique-id a11y contract in quality-bar.md. The Step (under ## Steps, line 16) precedes the markdown section (line 102) in document order. (5) toc.tsx entriesFromDom (lines 50-72) iterates 'h2[id], h3[id]' in document order and dedupes via a `seen` Set (lines 54, 61: `if (!id || seen.has(id)) continue`), keeping the first occurrence (the Step) and silently dropping the real ## Read the findings section from the post-hydration 'On this page' TOC. (6) use-active-heading.ts resolves targets via doc.getElementById(id) for scroll-spy (lines 126, 147) and scrollTo (line 222), all returning the first match (the Step), so the #read-the-findings anchor / TOC click scrolls to the wizard step instead of the dedicated content section. The SSR seed (entriesFromToc, toc.tsx:38-44) maps read-the-findings to the markdown section title, so there is also an SSR-to-post-mount title/target shift. Not intentional (no convention sanctions a duplicate id; quality-bar explicitly requires unique heading ids). In scope (changed file, new Steps block, changed TOC). Fix (rename either the Step title or the markdown heading so slugs differ, or slug Step ids with github-slugger and de-dup against page headings) genuinely resolves it without churn. Severity adjusted high -> medium: the defect is real and user-visible (a documentation section becomes unreachable from navigation and its anchor misroutes) but is confined to a single getting-started docs page, causes no crash/data loss/security issue, and the section content itself still renders.)

### F-002 · medium · tests · apps/docs/src/components/toc.test.tsx:42-89, apps/docs/src/components/toc.tsx:115-135, apps/docs/src/routes/$lib/$.tsx:94-116
The change set rewrites TableOfContentsPanel from a pure derive-during-render of the compile-time fumadocs TOC into a useState seed + useEffect + MutationObserver that re-derives entries from the rendered DOM as headings appear. The stated reason (toc.tsx:111-114) is that MDX is Suspense-loaded and runtime headings (<Step> titles, API reference) are injected AFTER the panel mounts, so the static TOC is incomplete. The clientLoader at $lib/$.tsx:94-116 renders the MDX asynchronously, so in production the observer (not the initial sync()) is the path that picks up those headings. The brand-new toc.test.tsx is the only behavioral guard for this component (no e2e/playwright covers it), yet all four tests render every heading synchronously via renderWithHeadings before TableOfContentsPanel mounts. They therefore only exercise the one-shot sync() inside the effect; none mutates #main-content after render. The test named 'includes runtime headings (Step titles) the compile-time TOC misses' (lines 53-70) is misleading: it renders the 'runtime' headings statically up front, proving the DOM-read path, not the observer-refresh path. A regression that broke the observer wiring (wrong target, dropped subtree:true, broken entriesSignature dedupe, or a missing re-subscription) would pass every test while breaking the TOC for every Steps/api-reference page in the app.
Evidence: Read all four cases in toc.test.tsx (renderWithHeadings renders headings + panel together, no post-render DOM mutation). Read toc.tsx:107-135 (useState seed from entriesFromToc, effect runs sync() once then attaches MutationObserver with subtree:true and entriesSignature dedupe). Confirmed via $lib/$.tsx:4,94-116 that MDX is Suspense-loaded through createClientLoader, so headings arrive after mount and only the observer catches them. Ran the file: 4/4 pass; rg confirmed no playwright/e2e and no other test references TableOfContentsPanel.
Fix: Add a behavior test that renders the panel first, then appends an h2[id] into #main-content after mount and waits for the new link to appear (asserting the observer-driven refresh), plus a case that removes a heading.
round: 1 · skeptic: confirmed (Verified end-to-end. toc.tsx:115-135 is the new useEffect+MutationObserver(subtree:true) with entriesSignature dedupe and disconnect() cleanup — the genuinely effect-requiring core of the change set's headline TOC rework. All four toc.test.tsx cases (lines 42-89) render headings synchronously via renderWithHeadings (lines 17-31) so the panel mounts with headings already present; none mutates #main-content after mount. They therefore only exercise the one-shot sync() at toc.tsx:130 plus entriesFromDom, never the observer's reaction at lines 132-134 or its cleanup. Production ($.tsx:94-116 Suspense-loaded clientLoader.useContent) injects headings AFTER mount, so the observer — not the initial sync — is the real production path. A regression breaking the observer target, dropping subtree:true, breaking the toc-dep re-subscription, or removing disconnect() would pass all four tests while breaking the TOC for every Steps/api-reference page. The case named 'includes runtime headings (Step titles) the compile-time TOC misses' (lines 53-70) renders those headings statically, proving the DOM-read path not the refresh path. Confirmed no playwright/e2e covers it. The quality bar explicitly requires testing observer active-id transitions, cleanup on unmount, and re-subscription on id changes. Fix (render panel, then append/remove an h2[id] and waitFor the link) is a genuine improvement. Medium: central new behavior, real wiring-regression blind spot, no live bug yet.)

### F-003 · low · tests · apps/docs/src/components/toc.tsx:50-72, apps/docs/src/components/toc.test.tsx:17-31
entriesFromDom is new code with two branches the tests never reach: (1) depth derivation distinguishes h3 (depth 3) from h2 (depth 2) at toc.tsx:65, which feeds TocItem depth (indentation) and the headingIds passed to useActiveHeading; (2) a `seen` Set at toc.tsx:60-62 dedupes repeated ids so a duplicate heading id does not create two TOC links. The renderWithHeadings helper's type even advertises an `h2 | h3` tag union (toc.test.tsx:18), but every test passes only h2 with unique ids, so neither the depth=3 path nor the dedupe path is covered. A regression flipping the H3 check or dropping the seen guard would not be caught.
Evidence: Read toc.tsx:50-72 (depth = tagName === 'H3' ? 3 : 2; seen-Set dedupe). Read toc.test.tsx: helper accepts 'h3' in its type but rg for 'h3|duplicate|depth' in the test file returns only the type annotation and an unrelated toc-prop depth — no test renders an h3 or a duplicate id.
Fix: Add cases rendering an h3 (assert it is included and rendered at the nested depth) and two headings sharing one id (assert only one TOC link is produced).
round: 1 · skeptic: confirmed (Verified. toc.tsx:64 derives depth = tagName==='H3' ? 3 : 2 (feeds TocItem depth/indentation and the headingIds passed to useActiveHeading at toc.tsx:137-146); toc.tsx:60-62 dedupes ids via a seen Set. The test helper type at toc.test.tsx:18 advertises 'h2'|'h3', but all four cases pass only unique-id h2 headings, so neither the depth=3 branch nor the dedupe branch is reached. Both are real new code in the change set. The h3-depth path is commonly hit in real MDX and is genuinely worth guarding; the dedupe is more defensive. Net it is a factual, in-scope, low-value coverage gap and the proposed cases (an h3 asserting nested depth; two headings sharing an id asserting one link) are a real, non-churn improvement. Low: narrow branches, mostly visual/defensive impact.)

### F-004 · low · tests · apps/docs/src/lib/docs-library.test.ts:44-58, apps/docs/src/lib/docs-library.test.ts:60-71, apps/docs/src/lib/docs-library.test.ts:73-101, apps/docs/src/lib/docs-library.test.ts:151-161, apps/docs/src/lib/docs-library.test.ts:303-322, libs/keys/docs/hook-docs/use-action-row-navigation.ts:177-179, libs/keys/docs/hook-docs/use-focus-restore.ts, apps/docs/src/components/docs-mdx/blocks/examples.tsx:30-32, apps/docs/src/lib/resolve-examples.ts:7-13
This change set adds/edits the `examples: ExampleRef[]` arrays in 8 keys hook-docs (use-focus-restore.ts, use-focus-trap.ts, use-focus-zone.ts, use-key.ts, use-scope.ts, use-scoped-navigation.ts, use-scroll-lock.ts +1 each, and the new use-action-row-navigation.ts). Those declared `name`s are the live source of truth for what each keys hook page renders: resolveExamples() (resolve-examples.ts:7) returns `docs.examples` when non-empty, and the <Examples /> block does `const src = d.exampleSource?.[ex.name]; if (!src) throw new Error('Missing keys docs example source: ' + ex.name)` (examples.tsx:30-32). The repo has a regression guard for exactly this — docs-library.test.ts:303 'fails static validation when component, hook docs, or pages reference missing examples' plus :315 'requires hook pages with example sections to declare source examples'. But every collector feeding those assertions is hard-coded to the UI library only: collectExampleNamesFromComponentDocs/collectExampleNamesFromHookDocs scan `libs/ui/registry/*`, collectHookDocExampleCounts scans `libs/ui/registry/hook-docs`, collectHookPagesWithExamplesSection scans `libs/ui/docs/content/hooks`, and collectExampleFileNames scans `libs/ui/registry/examples`. So the keys hook-doc example references introduced here have zero test coverage in either drift direction.
Evidence: Traced both drift directions end-to-end. Direction 1 (declared name with no .tsx) crashes at render via examples.tsx:31; for UI it is caught by docs-library.test.ts:303, for keys it is caught by nothing. Direction 2 (an on-disk example omitted from the declared array) is silently dropped because resolveExamples short-circuits on the non-empty docs.examples list (resolve-examples.ts:7-13); neither library guards equality, only UI guards count>0 (line 315). build-docs-data derives the runtime demo loader from files only (examples.ts findExamples -> readdirSync), while resolve-examples.ts lists demos from the DECLARED examples[] — the two are never reconciled in the keys path. Verified the current change set is consistent (all 8 keys hook-doc `examples` arrays exactly match their `libs/keys/registry/examples/<hook>/*.tsx` files), so this is a coverage gap, not a manifested bug — a future typo/rename in any keys hook-doc example name would throw at page render with no failing test. No inline `<Example name=>` refs exist in keys content, so the unguarded surface is precisely the hook-doc `examples` arrays.
Fix: Generalize the UI-only collectors in docs-library.test.ts to also scan `libs/keys/docs/hook-docs`, `libs/keys/docs/content/hooks`, and `libs/keys/registry/examples` (or parametrize by library) so the existing missing-example and declared-source assertions also cover keys hook-docs.
round: 1 · skeptic: confirmed (Verified the asymmetry. Every collector feeding the missing-example guards is UI-only: collectExampleNamesFromHookDocs/collectHookDocExampleCounts scan libs/ui/registry/hook-docs (docs-library.test.ts:60-89), collectHookPagesWithExamplesSection scans libs/ui/docs/content/hooks (91-101), collectExampleFileNames scans libs/ui/registry/examples (151-161); the assertions at :303 and :315 only cover UI. This change set adds/edits the examples[] arrays in 9 keys hook-docs (8 modified + new use-action-row-navigation.ts:177-179). Those declared names are the live render source: resolveExamples returns docs.examples when non-empty (resolve-examples.ts:7-9) and the Examples block throws 'Missing keys docs example source: <name>' at render (examples.tsx:30-31). I confirmed the keys arrays currently match their on-disk libs/keys/registry/examples/<hook>/*.tsx files exactly, so this is a coverage gap, not a live bug — a future typo/rename in a keys example name would throw at page render with no failing test, and no keys test renders the declared-array path (the keys demo test at :376 only loads use-navigation-basic via the file-based demo loader). Fix (parametrize the UI-only collectors by library) is a genuine improvement on a public handoff surface. Severity lowered to low: no manifested bug and the failure mode is a loud render-time throw, not silent corruption.)

### F-005 · low · slop · apps/docs/src/components/toc.tsx:16-21, apps/docs/src/components/toc.tsx:38-44, apps/docs/src/components/toc.tsx:50-72, apps/docs/src/components/toc.tsx:212
`TocEntry` carries both `id` and a separate `key` field. The two builders assign `key` from different sources: `entriesFromToc` sets `key: item.url` (e.g. `#setup`, toc.tsx:42) while `entriesFromDom` sets `key: id` (the bare id, e.g. `setup`, toc.tsx:67). `key` is used only as the React list key (`<TocItem key={entry.key}>`, toc.tsx:212). Since `entry.id` is already unique within `tocEntries` in both paths (the DOM path dedupes via the `seen` Set; toc-seed ids derive 1:1 from unique urls), `key={entry.id}` (or `#${entry.id}`) is equivalent — the extra field is dead weight that forces the two builders to diverge. Worse, when the post-mount effect replaces the SSR seed with DOM entries (toc.tsx:115-135), the keys change from `#setup` to `setup` for every item, so React unmounts and remounts the entire TOC list rather than reconciling in place. The repo convention prefers stable IDs over duplicated derived state.
Evidence: Traced both producers (entriesFromToc toc.tsx:38-44 sets key=item.url; entriesFromDom toc.tsx:50-72 sets key=id, deduping ids via `seen`) and the sole consumer (toc.tsx:212 `key={entry.key}`). `id` drives href (220), active state (208/223), and `itemRefs` (215/217). The `key` field is never read anywhere else (rg over toc.tsx confirms only lines 20/42/67/212). The `entriesSignature` dedupe (line 76-78) does NOT consider `key`, so when DOM differs from the seed the effect always calls `setTocEntries` with the differently-keyed entries -> React remount of the whole list. No path produces two entries that share `id` but differ in `key`, so it adds no disambiguation over `id`.
Fix: Drop the `key` field from `TocEntry` and use `entry.id` directly as the React list key at toc.tsx:212, removing `key:` from both builder functions so the reconciliation key is stable across the SSR-to-DOM transition.
round: 1 · skeptic: confirmed (Verified against toc.tsx and the diff. `TocEntry` carries both `id` and `key` (16-21). `entriesFromToc` sets `key: item.url` (#setup, line 42) while the NEW `entriesFromDom` sets `key: id` (bare `setup`, line 67); `key` is read ONLY as the React list key (line 212 `key={entry.key}` — rg confirms lines 20/42/67/212 are the sole occurrences). `id` already uniquely identifies entries in both builders (DOM path dedupes via the `seen` Set; toc path ids derive 1:1 from unique fumadocs urls), so `key={entry.id}` is equivalent — the field adds no disambiguation and forces the two builders to diverge meaninglessly. The `entriesSignature` dedupe (76-78) ignores `key`, so when the post-mount effect (115-135) swaps the SSR seed for DOM entries, logically-identical surviving entries flip key from `#setup` to `setup` and remount needlessly. This divergence is newly introduced by this change (HEAD had a single static list). Severity adjusted to low (not the claimed 'full list remount': for pure-markdown pages signatures match and no swap occurs; the unnecessary remount is confined to entries that survive the seed->DOM transition on Step/api-reference pages). The fix — drop `key` from TocEntry, use `entry.id` at line 212, remove `key:` from both builders — is surgical, harmonizes the builders, stabilizes reconciliation, and matches the repo 'store stable IDs' convention. Genuine improvement, not churn.)

### F-006 · info · types · apps/docs/src/components/docs-mdx/blocks/steps.tsx:24-36, apps/docs/src/components/docs-mdx/blocks/steps.tsx:25-27, apps/docs/src/components/docs-mdx/blocks/steps.tsx:32, apps/docs/src/components/docs-mdx/blocks/steps.tsx:33
In the new `Steps` component, children are filtered with an explicit type predicate that over-claims: `Children.toArray(children).filter((child): child is ReactElement<StepProps> => isValidElement(child))`. React 19's `isValidElement<P>(object): object is ReactElement<P>` infers `P = StepProps` from the predicate but never verifies it — nothing checks `child.type === Step`. The result is `step.props.title` typed as `string` (line 32/33) even though a non-Step valid element carries no `title`. The typed-but-wrong value is used as the React `key` (`cloneElement(step, { index, key: step.props.title })`) and `cloneElement` injects an `index` prop the element may not accept. If any non-Step valid element ends up inside `<Steps>` (e.g. an MDX prose paragraph or other MDX component between `<Step>` blocks), `title` is `undefined` at runtime => `key` becomes `undefined` and step numbering miscounts, with zero compiler warning. This is the lying-guard anti-pattern: a name asserting safety the runtime check does not provide.
Evidence: React 19.2.14 types at @types+react/index.d.ts:722 (`isValidElement<P>(object): object is ReactElement<P>`) take P from the predicate, not validated. The repo's own sound patterns for the identical scenario gate the props-type claim on a `child.type ===` identity check: libs/ui/registry/ui/breadcrumbs/breadcrumbs.tsx:20-22 (`isValidElement<BreadcrumbsItemProps>(child) && child.type === BreadcrumbsItem`) and libs/ui/registry/ui/stepper/stepper.tsx:41 (`if (!isValidElement(child) || child.type !== StepperStep) return [];`). steps.tsx is the only Children-filtering compound component in the change set that omits the identity check. `<Step>` is exported and registered as an MDX component (blocks/index.ts:12, feature-mdx-components.tsx:19-40) and used across docs MDX, so author-supplied children inside `<Steps>` are the real input boundary; today only `<Step>` children are used so it does not break yet.
Fix: Guard the props claim with an identity check, e.g. `(child): child is ReactElement<StepProps> => isValidElement(child) && child.type === Step`, mirroring breadcrumbs.tsx/stepper.tsx.
round: 1 · skeptic: confirmed (Factually real at apps/docs/src/components/docs-mdx/blocks/steps.tsx:25-27: the predicate `(child): child is ReactElement<StepProps> => isValidElement(child)` over-claims StepProps with no `child.type === Step` identity check, while the repo's own sibling compound components do guard it (libs/ui/registry/ui/breadcrumbs/breadcrumbs.tsx:21 `isValidElement<BreadcrumbsItemProps>(child) && child.type === BreadcrumbsItem`, libs/ui/registry/ui/stepper/stepper.tsx:41 `if (!isValidElement(child) || child.type !== StepperStep) return [];`). Steps is a NEW file in scope and Step/Steps are MDX-registered (blocks/index.ts:12, feature-mdx-components.tsx:19-20,38-39) and used in 15 MDX pages, so author children are the real boundary. The fix (add `&& child.type === Step`) is a genuine low-churn soundness improvement matching established repo precedent, not churn. Downgraded low->info: the candidate's consequence trace is partly wrong — `index` at steps.tsx:31 comes from the `.map((step, index))` position, NOT props, so numbering can never miscount; the only real fallout for a hypothetical non-Step valid child is `key: step.props.title` becoming undefined (a React dev key warning, array-index fallback) plus an ignored injected `index` prop. No non-Step direct child exists in any of the 15 current usages (MDX block JSX siblings are not wrapped in prose elements), so the defect is latent, not a live break. Confirmed as a real lying-guard type issue at info severity.)

### F-007 · medium · errors · libs/keys/registry/examples/use-key/use-key-scoped.tsx:12-23, libs/keys/src/providers/keyboard-provider.tsx:153, libs/keys/src/dom/keyboard-utils.ts:166-171
The new use-key-scoped registry example binds `useKey("ctrl+s", () => setSaved(n => n+1), { containerRef: cardRef, focusWithinOnly: true, preventDefault: true })` and prints the instruction 'Click into the textarea, then press Ctrl+S. Saves: {saved}'. Because `focusWithinOnly: true` requires focus to be inside the card, and the card's only focusable element is the `<textarea>` (an editable element), every keypress that can satisfy the focus-within gate originates from an editable target. The example omits `allowInInput: true`.
Evidence: Traced end-to-end: (1) Example useKey options at use-key-scoped.tsx:12-16 set focusWithinOnly+containerRef but NOT allowInInput. (2) KeyboardProvider.handleKeyDown computes `isEditable = isEditableElement(event.target)` and runs `if (isEditable && !entry.options?.allowInInput) continue;` (keyboard-provider.tsx:146,153), skipping the handler. (3) isEditableElement returns true for a non-disabled, non-readonly `<textarea>` (keyboard-utils.ts:169-171). (4) The only focusable element inside cardRef is the textarea, so satisfying focusWithinOnly necessarily means focus is on the editable textarea, which always trips the guard. Result: following the example's own printed instructions never increments `saved`. Sibling examples (use-key-basic.tsx, use-key-map.tsx) avoid this because their shortcuts are not bound to inputs.
Fix: Add `allowInInput: true` to the useKey options in use-key-scoped.tsx so the container-scoped Ctrl+S fires while the textarea is focused, matching the example's instructions.
round: 1 · skeptic: confirmed (Verified factually at all three cited locations and traced end-to-end. The new registry example libs/keys/registry/examples/use-key/use-key-scoped.tsx:12-16 binds ctrl+s with containerRef+focusWithinOnly:true+preventDefault:true and OMITS allowInInput. The card (cardRef) at line 19-22 contains only a <p> and a <textarea>; the div has no tabIndex, so the textarea is the ONLY focusable element. focusWithinOnly requires the keydown's event.target to be inside cardRef (isEventWithinContainer, keyboard-provider.tsx:86-95, 154), which for a Ctrl+S keypress means the focused textarea is event.target. In handleKeyDown, isEditable = isEditableElement(event.target) is true for a non-disabled/non-readonly textarea (keyboard-utils.ts:169-171), and line 153 `if (isEditable && !entry.options?.allowInInput) continue;` skips the handler BEFORE the container check and before the preventDefault at line 156. Result: following the example's own printed instruction ('Click into the textarea, then press Ctrl+S. Saves: {saved}') never increments saved, and the native Ctrl+S is not even prevented. This directly contradicts the hook docs behavior table (use-key.mdx:37-38), which states focusWithinOnly fires while focus is inside the container and that editable targets are ignored 'unless allowInInput is set.' The useKey hook (use-key.ts:62-69) and KeyboardProvider register the binding under the global scope correctly, so the registration path is fine — the example's option set is the defect. Sibling examples use-key-basic/use-key-map bind page-level shortcuts not tied to inputs, so they are unaffected. This is a NEW untracked file in the change set (context.md section 4), reviewed top-to-bottom, so it is in scope. The proposed fix (add allowInInput: true) is the correct minimal fix and matches the documented mechanism for firing a shortcut while an editable element is focused — not churn. Downgraded from high to medium: it is a confirmed, self-contradicting public registry example that will mislead copy-mode consumers, but the blast radius is one demo file with no functional/library/build/test breakage.)

### F-008 · low · conventions · libs/keys/docs/hook-docs/use-action-row-navigation.ts:149
In the new use-action-row-navigation hook-doc, the getActionProps return type string is '(index: number) => { ref; "data-action-index": number; onFocus: () => void }' — the `ref` member has no type annotation. The actual hook source declares `ref: RefCallback<HTMLElement>` (libs/keys/src/hooks/use-action-row-navigation.ts:92-96). Every other inline-object type across the entire hook-docs corpus fully annotates each member (e.g. use-focus-zone.ts:148 `{ "data-focused": true | undefined }`, use-focus-restore.ts:43 `{ capture: (...) => ...; restore: () => boolean; target: HTMLElement | null }`). This is the sole bare untyped member, so it both diverges from the established convention and under-documents the public getActionProps contract.
Evidence: Traced the full docs pipeline: createHookDocLoader (libs/registry/src/docs-data/utils.ts:86) loads the doc by export-name convention; build-docs-data.ts feeds returns.properties into the generated hook JSON; apps/docs ReturnsTable (returns-table.tsx:8-22) renders properties via ParameterTable, displaying each row's `type` string verbatim. So the getActionProps row in the rendered API reference shows a bare `ref;` with no type. Confirmed via `rg '\{ [a-z]+;' libs/keys/docs/hook-docs/*.ts` that this is the only untyped inline-object member, and confirmed the actual source ref type is RefCallback<HTMLElement> at use-action-row-navigation.ts:93.
Fix: Annotate the member as `ref: RefCallback<HTMLElement>` in the getActionProps type string to match the source signature and the corpus-wide convention.
round: 1 · skeptic: confirmed (Verified factually real and in scope. At libs/keys/docs/hook-docs/use-action-row-navigation.ts:149 the getActionProps type string is `(index: number) => { ref; "data-action-index": number; onFocus: () => void }` — `ref` is bare with no type. The actual source declares `ref: RefCallback<HTMLElement>` (libs/keys/src/hooks/use-action-row-navigation.ts:92-96). This is a NEW untracked file (in scope as fully-new code). `rg` confirms it is the SOLE bare inline-object member across the hook-docs corpus, which otherwise fully annotates every member (use-focus-restore.ts:43, use-focus-zone.ts:109/148). I traced rendering: returns-table.tsx:8-22 -> parameter-table.tsx:32-33 renders the `type` string verbatim (`: {param.type}`), so the rendered public-API docs show a bare `ref;`, under-documenting the public return contract. Not intentional (the same file fully spells RefObject types, e.g. line 44); one-token fix matching the source signature and corpus convention. Severity low is appropriate (docs string, cosmetic but user-visible).)

### F-009 · info · simplicity · libs/keys/docs/content/hooks/use-scroll-lock.mdx:7, libs/keys/docs/content/hooks/use-scroll-lock.mdx:50, libs/keys/docs/hook-docs/use-scroll-lock.ts:34
The change set adds an intro paragraph (use-scroll-lock.mdx:7) and an 'Edge cases -> Refcounted release' bullet (use-scroll-lock.mdx:50) that both restate the reference-counting mechanic. The new bullet ('Each enabled useScrollLock on the same element increments a shared counter; the original overflow is only restored when the last lock releases') is a near-verbatim duplicate of the auto-rendered <Notes /> block sourced from use-scroll-lock.ts:34 ('Multiple useScrollLock calls on the same element increment a shared counter. Scrolling is only restored when all locks are released'), which renders immediately above the Edge cases section on the same page. The page now explains the same fact three times (intro, Notes, Edge cases).
Evidence: Traced the rendered page composition end-to-end: use-scroll-lock.mdx renders <Notes /> (line 46) directly before the new '## Edge cases' section (line 48). The <Notes /> block emits the metadata note at use-scroll-lock.ts:34, which states the identical 'shared counter / restored when all locks release' mechanic as the newly-added Edge-cases bullet at use-scroll-lock.mdx:50, plus the intro at line 7. All three are on the same rendered page. Spot-checked other Edge cases sections (use-key.mdx, use-focus-trap.mdx, use-focus-zone.mdx) — they add distinct facts.
Fix: Drop the redundant 'Refcounted release' bullet from the Edge cases list (use-scroll-lock.mdx:50) since the Notes block already states the refcounting behavior, keeping only the non-duplicate 'Toggling enabled' bullet.
round: 1 · skeptic: confirmed (Verified end-to-end. The change set (git diff HEAD) adds the intro paragraph at use-scroll-lock.mdx:7 ('Locks are reference-counted per element ... scrolling is only restored once both release') AND the '## Edge cases -> Refcounted release' bullet at line 50 ('Each enabled useScrollLock ... increments a shared counter; the original overflow is only restored when the last lock releases'). The page renders <Notes /> (line 46) immediately above the Edge cases section; <Notes /> emits the 'Reference counting' note from use-scroll-lock.ts:34 ('Multiple useScrollLock calls ... increment a shared counter. Scrolling is only restored when all locks are released, preventing double-unlock bugs') — a near-verbatim restatement of the new Edge-cases bullet. Confirmed the asymmetry against siblings: use-focus-trap (Notes: Standalone/Tab wrapping; Edge cases: initialFocus/restoreFocus/Container tabindex), use-key (Notes: overloads/provider/scope/declining; Edge cases: No provider/scope null/key map overload), and use-focus-zone all add DISTINCT Edge-cases facts with no Notes overlap. use-scroll-lock is the lone outlier whose Edge-cases bullet duplicates its Notes block. The proposed fix (drop the redundant 'Refcounted release' bullet, keep 'Toggling enabled') aligns the page with its siblings and is a genuine, non-churn improvement. Severity info stands.)

### F-010 · low · tests · apps/docs/src/lib/docs-tree.test.ts:115-164, apps/docs/src/lib/docs-tree.ts:69-77, apps/docs/src/routes/$lib/$.tsx:187, apps/docs/src/routes/$lib.tsx:18-21
`findPageNeighbors` is the engine behind the new footer prev/next pager (`DocsFooterPager` at $.tsx:187). In production it is always called with the mapped library tree from `mapPageTreeForLibrary` ($lib.tsx:18-21), which always retains `separator` nodes interleaved between pages (and may contain folder children via the single-folder flatten special-case at docs-tree.ts:215-218). The pager's correctness depends on `flattenPageTree` skipping those separators so that `previous`/`next` resolve to the adjacent navigable *pages*, not to a separator index. However, all three `findPageNeighbors` test fixtures (docs-tree.test.ts:116-131) are flat lists of only `type:"page"` nodes — none places a `separator` between two pages, and none uses a folder. So the separator/folder-skip behavior that every real pager render relies on is not asserted for this function; it is only covered transitively through `firstNavigablePage` (which does test leading-separator skip and folder descent at docs-tree.test.ts:166-218).
Evidence: Read docs-tree.ts: `findPageNeighbors` -> `flattenPageTree` (lines 42-77) which skips separators/url-less folders. Traced the production caller chain: $lib.tsx:18-21 builds `pageTree = mapPageTreeForLibrary(...)` (separators always returned by `mapNodeForLibrary` line 168-170), threaded to `DocsFooterPager` at $.tsx:169/187 which calls `findPageNeighbors(tree, pageUrl)`. Inspected all 3 fixtures at docs-tree.test.ts:116-163: every node is `type:"page"`; no separator sits between pages, no folder is present. Ran the suite (vitest) — 25 tests pass, confirming the fixtures as-is.
Fix: Add a `findPageNeighbors` case whose fixture interleaves separators (and a folder) between pages and assert prev/next still resolve to the adjacent pages, matching the separator-bearing tree the pager receives in production.
round: 1 · skeptic: confirmed (Verified. findPageNeighbors (docs-tree.ts:69-77) delegates to flattenPageTree (42-53) which strips separators and url-less folders, then does findIndex/prev/next. Production always feeds it a separator-bearing tree: $lib.tsx:18-22 builds pageTree via mapPageTreeForLibrary (separators retained at mapNodeForLibrary:168-170), threaded through $.tsx:121 -> DocsFooterPager ($.tsx:169/187) -> findPageNeighbors(tree, pageUrl). But all three findPageNeighbors fixtures (docs-tree.test.ts:116-131) are flat type:'page' lists — no separator between pages, no folder. So the separator/folder skip is exercised for findPageNeighbors only transitively (firstNavigablePage tests it at :167 and :184 via the shared flattenPageTree). The specific 'neighbor resolution across a separator/folder boundary' index behavior for findPageNeighbors is unasserted. In scope (docs-tree.test.ts +191, findPageNeighbors is new). Fix (a fixture interleaving separators+folder asserting prev/next resolve to adjacent pages) matches the production tree and is non-churn. Low: underlying skip logic is already covered through firstNavigablePage, so residual risk is small.)

### F-011 · info · slop · libs/keys/registry/examples/use-focus-restore/use-focus-restore-fallback.tsx:11-13, libs/keys/registry/examples/use-focus-restore/use-focus-restore-fallback.tsx:32-34
The new example's comment (lines 11-12) states 'No opener button: the palette is summoned from a global shortcut in real apps. `fallback` decides where focus lands if nothing was focused on open.' But the example renders a literal opener button at lines 32-34 (`<button onClick={openPalette}>Summon palette</button>`) that is the only way to open the palette in the demo. The comment's premise (no opener / fallback-as-primary-path) is false for the code it annotates, and the demonstrated `fallback` path is never exercised through the visible UI.
Evidence: Traced useFocusRestore (use-focus-restore.ts:74-96) -> capture() reads getRestorableFocusTarget(), and getRestorableFocusTarget (focus-restore.ts:11-21) returns doc.activeElement when it is a connected non-body element. In the demo, openPalette() runs from the Summon button's onClick, so document.activeElement IS the Summon button at capture time; capture records that button (not null), so restore() returns focus to the button and the anchorRef fallback (line 13) is never reached via the demo's own interaction. The comment asserting there is no opener button and that fallback governs the open path therefore contradicts the rendered code and the actual runtime behavior. Compared against sibling use-focus-restore-basic.tsx (same capture-on-button-click pattern), which carries no such misleading narration.
Fix: Either drop the contradictory 'No opener button' framing and describe what the button-driven demo actually shows, or remove the visible opener and trigger the palette via a real global shortcut so the fallback path is genuinely demonstrated.
round: 1 · skeptic: confirmed (Verified end-to-end in the NEW in-scope file use-focus-restore-fallback.tsx. Lines 11-12 assert 'No opener button: the palette is summoned from a global shortcut in real apps,' but lines 32-34 render `<button onClick={openPalette}>Summon palette</button>` — the demo's only way to open the palette. Traced the runtime: openPalette() (15-19) runs from that button's onClick, so at capture() document.activeElement IS the Summon button; getRestorableFocusTarget (focus-restore.ts:11-21) returns that connected non-body element, so capture records the button (use-focus-restore.ts:86-95), NOT null. On close, restore() -> releaseEntry -> restoreFocus(entry.target) succeeds (button still connected), returning focus to the Summon button; the `anchorRef` fallback (line 13) the example purports to demonstrate is never reached via the visible UI. The comment is internally contradictory and the named feature is dead in the demo's interaction — a real accuracy defect in a shipped registry teaching example (sibling use-focus-restore-basic.tsx carries no such misleading narration). Kept at info: documentation/comment accuracy in an example, no runtime/product impact. The proposed fix (drop the false 'No opener button' framing or trigger via a real global shortcut so fallback is exercised) is a genuine, scoped improvement.)

### F-012 · low · dry · apps/docs/src/routes/$lib/$.tsx:174-176, apps/docs/src/routes/$lib/index.tsx:20, apps/docs/src/layouts/sidebar.tsx:131-133, apps/docs/src/features/home/home-data.ts:53-55, apps/docs/src/lib/docs-library.ts:142-157
Converting a docs page url (always leading-slash, shaped /{library}/{...slugs}, e.g. '/ui/getting-started/installation') back into the TanStack route `_splat` ('getting-started/installation') is hand-duplicated across four sites with two different implementations. `$.tsx` (NEW DocsFooterPager) and `sidebar.tsx` define byte-identical `function splatFromUrl(url){ return url.split('/').slice(2).join('/'); }`; `index.tsx`'s new `resolveFirstPageSplat` server fn open-codes the same expression inline (`firstPage.url.split('/').slice(2).join('/')`); and `home-data.ts` carries a near-duplicate spelling (`url.replace(/^\//,'').split('/').slice(1).join('/')`) that yields the identical result. This change set ADDED the `$.tsx` copy and the `index.tsx` inline occurrence, pushing the count past the 3-occurrence threshold. `splatFromUrl` names a real, repeated concept and the repo already owns the inverse and sibling conversions in one canonical module (docs-library.ts: `docsPath`, `getRouteSlugsFromPathname`, `sourceSlugsForLibrary`, `routeSlugsFromSourcePath`), the obvious home for the helper. The divergent home-data spelling also means the 'same' concept now has two implementations that only agree for well-formed `/lib/...` urls.
Evidence: grep across apps/docs/src found splatFromUrl defined identically at $.tsx:174-176 and sidebar.tsx:131-133, inline at index.tsx:20, and a near-dup at home-data.ts:53-55. Traced url shape: every LandingItem.url comes from docsPath() (docs-library.ts:142-157), so it is always `/${library}` or `/${library}/${slugs}`; all four implementations produce the same splat (verified for /ui/getting-started/installation → getting-started/installation). `git show HEAD:apps/docs/src/routes/$lib/$.tsx` confirms no splatFromUrl in HEAD (added by this change) and HEAD sidebar.tsx already had it; index.tsx was fully rewritten (old version used collectLandingSections). All four call sites feed `_splat` to `<Link to="/$lib/$">` or the redirect. docsPath in docs-library.ts:142-157 is the canonical inverse, so a shared helper belongs beside it.
Fix: Add one exported `routeSplatFromDocsPath(url)` (a.k.a. splatFromUrl) helper in apps/docs/src/lib/docs-library.ts next to `docsPath` and have $.tsx, index.tsx, sidebar.tsx, and home-data.ts call it instead of re-deriving the slice.
round: 1 · skeptic: confirmed (Verified end-to-end. $.tsx:174-176 defines splatFromUrl = url.split('/').slice(2).join('/') and is NEW this change set (git show HEAD:$.tsx has no splatFromUrl). index.tsx:20 open-codes the identical expression inline (firstPage.url.split('/').slice(2).join('/')), also new (route fully rewritten). sidebar.tsx:131-133 has a byte-identical helper, but it ALREADY existed at HEAD (HEAD sidebar.tsx:88-89), so it is pre-existing context, not change-set-introduced. home-data.ts:53-55 (replace(/^\//,'').split('/').slice(1).join('/')) is a near-dup that is BOTH pre-existing AND entirely out of the 82-file audit scope (not in context.md section 4; git status clean) — so the candidate's '4 consumers / past the 3-occurrence threshold' framing is inflated. The genuine, in-scope, change-set-introduced duplication is the two new sites ($.tsx + index.tsx) re-deriving a named concept (route splat = inverse of docsPath) that has a canonical home: docsPath in docs-library.ts:142-157 always emits '/${library}' or '/${library}/${slugs}', so every node url (docs-tree.ts:180,200) is well-formed and all four spellings produce identical splats (traced /ui/getting-started/installation → getting-started/installation for slice(2) and slice(1)). A single exported helper beside docsPath genuinely removes the new duplication and the divergent-spelling risk; it is not churn. Downgraded from medium to low: this is a tiny pure-string helper with no behavioral bug and minimal blast radius, and only 2 of the 4 cited sites are actually in scope.)

### F-013 · low · correctness · libs/keys/registry/examples/use-focus-restore/use-focus-restore-fallback.tsx:8-9, libs/keys/registry/examples/use-focus-restore/use-focus-restore-fallback.tsx:13, libs/keys/registry/examples/use-focus-restore/use-focus-restore-fallback.tsx:16-19, apps/docs/registry/examples/keys/use-focus-restore/use-focus-restore-fallback.tsx:13, libs/keys/src/hooks/use-focus-restore.ts:35-42, libs/keys/src/hooks/use-focus-restore.ts:60-96
This NEW example is the canonical, live, copyable reference for useFocusRestore's `fallback` option (registered in hook-docs use-focus-restore.ts as 'Triggerless palette with fallback', rendered via <Examples /> in use-focus-restore.mdx). It calls `useFocusRestore({ fallback: anchorRef.current })`. `anchorRef` is a useRef(null) attached to the editor-surface div, so its .current is null during the component's only render that reads it (refs attach during commit, AFTER render, and ref attachment does not trigger a re-render), and there is no state change between mount and the first 'Summon palette' click. useFocusRestore copies options into optionsRef.current every render (use-focus-restore.ts:72) and capture()/restore() read optionsRef.current at call time. On the first open, capture() reads optionsRef.current.fallback === null, so in the 'nothing was focused on open' path (getRestorableFocusTarget returns null, then resolvedOptions.fallback at line 86) the advertised fallback-to-editor-surface behavior silently no-ops; it only begins working after a later open/close cycle re-renders and repopulates the ref. The hook's `fallback` is typed `HTMLElement | null` (no ref overload), so the example should read `.current` at event time instead of during render.
Evidence: Read the example and the hook source end-to-end. Traced: render passes anchorRef.current=null -> resolveOptions sets fallback:null (use-focus-restore.ts:35-42) -> optionsRef.current.fallback=null (line 72); ref attaches at commit without re-render; first capture() in openPalette (runs BEFORE setOpen(true)) reads optionsRef.current.fallback=null; getRestorableFocusTarget returns null when body/nothing is focused (focus-restore.ts:11-21). Confirmed the file is synced verbatim into apps/docs/registry/examples/keys/... and that apps/docs `tsc --noEmit` passes with 0 errors, so this is a runtime/teaching defect, not a type error. Compared against sibling use-focus-restore-basic.tsx which does not use fallback, confirming the defect is unique to the new file. (Note: in the demo's own button-driven flow the clicked Summon button is the active element at capture, so the fallback path is also never exercised through the visible UI — the feature is dead either way.)
Fix: Read the anchor element at call time (capture/restore the element inside openPalette/closePalette via anchorRef.current), or have useFocusRestore accept a RefObject fallback (mirroring useFocusTrap's initialFocus), so the resolved element is the live anchor rather than a first-render null snapshot.
round: 1 · skeptic: confirmed (Verified end-to-end. This NEW file is the SOLE canonical, registered ('Triggerless palette with fallback', hook-docs use-focus-restore.ts:61), <Examples />-rendered (use-focus-restore.mdx:33), and verbatim-synced (apps/docs/registry/examples/keys/use-focus-restore/use-focus-restore-fallback.tsx) copyable reference for the `fallback` option, whose edge-case the MDX explicitly documents (line 41: 'Supply one for triggerless UI so focus never lands on <body>'). The example calls useFocusRestore({ fallback: anchorRef.current }) (line 13) with anchorRef=useRef(null) attached to the editor div (line 28); on the only render before first open, .current is null (refs attach at commit without re-render). resolveOptions sets fallback:null (use-focus-restore.ts:35-42), optionsRef.current.fallback=null (line 72), and the first capture() in openPalette runs BEFORE setOpen(true) (lines 16-17) reading the null snapshot. Compounding this: getRestorableFocusTarget returns the clicked Summon button (it is document.activeElement, an HTMLElement, connected — focus-restore.ts:11-21), so the fallback branch (??resolvedOptions.fallback at line 86) is never even reached on any user-triggerable path. Net: the canonical example for `fallback` never exercises `fallback`, and a consumer copying the `fallback: ref.current` pattern gets a no-op on the critical first open — a real handoff/teaching defect in copyable registry source. Severity adjusted medium->low: the hook API is sound, there is no product runtime break (focus still restores to the Summon button), and the impact is confined to an example/teaching pattern. Fix (read the element at event time, or accept a RefObject like useFocusTrap's initialFocus) is a genuine improvement, not churn.)

### F-014 · low · correctness · libs/keys/registry/examples/use-scope/use-scope-nested.tsx:6-18, libs/keys/registry/examples/use-scope/use-scope-nested.tsx:22-23, libs/keys/src/providers/keyboard-provider.tsx:137-168
The Page component renders the Drawer unconditionally (line 16) and registers a global-scope Escape handler (line 10/11, no scope) with copy 'Escape here logs from the page. Open layers to see deeper scopes win.' The Drawer unconditionally calls useScope('drawer') (line 22), so the 'drawer' scope is always the active scope from first render (or 'dialog' when the dialog opens). The provider's handleKeyDown only dispatches to the active scope's handler map (keyboard-provider.tsx:140-168) with no walk-up to ancestor scopes, so the global-scope page Escape handler can never fire. Pressing Escape always yields 'drawer Escape', never 'page Escape', contradicting the example's own prose and the initial useState('page') label. The example still demonstrates dialog>drawer precedence, but the page-level statement is unreachable/misleading for a didactic reference shipped via the hook-doc.
Evidence: Read provider dispatch: it resolves activeRegistrationScope = getActiveScope() (top of scope stack) and only iterates handlers.current.get(activeRegistrationScope); no fallback to parent scopes, and it returns after the first matching handler. With Drawer always mounted, 'drawer' is always active over 'global', so setLog('page Escape') is dead. Confirmed Drawer is rendered unconditionally in Page (no conditional gating), and there is no control to unmount the drawer. The hook-doc references this example (libs/keys/docs/hook-docs/use-scope.ts: {name:'use-scope-nested', title:'Nested scopes resolve deepest-first'}), so it ships as a teaching example.
Fix: Make the Drawer conditionally mounted (e.g. an Open/Close drawer toggle on Page) so the page-scope Escape is genuinely reachable when the drawer is closed, matching the narrative; or adjust the page copy to not claim the page handler runs while the drawer is mounted.
round: 1 · skeptic: confirmed (Verified. Page renders <Drawer> unconditionally (use-scope-nested.tsx:15) and Drawer calls useScope('drawer') unconditionally (line 22), so 'drawer' is always the active scope (or 'dialog' when open). handleKeyDown resolves only the active scope's handler map (keyboard-provider.tsx:140-144) with NO walk-up to ancestor scopes and returns after the first match (lines 148-168); confirmed the page's scopeless useKey('Escape') (line 10) registers under 'global' via getScopeForOrder (it renders before the drawer's useScope, so its useId order precedes O_drawer, yielding 'global'). Because the drawer is always mounted with no UI to unmount it, the page-scope handler's setLog('page Escape') is dead — the line-14 prose 'Escape here logs from the page' describes an outcome the user can never observe (first Escape always logs 'drawer Escape'). The example is the registered/rendered teaching reference ('Nested scopes resolve deepest-first', use-scope.ts:48; <Examples /> in use-scope.mdx:35). Severity low (matches candidate): the example still correctly demonstrates dialog>drawer precedence via the toggleable dialog, and its own inline copy (line 9 'suppressed while a deeper scope is active', line 28 'Escape logs from the drawer, not the page') partly mitigates, but one framing sentence remains operationally false and the demo offers no path to reach the page-scope state. Fix (toggle the drawer mount, or reword the page copy) is a genuine improvement, not churn.)

### F-015 · low · errors · libs/keys/registry/examples/use-focus-trap/use-focus-trap-initial-focus.tsx:9-37, libs/keys/docs/content/hooks/use-focus-trap.mdx:52
The new worked example for useFocusTrap demonstrates initial focus by pointing it at the destructive Confirm button of a delete dialog, which directly contradicts the same hook page's new edge-case guidance that says to point initialFocus at Cancel rather than Confirm for destructive prompts.
Evidence: The new use-focus-trap.mdx 'Edge cases' section (added in this change set) states for `initialFocus`: 'for a destructive prompt, point it at Cancel rather than Confirm.' The new worked example referenced by that same hook page (`use-focus-trap-initial-focus`, wired via hook-docs/use-focus-trap.ts examples[] and surfaced through the <Examples /> block) is a 'Delete item' destructive dialog that does the opposite: `useFocusTrap(dialogRef, { enabled: open, initialFocus: confirmRef, ... })` with `confirmRef` on the `<button>Confirm</button>` (delete action), and its body text says 'This dialog opens with focus on Confirm.' I verified pickInitialTarget in use-focus-trap.ts:75-82 honors initialFocus, so the example does focus the destructive Confirm. A reader who follows the prose guidance then sees the canonical example demonstrating the anti-pattern for a destructive confirmation.
Fix: Make the destructive example point `initialFocus` at the Cancel button (or change the example dialog to a non-destructive action) so it matches the hook page's own a11y guidance.
round: 1 · skeptic: confirmed (Verified end-to-end against the actual files, all part of this change set. (1) libs/keys/docs/content/hooks/use-focus-trap.mdx:52 — the 'Edge cases' section, ADDED in this change set (git diff HEAD confirms it is a new hunk), states for initialFocus: 'for a destructive prompt, point it at Cancel rather than Confirm.' (2) The new (untracked) worked example libs/keys/registry/examples/use-focus-trap/use-focus-trap-initial-focus.tsx is a 'Delete item' destructive dialog: useFocusTrap(dialogRef, { enabled: open, initialFocus: confirmRef, ... }) with confirmRef on the <button>Confirm</button> delete action (line 33), and its body text reads 'This dialog opens with focus on Confirm' (line 29) — the exact opposite of the prose. (3) libs/keys/docs/hook-docs/use-focus-trap.ts:59 wires this example (title 'Custom initial focus') into examples[], which the same MDX page surfaces via <Examples /> at line 44, so both the guidance and the contradicting example render on one page. (4) The hook source libs/keys/src/hooks/use-focus-trap.ts honors initialFocus (pickInitialTarget, minified), so the example genuinely focuses the destructive control. There is a real tension — Cancel is the first focusable element (line 30, before Confirm at line 33), so to DEMONSTRATE that initialFocus overrides the default the example must target a non-default control, which is why it targets Confirm — but the candidate's alternative fix (make the dialog a non-destructive action, e.g. Save/Cancel) preserves the demonstration while removing the destructive-prompt anti-pattern. Not a duplicate (ledger is empty), squarely in scope (all three files changed/new), intentional-only as a feature demo but not as a11y guidance, and cleanly fixable without churn. For an accessibility-first keyboard library, the canonical example teaching the exact anti-pattern the adjacent prose warns against is a legitimate low-severity docs/a11y-guidance defect.)

### F-016 · low · conventions · libs/ui/docs/content/changelog.mdx:1-3, libs/ui/docs/content/contributing.mdx:1-3, apps/docs/src/lib/seo.ts:65, apps/docs/src/routes/$lib/$.tsx:62-71, apps/docs/src/components/docs-page.tsx:38
This change set removes the `description:` frontmatter line from libs/ui/docs/content/changelog.mdx and libs/ui/docs/content/contributing.mdx. These are now the ONLY two MDX files in the libs/ui docs corpus without a description: 91 of 93 libs/ui pages keep one, and 100% of libs/keys docs pages have one. This breaks the otherwise-uniform per-page description convention these docs follow.
Evidence: Verified `git diff HEAD` on both files removes exactly the `description:` line. Scanned all 93 libs/ui/docs/content/*.mdx: only changelog.mdx and contributing.mdx lack `^description:` (the two changed files); every libs/keys/docs/content MDX has one. Traced the consequence: the `head` fn in apps/docs/src/routes/$lib/$.tsx:62-71 calls buildPageSeo({ description: loaderData.description }), and seo.ts:65 resolves `description ?? DEFAULT_SITE_DESCRIPTION` — so both pages now emit the generic site-wide description for name=description, og:description, and twitter:description instead of a page-specific one, and docs-page.tsx:38 renders no header subtitle (hasDescription is false).
Fix: Restore a page-specific `description:` to both changelog.mdx and contributing.mdx to keep the docs frontmatter convention uniform and preserve per-page SEO/og metadata.
round: 1 · skeptic: confirmed (Verified end-to-end. git diff HEAD on both files removes exactly the `description:` frontmatter line (changelog.mdx: 'Release notes for @diffgazer/ui.'; contributing.mdx: 'How to contribute to @diffgazer/ui.'). Scanned all 93 libs/ui/docs/content MDX files: only these two now lack a description (the exact two changed files) — every other page keeps one. Both pages are explicitly listed in libs/ui/docs/content/meta.json (lines 8-9: 'contributing','changelog' under '---Project---'), so they are navigable/served pages routed through apps/docs/src/routes/$lib/$.tsx. Consequence trace confirmed: head fn ($.tsx:65-69) passes loaderData.description to buildPageSeo; seo.ts:65 resolves `description ?? DEFAULT_SITE_DESCRIPTION`, so both pages now emit the generic site description for name=description, og:description, twitter:description (seo.ts:69-77); docs-page.tsx:38 sets hasDescription=false so no header subtitle renders. Not intentional: the same change set ENRICHED descriptions on every sibling index page (hooks/theme/utils/patterns/integrations all rewritten to more descriptive text), making these two the only outliers and contradicting the deliberate frontmatter curation pattern. low is correct — docs metadata/SEO + visual subtitle regression, not a functional/build break.)

### F-017 · medium · conventions · libs/ui/registry/component-docs/sidebar.ts:5, libs/ui/registry/component-docs/sidebar.ts:49, libs/ui/registry/component-docs/sidebar.ts:90, libs/ui/registry/component-docs/sidebar.ts:91-94, libs/ui/registry/lib/sidebar-variants.ts:3-9, apps/docs/src/components/docs-mdx/blocks/notes.tsx:6-20, apps/docs/src/components/docs-mdx/blocks/api-reference.tsx:5-24, apps/docs/src/components/props-table.tsx:16-19
This change set adds a sixth public Sidebar variant `terminal` to the source union (sidebar-variants.ts:3-9), the sidebarItemVariants CVA, the VariantGlyph (sidebar-item.tsx:100-109), a test, an example, the public registry JSON (public/r/sidebar.json, sidebar-variants.json), registry.json, and the docs MDX intro (components/sidebar.mdx now enumerates all six including terminal). It also bumped only the top-level `description` field in component-docs/sidebar.ts from 'five' to 'six active-marker variants' (line 5). But three other parts of the SAME hand-written doc file — the source of truth fed into build-docs-data.ts and rendered verbatim in the docs API-reference props table and notes — were not updated in lockstep and now contradict the shipped contract: (1) the 'Visual variants' note (line 49) still opens 'Five active-marker variants ...' enumerating only caret/inverted/bar/bracket/block; (2) props.Sidebar.variant.type (line 90) is still the literal string '"caret" | "inverted" | "bar" | "bracket" | "block"', omitting terminal so the published API type no longer matches the SidebarVariant union; (3) props.Sidebar.variant.description (lines 91-94) enumerates only those five markers. The variant.type field is an ad-hoc string mirror of the real union with no derivation or parity check, so it silently drifted. The rendered page is internally self-contradictory: line 5 + the MDX intro say 'six' and the component genuinely accepts terminal, while the props table advertises a 5-member union type and the Visual-variants note describes only five. This violates the AGENTS rule 'Update source registry files, public registry JSON, docs, examples, generated bundles, and app consumers together when public APIs change'.
Evidence: Traced the full docs pipeline: component-docs/sidebar.ts is loaded by libs/ui/scripts/build-docs-data.ts (COMPONENT_DOCS_DIR / loadComponentDoc, lines 61-70) into processComponent (line 137 `props: docs?.props`, line 147 emits `docs`), writing docs/generated/components/sidebar.json (confirmed: line 4 description='...six active-marker variants...', but line ~29 type=`"caret" | "inverted" | "bar" | "bracket" | "block"` and the variant description omit terminal). On the docs site, APIReference (api-reference.tsx) -> PropsTableBlock -> props-table.tsx:16-19 renders info.type/info.description verbatim, and the 'Visual variants' note renders verbatim via notes.tsx:10-20 — so consumers read a 5-member union and 'Five active-marker variants' even though terminal is a real selectable value. `git diff HEAD -- component-docs/sidebar.ts` shows the ONLY changed line is the description (line 5, five->six); lines 49/90/94 are unchanged and stale; `rg "terminal" component-docs/sidebar.ts` returns nothing. Confirmed terminal is genuinely public: in the SidebarVariant union (sidebar-variants.ts:3-9), with a CVA branch and VariantGlyph branch (sidebar-item.tsx:100-109), faithfully mirrored in public/r/sidebar.json and sidebar-variants.json (byte-match source), covered by a passing test (sidebar.test.tsx:492-501), and exercised by the new example (examples/sidebar/sidebar-variants.tsx:23) and apps/docs (sidebar.tsx:161 uses variant="terminal"). No validator guards it: validate-registry-metadata.ts / registry-exports-validator.ts only check the props table is non-empty (prop count), never the type string against the TS source; docs-library.test.ts only checks example references resolve. UI sidebar test suite (68/68) and type-check pass, so this is documentation-only, not a build break. The sibling sidebar.mdx WAS correctly updated to 'Six active-marker variants ... and terminal', proving the component-docs omission is an inconsistency, not intentional scoping.
Fix: In libs/ui/registry/component-docs/sidebar.ts add `terminal` to props.Sidebar.variant.type (line 90) and its description (line 94), and change the 'Visual variants' note (line 49) from 'Five' to 'Six' with a terminal clause matching the new description (> prompt + 1px hairline rail, no bg fill), so the API-reference contract matches the SidebarVariant union and the MDX prose.
round: 1 · skeptic: confirmed (Verified end-to-end. component-docs/sidebar.ts is hand-written registry source modified in this change set; git diff HEAD shows the ONLY changed line is the top description (line 5, 'five'->'six active-marker variants'), while three sub-fields stayed stale: line 49 'Visual variants' note still 'Five active-marker variants ...' (caret/inverted/bar/bracket/block only); line 90 props.Sidebar.variant.type literal '"caret" | "inverted" | "bar" | "bracket" | "block"' omits terminal; line 94 description omits terminal. `grep terminal` on the file returns nothing. terminal is genuinely a public value: SidebarVariant union member (sidebar-variants.ts:9), real CVA branch (sidebar-variants.ts:97-102), VariantGlyph '>' branch (sidebar-item.tsx:100), present in public/r/sidebar.json, and registry.json sidebar-variants description WAS updated five->six in the same diff, and sidebar.mdx WAS updated to enumerate all six — proving the omission is an inconsistency, not scoping. Pipeline confirmed: build-docs-data.ts:137 emits `props: docs?.props` and :147 emits `docs` (carrying notes); props-table.tsx:16 renders info.type verbatim and notes.tsx:20 renders note.content verbatim. The materialized libs/ui/docs/generated/components/sidebar.json currently shows TOP description='...six...' but VARIANT TYPE='"caret" | "inverted" | "bar" | "bracket" | "block"' and the note 'Five active-marker variants ...' — so the rendered API-reference page is self-contradictory and understates the public union, misleading consumers into thinking variant="terminal" is invalid. Violates the AGENTS lockstep rule. medium is appropriate: it is a wrong published public-API documentation contract introduced by an incomplete update in this change set, internally contradictory on the same page; not raised higher because type-check/tests pass and it is documentation-only (no runtime/build break).)

### F-018 · medium · correctness · libs/registry/src/docs/cache.ts:13-32, libs/registry/src/docs/cache.ts:57-110, libs/registry/src/docs/cache.ts:112-123, libs/registry/src/docs/index.ts:27, libs/registry/src/docs/index.ts:51-67, libs/registry/src/docs/index.ts:70, libs/registry/src/docs/sync-operations.ts:201-216, libs/registry/src/docs/sync-operations.ts:348, apps/docs/scripts/sync-artifacts.mjs:27-47
This change set threads a new optional `extraRootPages: string[]` through `SyncDocsOptions -> runDocsSyncPass -> writeRootMeta`, where it is appended to the root `content/docs/meta.json` `pages` array (sync-operations.ts:201-216). In apps/docs this value is derived at runtime from `config/docs-libraries.json` (authored, non-artifact libraries: `enabled && !artifactSource`, currently `["...app"]`) — see sync-artifacts.mjs:27-46. However the cache gate that decides whether to run the sync at all does NOT account for `extraRootPages`. `computeSyncFingerprint` (cache.ts:13-32) hashes only `origin`, `syncSchemaVersion`, and per-ARTIFACT manifest path/content + fingerprint path/value; authored non-artifact root pages contribute nothing because `getArtifactLibraries` filters out libraries with no `artifactSource`, so the `app` entry never reaches the fingerprint. `shouldSkipSync` (cache.ts:112-123) skips when the stored fingerprint matches and `docsOutputsExist` returns true, and `docsOutputsExist` (cache.ts:57-110) only checks file EXISTENCE — it never validates that root `meta.json`'s `pages` array reflects the current `extraRootPages`. Net: a config-only edit (add/remove/rename/reorder/disable an authored non-artifact library) with byte-identical artifact manifests leaves the fingerprint unchanged, the sync is skipped, `writeRootMeta` never runs, and the stale `pages` array persists — a newly-added authored root page silently never appears in nav, or a removed one lingers. Since the per-library index.mdx pages were deleted in this same change set, extraRootPages is now the only mechanism re-adding those root entries. `rootTitle` shares the same omission and root cause but is not currently wired by the caller; extraRootPages is the first live consumer this change introduces.
Evidence: Traced end-to-end: (1) sync-artifacts.mjs:27-29 builds `authoredRootPages` from docs-libraries.json (currently `['...app']` since `app` has no `artifactSource`), passed as `extraRootPages` (line 46). (2) index.ts destructures `extraRootPages` (line 27) and forwards it to `runDocsSyncPass` (line 70), but `computeSyncFingerprint(origin, syncSchemaVersion, artifacts)` (index.ts:51-55, cache.ts:13-32) never receives or hashes it; getArtifactLibraries (scripts/monorepo/artifacts/config.mjs:87-95) filters to `enabled && artifactSource` (ui, keys only), so the `app` library producing extraRootPages is never in the fingerprint's artifact list. (3) After a first sync, writeSyncState persists the fingerprint to .cache/sync-artifacts-state.json (gitignored, persists across local runs) (index.ts:72-76). (4) On a later run where ONLY docs-libraries.json changed while artifact manifests+fingerprints are byte-identical, `syncFingerprint` is unchanged, `docsOutputsExist` still returns true (meta.json files exist), so `shouldSkipSync` returns true (index.ts:58-67), logs `[docs-sync] Artifacts unchanged; skipping sync.`, and writeRootMeta never runs — the stale `pages` array persists. The committed, TRACKED root content/docs/meta.json (current pages = [...ui, ...keys, ...app]) drifts from config; it feeds the fumadocs source.pageTree consumed by resolveFirstPageSplat/firstNavigablePage ($lib/index.tsx:12-20) and the sidebar, yielding wrong navigation. Verification gates do not catch it: assertArtifactSyncOutputs / collectArtifactSyncValidationErrors (scripts/monorepo/artifacts/sync.mjs:375-388) only existsSync-check content/docs/meta.json, never its pages content. The two new tests (docs-sync.test.ts:305-345) use a fresh mkdtemp root with no prior state file, exercising only the FRESH sync path and never the skip-with-stale-meta path, so the regression is untested.
Fix: Include `extraRootPages` (and `rootTitle`) in `computeSyncFingerprint` by hashing them as scalar inputs alongside `origin`/`syncSchemaVersion` (or validate the root meta.json `pages` content in `docsOutputsExist`/`assertArtifactSyncOutputs`), and add a re-sync test that changes `extraRootPages` between runs with unchanged artifacts and asserts the root meta.json `pages` is rewritten.
round: 1 · skeptic: confirmed (Verified end-to-end against the change set. The NEW extraRootPages option threads SyncDocsOptions->runDocsSyncPass->writeRootMeta (sync-operations.ts:201-216, 348) and is the only mechanism re-adding authored root pages now that per-library index.mdx pages were deleted in this same change set. It is derived in apps/docs from docs-libraries.json (sync-artifacts.mjs:27-29,46) where the 'app' entry has enabled:true and NO artifactSource (confirmed in config/docs-libraries.json), so authoredRootPages=['...app']. computeSyncFingerprint(origin, syncSchemaVersion, artifacts) (index.ts:51-55, cache.ts:13-32) never receives extraRootPages/rootTitle; getArtifactLibraries filters to enabled&&artifactSource (config.mjs:87-95), so the authored 'app' library never reaches the fingerprint inputs. shouldSkipSync (cache.ts:112-123) skips when the stored fingerprint matches and docsOutputsExist returns true, and docsOutputsExist (cache.ts:57-110) only existsSync-checks meta.json, never validates its `pages` content. Confirmed the root content/docs/meta.json is TRACKED/committed (pages=[...ui,...keys,...app]) and the state file apps/docs/.cache/sync-artifacts-state.json IS gitignored (persists across local runs). Net: a config-only edit (add/remove/rename/reorder an authored non-artifact library) with byte-identical artifact manifests leaves the fingerprint unchanged on a warm cache -> sync skipped -> writeRootMeta never runs -> stale root `pages`, yielding wrong sidebar nav / firstNavigablePage resolution; the committed meta.json then drifts from the committed config. Confirmed CI cold-cache and the currently-committed artifacts are correct (no state file -> first run always syncs), and assertArtifactSyncOutputs (sync.mjs:~375) only existsSync-checks meta.json, so gates don't catch it. The two new tests (docs-sync.test.ts:305-345) run on a fresh mkdtemp root with no prior state, exercising only result.synced===true (fresh path) and never the skip-with-stale-meta path, so the regression is untested. Medium is justified: editing the authored-library config on a warm cache is a normal incremental-dev operation that silently produces wrong output for a newly-introduced feature whose input is excluded from its own cache key. Fix (hash extraRootPages/rootTitle into computeSyncFingerprint, or validate pages content; plus a re-sync regression test) is a clean correctness fix, not churn.)

## Rejected — do not re-report, do not re-judge

### R-001 · conventions · apps/docs/content/docs/app/getting-started/quickstart.mdx:12
Claimed: quickstart's canonical "Z.AI" spelling now diverges from sibling reference docs still using "Z.ai"; refuted — The changed file is itself correct. quickstart.mdx:12 was updated to the canonical "Z.AI, Z.AI Coding Plan" matching the source of truth (libs/core/src/catalog/catalog-snapshot.ts:8914,9147). The candidate concedes "That is the correct spelling." The claimed divergence is PRE-EXISTING at HEAD, not caused or newly exposed by this change: confirmed via `git show HEAD:` that privacy.mdx (L57) and providers-and-models.mdx (L10,12) already used "Z.AI" at HEAD, while providers.mdx, environment-variables.mdx, and faq.mdx already used "Z.ai" at HEAD. The change simply moved quickstart from the legacy camp to the canonical camp. The proposed fix targets three files (providers.mdx, environment-variables.mdx, faq.mdx) NOT in the working-tree change set. Out of scope and churn on unchanged files; the in-scope edit is correct.
round: 1

### R-002 · conventions · apps/docs/src/components/docs-mdx/blocks/steps.tsx:48-54
Claimed: Step titles render as flat h2 siblings of "## Steps", flooding the TOC with non-nested entries; refuted — Factually accurate (steps.tsx:48 renders Step titles as as="h2" with slugified ids; toc.tsx:57-64 lists all h2[id] at depth 2), but this is the documented, intended design, not a defect. context.md section 5 explicitly describes the Steps block as producing "slugified `h2` ids that the new TOC picks up," and the committed toc.test.tsx ("includes runtime headings (Step titles) the compile-time TOC misses", lines 53-70) asserts Step titles appear as first-class flat TOC entries alongside sections as the EXPECTED output. No a11y rule is violated: the quality bar's heading rule is "no skipped levels"; quickstart.mdx/first-review.mdx have all h2 headings (after the frontmatter h1), so no level is skipped. The proposed h3 fix contradicts the documented intent and would break the committed test. Churn on intentional design.
round: 1

### R-003 · conventions · apps/docs/src/components/source-viewer.tsx:36-42
Claimed: Source block puts heading id on wrapper div, so new DOM-derived TOC silently omits it while API Reference appears; refuted — Factually accurate (id="source" sits on the wrapper div at source-viewer.tsx:36-39 and source-viewer-block.tsx:90-93, so the h2 has no id and the h2[id] selector skips it), but the id placement is PRE-EXISTING unchanged code. `git diff HEAD` shows the only change to both files is size="2xl"->"xl"; the id placement was identical at HEAD. The candidate itself admits this is "not a regression" — "Source" was never in the TOC before (old TOC was compile-time-only) and isn't now, so behavior is identical to HEAD with no user-visible harm (the #source scroll anchor with scroll-mt-16 still works on the div). The candidate frames it as an inconsistency the new mechanism "could and should resolve," i.e. a desired enhancement, not a defect introduced or exposed by the change set. Out-of-scope enhancement of pre-existing code.
round: 1

### R-004 · simplicity · apps/docs/scripts/generate-sitemap.test.ts:98
Claimed: Renamed sitemap test claims "lastmod can use mtime" but the code uses git timestamps; refuted — Factually accurate that the test name's 'so lastmod can use mtime' clause is wrong: formatLastMod (generate-sitemap.mjs:117-124) derives lastmod from getGitTimestamp (`git log -1 --format=%aI`) with only a hardcoded '2025-01-01' fallback; no statSync/mtime exists anywhere. However the inaccurate clause is verbatim pre-existing at HEAD — `git show HEAD` shows the test was named 'resolves a source MDX file for landing pages so lastmod can use mtime'. The change set only swapped 'landing pages' -> 'first-page targets' while retargeting the assertion to /ui/getting-started; it neither introduced nor exposed the mtime inaccuracy. Per the audit's anchoring rule, this is a stale wording carry-over on a test-name justification clause that the author touched incidentally. Lowest-possible signal; default-to-reject.
round: 1

### R-005 · tests · apps/docs/scripts/generate-sitemap.test.ts:29-38
Claimed: Sitemap test name claims redirect-target parity it cannot verify; refuted — Factually the assertions (generate-sitemap.test.ts:29-38, 98-107) only check filesystem presence of /ui|/keys|/app/getting-started, and getPreRenderPages() (generate-sitemap.mjs:31-56) derives paths from walkMdx, not from the redirect engine (routes/$lib/index.tsx -> firstNavigablePage over source.pageTree). That part is accurate. But the test NAMES ('omits the library roots that redirect...', 'lists the first-page redirect targets instead of the library roots') describe the sitemap's behavioral job — list canonical first pages, omit redirecting roots — which the assertions DO verify. They do not promise a cross-system parity assertion. The actual redirect-vs-tree parity the candidate worries about IS guarded elsewhere: docs-tree.test.ts:222-243 ('derives the scoped library's first page as the redirect target') asserts firstNavigablePage(mapPageTreeForLibrary(mergedRoot, lib)).url equals /{lib}/getting-started for ui/keys/app over a synthetic merged tree. The residual 'meta.json reorder' scenario is a theoretical edge the quality bar lists as a verification note, not a defect. The proposed fix is a cosmetic rename or a speculative duplicate parity test — churn. Reject per skeptic default.
round: 1

### R-006 · slop · apps/docs/src/components/docs-mdx/blocks/steps.tsx:38-39
Claimed: `Step` index default `= 0` is a defensive default for an always-supplied prop; refuted — Premise is factually wrong and the proposed fix breaks the build. `index` MUST be optional on StepProps: in MDX, every `<Step title="...">` (verified across apps/docs and libs/keys/libs/ui getting-started .mdx) is authored WITHOUT an `index` prop — it is injected at runtime by the parent `Steps` via `cloneElement(step, { index, key })` (steps.tsx:32). MDX compiles `<Step title>` to a JSX element whose props are type-checked against StepProps; if `index` were required (the candidate's fix), every such usage would be a TS error. The `= 0` default is the necessary complement to that forced-optional prop — without it `index + 1` evaluates to `NaN` and renders `NaN.`. This is the idiomatic compound-component clone-injection pattern, not defensive over-coding. The fix would churn the code and break `pnpm --filter @diffgazer/docs type-check`. Reject.
round: 1

### R-007 · types · apps/docs/src/layouts/sidebar.test.ts:105-106
Claimed: Redundant `as PageTreeNode` casts in new sidebar tests mask index-access undefined inconsistently; refuted — The factual base is accurate — apps/docs/tsconfig.json:20 sets strict:true with NO noUncheckedIndexedAccess, tsconfig.test.json extends it adding no such flag, so items[N] is already typed PageTreeNode and every `items[0] as PageTreeNode` cast in sidebar.test.ts:105-165 is a verified no-op; the same file uses `?.` at lines 57/70/71. But this is rejected as churn: the casts cause no bug, mask nothing today (the type is already non-undefined, so there is no `| undefined` for the cast to swallow), and have zero behavioral or type-safety impact. The 'if noUncheckedIndexedAccess is ever turned on' justification is purely speculative — apps/docs does not enable it and this change set does not touch that config. Removing the redundant casts is cosmetic test-code churn with no functional benefit; per skeptic charter (churn = reject).
round: 1

### R-008 · stack · apps/docs/src/components/docs-mdx/blocks/steps.tsx:31-35
Claimed: Steps block derives both React keys and heading ids from step title with no uniqueness fallback; refuted — Factually accurate: steps.tsx:32 sets `key: step.props.title` and steps.tsx:50 sets `id={slugify(title)}`, both keyed solely on the title with no de-dup, and the new toc.tsx `entriesFromDom` (toc.tsx:54-69) dedupes by id while `getElementById`/scrollTo resolve first-match only. BUT this is a non-triggering, speculative defect that the repo conventions explicitly tell us NOT to fix. Enumerated every MDX file using <Step> across apps/docs, libs/keys/docs, libs/ui/docs: ZERO within-page title collisions and ZERO within-page slug collisions today, so neither the duplicate React key nor the duplicate DOM id ever fires. The candidate concedes this ("does not fire yet — hence low severity. The gap is latent"). To trigger it an author must hand-write two identically-titled steps on ONE page — at which point React's dev-mode duplicate-key warning and the visibly identical step headings make the mistake self-evident at authoring time. Adding `${slugify(title)}-${index+1}` fallback is exactly the defensive over-engineering / speculative pattern the project conventions prohibit. Real markdown h2/h3 ids are produced by fumadocs' rehype-slug (which auto-suffixes duplicates), so Step's local slugify is a minor divergence, not a live bug. Low-signal, non-triggering, fix is churn against convention. Reject.
round: 1

### R-009 · dry · apps/docs/src/components/docs-mdx/markdown-renderers.tsx:73-85
Claimed: Docs section-heading style hand-duplicated across 5 sites (this change set edited all of them for the size change); refuted — Facts verified: 2xl->xl was edited in markdown-renderers.tsx:75, api-reference.tsx:16, source-viewer.tsx:40, source-viewer-block.tsx:94, and no SectionHeading abstraction exists. But the claimed 'single visual contract across 5 sites' overstates cohesion. The 5 sites are structurally heterogeneous: markdown-renderers is a generic MDX h2 renderer with className passthrough; api-reference is a bare h2 with id and no copy slot; source-viewer/source-viewer-block put the border/scroll-mt on a flex div[id=source] wrapper around a Typography (no id) plus a copy button; and steps.tsx (the 5th) does NOT use the border style at all — it uses 'mb-3 scroll-mt-16' with no pb-2/border-b, so it is not part of the same contract (verified: the full string 'pb-2 border-b border-border scroll-mt-16' appears in exactly 4 files, not 5). The only thing all sites truly share is a 3-token Typography invocation; spacing, border, wrapper, id, and copy-slot all differ per site. AGENTS.md Extraction Rules explicitly forbid this: 'Do not extract a helper only because two files look similar.' The 2xl->xl edit is a trivial one-token visual sync, not costly shotgun surgery. Premature abstraction / churn, not an improvement. Reject.
round: 1

### R-010 · conventions · libs/keys/docs/content/getting-started/installation.mdx:14-34
Claimed: Steps block renders step titles as <h2>, flattening the heading outline and TOC on the keys installation page; refuted — Same root cause and reasoning as the first-review/quickstart Steps candidate (already covered by R-002), effectively a duplicate framing on a different page. Step titles as <h2> is the documented intended design locked by the committed toc.test.tsx. No a11y violation: installation.mdx headings are all h2 (## Package install, ## Copy standalone hooks, and the step h2s), so no level is skipped; the candidate also confirms no slug collisions on this page. The proposed h3 fix contradicts documented intent and would break the committed test. 'Subordination/nesting' is the auditor's stylistic preference, not a correctness defect. Churn on intentional design.
round: 1

### R-011 · conventions · libs/keys/registry/examples/use-focus-zone/use-focus-zone-tab-cycle.tsx:29-35
Claimed: use-focus-zone-tab-cycle spreads getZoneProps but ignores its data-focused output, diverging from the styling it actually uses; refuted — Facts verified (getZoneProps at use-focus-zone.ts:265-266 returns only { "data-focused" }; the example styles active panes from `zone === z` inline styles), but not a defect. (1) Spreading getZoneProps onto the container is documented API usage — a consumer copying it gets a working data-focused attribute to hook CSS into; the demonstration is meaningful regardless of whether THIS example reads it back. (2) The examples use inline styles exclusively (no CSS files), so they cannot use an `&[data-focused]` selector — the proposed fix would force a CSS-file rewrite, churn. (3) The claimed inconsistency with use-focus-zone-basic is not a defect: basic uses neither tabCycle nor focus.targets so it has no getZoneProps to spread and uses isZone() instead; this example deliberately demonstrates tabCycle + focus.targets. (4) The search-zone placement (ref on inner <input>, getZoneProps on wrapper div) is correct intentional design. The candidate admits it is 'functionally harmless'. Pure churn on a new example file.
round: 1

### R-012 · stack · apps/docs/src/routes/$lib/index.tsx:9-23
Claimed: Bare library-root redirect recomputes the full mapped page tree in a second server function; refuted — Factually accurate ($lib/index.tsx:14-20 resolveFirstPageSplat re-runs mapPageTreeForLibrary(fromFumadocsRoot(source.pageTree), library), the same pure in-memory walk the parent docsShellLoader already performed; the index loader throws redirect before render so it cannot read useLoaderData()). BUT the impact is trivial and the proposed fix risks churn on a hotter path. The redundant work is a single synchronous walk of ~150 nodes; import('@/lib/source') is module-cached; during SSR a server fn is a direct call (no HTTP round-trip), and the redundant client round-trip only occurs on the rare bare-root /ui|/keys|/app hit which immediately redirects away. The suggested fix pushes added data/coupling onto the parent loader that runs on EVERY docs page (cached staleTime: Infinity) to save a trivial computation on a cold redirect-only path. The candidate itself states this is 'purely redundant computation, not a correctness defect' with type-check and 36 tests passing. Trivial cold-path micro-redundancy whose remedy trades hot-path cost for cold-path saving = churn. Reject per skeptic default.
round: 1

### R-013 · architecture · apps/docs/src/lib/docs-tree.ts:42-53
Claimed: Two independent re-derivations of 'sidebar order' from PageTree (pager/redirect vs rendered sidebar) can silently diverge; refuted — The cited lines are factually accurate (flattenPageTree recurses all children and emits folder urls; groupBySection emits only direct page children and never recurses; the 'in sidebar order' comments and both new consumers are genuine), but the divergence does not exist for any real input and is purely hypothetical. Verified every meta.json (root and leaf) under apps/docs/content/docs: every directory reference uses the fumadocs `...` extract-prefix spread, and fumadocs-core@16.7.5 flattens `...folder` INLINE as individual page nodes rather than a folder node. The rendered source.pageTree is therefore a flat separator/page list with zero top-level folder nodes, so mapPageTreeForLibrary never hits its folder branch or single-folder unwrap, and both functions consume identical flat data and produce identical ordering. The candidate concedes the folder branches are 'DORMANT for current content' and the risk only 'materializes if any meta.json switches a section to a non-spread folder reference' — a future content change that does not exist. Fails the audit scope gate (no current redirect, pager, or route affected). The two functions are also not redundant re-derivations of the same output (groupBySection builds titled/keyed sections for rendering; flatten builds a linear prev/next sequence — they share an input, not an output). New tests already exercise parity for the actual flat input. Speculative refactoring/churn against a non-occurring condition. Reject.
round: 1

### R-014 · correctness · libs/keys/registry/examples/use-action-row-navigation/use-action-row-navigation-basic.tsx:62-65
Claimed: use-action-row-navigation example narration claims ArrowUp moves focus to the title, but it only changes the zone; refuted — Factual trace confirmed (useActionRowNavigation builds useFocusZone WITHOUT a `focus` option; the focus-repair effect short-circuits on `!focus`; exitActions only sets zone+index, so DOM focus stays on the last action button after ArrowUp). HOWEVER the example communicates 'focus' purely via fontWeight (title: `fontWeight: !row.inActions ? 700 : 400` at line 31; actions also fontWeight at line 45). On ArrowUp `inActions` becomes false so the title visibly bolds — the highlight DOES 'return to the title' in the demo's own visual model, which is what the casual reader observes. The authoritative MDX keyboard-behavior table (use-action-row-navigation.mdx:40) correctly says 'Exits the actions zone back to content', so the canonical reference is accurate; only the example's casual prose is loose. The 'DOM focus doesn't move' objection is pedantic — the claim 'returns to the title' is true under the demo's visible bold-as-focus semantics, so it is not clearly false. Per the default-to-rejection-when-uncertain charter, this is a borderline prose nit, not a confirmable correctness defect.
round: 1

### R-015 · structure · libs/keys/registry/examples/use-action-row-navigation/use-action-row-navigation-basic.tsx:1
Claimed: New keys registry example sources are outside every libs/keys tsconfig type-check and test coverage; refuted — Mechanism is factually correct (no libs/keys tsconfig includes registry/examples; vitest includes only src/**/*.test.*; build-docs-data.ts reads them as raw text). BUT this is a pre-existing, intentional convention, not a defect introduced by the change set: the committed libs/keys/registry/examples/use-navigation/{use-navigation-basic,use-navigation-tabs}.tsx sit in the IDENTICAL directory, are equally not imported by src, and their synced apps/docs copies are equally gitignored. The repo's design type-checks registry examples via the synced apps/docs/registry/examples/keys/** copies (apps/docs tsconfig include **/*.tsx + tsc --noEmit), materialized by the registry/docs/public-handoff verification gate (prepare:artifacts + validate:artifacts:check) the change explicitly falls under. The AGENTS Verification Gates rule the candidate invokes is scoped to *.test.* files and the scripts/ dir (both covered), not to registry example fixtures. The candidate itself concedes the files are type-clean today and 'not an active break' — the harm is hypothetical and requires deliberately skipping artifact prepare. The change set merely adds more files to an established directory under an existing convention; the proposed extra tsconfig is duplicative churn against a documented design. Rejected on intentional-convention, out-of-scope, and churn grounds.
round: 1

### R-016 · types · libs/keys/tsconfig.json:39
Claimed: New keys registry examples are excluded from every tsconfig, so their types are never checked and a type error would pass all repo gates silently; refuted — The narrow claim that libs/keys tsconfigs don't compile registry/examples is literally true, but the load-bearing consequence ('a type error would pass ALL repo gates silently') is FALSE. These 8 example files are synced into apps/docs/registry/examples/keys/** by prepare:artifacts (the synced apps/docs/registry/examples/keys/use-focus-zone/use-focus-zone-tab-cycle.tsx is byte-IDENTICAL to the libs/keys source via diff -q), and apps/docs/src/generated/keys/demo-index.ts lazy-imports them. apps/docs/tsconfig.json include is ["**/*.ts","**/*.tsx"] with registry/examples NOT excluded, so they ARE type-checked: `tsc --noEmit --listFilesOnly` in apps/docs lists 20 files under registry/examples/keys including the new use-focus-zone-tab-cycle.tsx. Thus the apps/docs type-check gate (run by the SOTA turbo type-check) surfaces type errors in these examples. The candidate's premise that docs only synced use-presence is wrong — the full keys/ example tree is present. The proposed keys-tsconfig-include fix is redundant churn since the real consumer (apps/docs) already compiles them; the candidate even admits the files are currently type-correct. Rejected.
round: 1

### R-017 · simplicity · libs/ui/docs/content/getting-started/index.mdx:13-17
Claimed: getting-started/index.mdx Steps block duplicates install/CSS snippets from destination pages; refuted — Factual claim verified (the new <Steps> overview inlines `pnpm exec dgadd init`/`add ui/button` also at installation.mdx:112-113 and the two-line CSS `@import` block also at installation.mdx:125-126 and tailwind-setup.mdx:47-48). But this is intentional per the documented change-set intent (getting-started index pages are deliberately reworked into <Steps> walkthroughs, and steps.tsx is the new MDX block this change exists to use). Repeating short, stable, foundational commands across standalone orientation/install/setup doc pages is standard deliberate documentation practice — each page must stand alone for readers arriving via search, and the snippets serve different narrative positions (the installation.mdx version sits inside the publish-gated local-tarball flow, not the happy-path preview). DRY is a weak lens for human-facing prose, the maintenance-sync risk is speculative, and the proposed fix (strip code blocks from the orientation page) would reduce its usefulness. Churn, not improvement; reject.
round: 1

### R-018 · structure · libs/ui/docs/content/getting-started/installation.mdx:26-66
Claimed: Steps under markdown ## sections flatten the heading outline (all step titles render as h2); refuted — Mechanism verified (steps.tsx:48 renders Typography as="h2" id={slugify(title)}; toc.tsx:50-72 lists step h2s at TOC depth 2 alongside the parent ## heading). But rejected: (1) It is a deliberate, pervasive authoring pattern, not a defect on two cherry-picked pages — the committed apps/docs/content/docs/app/getting-started/{installation,quickstart,first-review}.mdx (all in the change set) use the SAME <Steps>-under-## structure, and Steps was purpose-built to render step titles as numbered h2 sections; the candidate's own 'index.mdx is unaffected' carve-out exposes the per-page IA choice. (2) Not a WCAG 1.3.1 skipped-level violation: h2 followed by sibling h2 step titles is valid, fully programmatically-determinable heading structure; the quality bar flags SKIPPED levels (h2->h4), which does not occur. No id collisions or broken anchors found. (3) The proposed fix (new heading-level prop, demote all step titles to h3, rewrite the MDX, adjust TOC depth) would churn the Steps contract, TOC logic, and many MDX files against a consistent intentional design and create inconsistency with the committed apps/docs Step pages unless those are also rewritten (scope creep). Rejected on intentional-convention and churn grounds.
round: 1

### R-019 · tests · libs/ui/docs/content/getting-started/tailwind-setup.mdx:15-55
Claimed: Step-based headings in changed getting-started MDX have no real-component TOC contract test; refuted — Factually accurate (MDX diffs convert `## N. ...` headings into <Step title="..."> blocks; Steps/Step are registered MDX components; Step renders h2 id={slugify(title)} at steps.tsx:50; toc.test.tsx:53-70 hand-writes pre-slugified ids and never imports/renders Step). BUT this is over-testing trivial, already-covered behavior. TableOfContentsPanel's contract is 'given h2[id]/h3[id] in #main-content, derive TOC entries' — that IS tested with real rendered headings (4 passing tests). The TOC does not depend on slugify; it reads whatever id exists in the DOM, so coupling Step+slugify+TOC in one test ties two units that communicate only through the DOM id attribute. slugify is a 6-line deterministic pure function used in exactly one place. All 28 distinct Step titles across the changed MDX slugify to non-empty valid ids; the only duplicate titles live on different pages (no intra-page collision). No current defect; a would-be slugify regression is best caught by a trivial unit test, not an integration test, and slugify is too simple to plausibly regress unnoticed. Speculative low-value test addition — reject.
round: 1

### R-020 · correctness · libs/ui/docs/content/utils/index.mdx:24
Claimed: utils/index.mdx composeRefs "ships with" list is inaccurate and materially incomplete vs registry.json; refuted — Factual enumeration confirmed (17 DIRECT compose-refs dependents in registry.json; menu and navigation-list declare compose-refs only transitively via listbox/floating-panel; the line-24 list is newly added). HOWEVER the prose does not claim DIRECTNESS — it says compose-refs is 'pulled in automatically when you add a component that depends on them' and 'ships with' the listed components. Adding menu or navigation-list DOES pull compose-refs into the consumer's project (transitively), so 'ships with' / 'pulled in automatically' is operationally TRUE for all 7 named components, and the user's actual decision ('do I still need a separate dgadd add ui/compose-refs?' -> no) is answered correctly. The list reads as illustrative (the preceding sentence states the general rule), not exhaustive, so omitting dialog/popover/switch is a reasonable editorial choice that at worst prompts a harmless idempotent re-add. The candidate's central objection (menu/navigation-list 'not DIRECT dependents') rests on a directness claim the prose never makes. Rewording an operationally-accurate illustrative list to satisfy a pedantic directness reading is churn, not improvement.
round: 1

### R-021 · dead-code · libs/ui/registry/lib/sidebar-variants.ts:80
Claimed: Redundant `bar` active-hover background override is a no-op class; refuted — Factually the CSS-resolution claim is correct (on an active+hovered `bar` row, `data-[active=true]:hover:bg-foreground/5` resolves to the identical `bg-foreground/5` already produced by base hover and the new always-on active rule, so it is a visual no-op). But rejected on three grounds: (1) NOT change-set-introduced — `git show HEAD:libs/ui/registry/lib/sidebar-variants.ts` shows the redundant token `data-[active=true]:hover:bg-foreground/5` already existed at HEAD (line 74) and was ALREADY a no-op there because base `hover:bg-foreground/5` covered every hovered row at the same `/5` value; the change set added a DIFFERENT class (`data-[active=true]:bg-foreground/5`) on the same string, it did not introduce the dead token. (2) Repeated intentional convention, NOT bar-specific — the identical redundant override appears on `caret` (line 55) and `bracket` (line 85), both with base `hover:bg-foreground/5` and no active-bg rule, so it is a deliberate cross-variant authoring template; the 'specific to bar' claim is wrong. (3) Pure churn that harms consistency — even the candidate concedes zero visual change; removing it from only `bar` breaks symmetry with caret/bracket, removing from all three is a broad style refactor outside scope (AGENTS.md 'Do not make broad refactors... unless the task requires them') and forces regenerating the committed public-registry handoff JSON for a no-op. The terminal contrast the candidate cites (line 99 base `hover:border-foreground/40` differs from line 101 active rail, so its override is load-bearing) is accurate and does not rescue the bar claim. Reject.
round: 1

### R-022 · tests · libs/registry/src/testing/docs-sync.test.ts:305-333
Claimed: extraRootPages tests assert meta.json strings but never prove authored non-artifact content survives the sync; refuted — Factually accurate at the cited lines (the two new tests assert only rootMeta.pages string arrays, and the 'appends' fixture never creates a content/docs/app/ dir). Verified production wiring (sync-artifacts.mjs:27-46 derives `...app` from enabled+!artifactSource; docs-libraries.json marks `app` authored; apps/docs/content/docs/app/** is git-tracked). BUT the proposed fix is speculative churn, not a real coverage gap. runDocsSyncPass does NOT own or write content/docs/app/ in the extraRootPages flow: `app` is never in `artifacts`, and the reset loop (sync-operations.ts:305-310) only resets join(contentDir, artifact.id) per artifact-id (ui, keys) — it never resets the root contentDir. The feature's actual contract is 'append these strings to meta.json pages', which IS directly tested. The proposed test would assert the ABSENCE of a behavior the function never performs, guarding a hypothetical future blanket-reset regression — contradicting the repo's testing philosophy (test user-visible behavior, not what-if-someone-changes-this-later). All 14 tests pass; no defect or untested load-bearing contract exists. Default-to-reject on a churn-only test addition.
round: 1

### R-023 · conventions · apps/docs/src/routes/$lib/$.tsx:191-207
Claimed: Footer pager renders "EOF" in the previous slot on the first page (inverted terminal metaphor; last page gets no marker); refuted — Factually accurate at the cited lines: DocsFooterPager ($lib/$.tsx:191-207) is new code (git show HEAD confirms no Pager/EOF/findPageNeighbors at HEAD) and renders the literal `EOF` in the previous slot when previous===null (first page), while the next slot (lines 209-223) has no else branch so the last page renders nothing; grep confirms EOF is the sole terminal marker. The asymmetry is real. BUT this is a subjective cosmetic/conventions opinion about a decorative, non-interactive placeholder, not a confirmable defect: (1) `EOF` can reasonably be read as a generic 'edge of the document set' marker, so the 'backwards metaphor' reading is one interpretation, not a clear error; the repo establishes no BOF/EOF placement rule. (2) The last-page 'no marker' is consistent with the committed pager-single example (libs/ui/registry/examples/pager/pager-single.tsx), which renders a single-slot Pager with no placeholder — so the absence on the last page matches an established convention, not a bug. (3) There is no functional, a11y, or contract breakage; the Pager keeps its aria-label and the EOF span carries no semantic role. (4) The proposed 'fix' offers three mutually-exclusive options (move EOF to last page + add BOF, or drop the placeholder), none clearly correct — a pure design judgment call. On a terminal-themed docs surface (terminal sidebar variant, `>` glyphs), an on-theme one-word placeholder is a stylistic choice. Per the default-to-reject charter, this info-level decorative opinion does not rise to a defect, and the fix is cosmetic churn.
round: 2

### R-024 · tests · apps/docs/scripts/sync-artifacts.mjs:27-29
Claimed: New authoredRootPages producer is open-coded in the sync entrypoint and untested, while its inverse getArtifactLibraries is an exported, unit-tested helper; refuted — Every factual claim verified: sync-artifacts.mjs:27-29 is the sole, newly-added, inline producer of authoredRootPages (`enabled && !artifactSource` → `...id`), wired to extraRootPages at line 46; it is the exact inverse of the exported, unit-tested getArtifactLibraries (config.mjs:87-89, tested in sync.test.ts:29-44); rg confirms zero test references to authoredRootPages/getAuthoredRootPages and no co-located test for sync-artifacts.mjs; the libs/registry docs-sync tests feed a literal `['...app']` and never exercise the producer. So this is a genuine coverage gap, not a live bug (current output `['...app']` is correct). Rejected on three grounds: (1) The proposed remedy — extract a 3-line, single-use predicate into a getAuthoredRootPages() helper — directly contradicts the repo's explicit anti-over-abstraction conventions (AGENTS.md/global CLAUDE.md: 'No premature abstractions — 3 similar lines > unnecessary helper function'; 'Do not extract a helper only because two files look similar'). The filter is used in exactly ONE place; the getArtifactLibraries symmetry argument does not establish reuse-driven duplication. (2) The producer's failure mode is already guarded by review: the committed, tracked root content/docs/meta.json pins `[...ui, ...keys, ...app]`, so any predicate regression visibly rewrites that snapshot on the next sync and surfaces in `git diff` during change-set review. (3) The substantive extraRootPages correctness risk (wrong/stale root nav) is already captured as confirmed F-018, whose recommended fix includes a re-sync regression test; adding a separate dedicated test for a one-line inline filter whose output is already snapshot-pinned is low-value test churn. Not a strict duplicate of F-018 (different mechanism — producer vs cache fingerprint), but rejected on convention/churn/low-residual-risk grounds.
round: 2

### R-025 · errors · libs/keys/docs/content/hooks/use-key.mdx:39
Claimed: use-key.mdx Behavior table omits "in the active scope" on the Declining row, implying cross-scope fall-through the dispatcher does not perform; refuted — Factually accurate that the new Behavior table (use-key.mdx:33-42, absent at HEAD) Declining row (line 40) says 'Return exactly false from a handler to fall through to the next lower-priority handler for that combo' and omits the 'in the active scope' qualifier present in the page's own Notes source (use-key.ts:93). The dispatcher (keyboard-provider.tsx:137-168) confirms decline never crosses scopes: handleKeyDown resolves a single activeRegistrationScope = getActiveScope(), reads only that scope's handler map (line 143), and the inner DECLINE `continue` (line 162) cascades only within that scope's entries array; no walk-up to shallower scopes. Not a duplicate (F-007 and F-014 cover the use-key/use-scope EXAMPLES, not the Behavior-table copy). Rejected because the table statement is accurate-if-less-precise, not false: within a single scope multiple registrations for the same combo ARE iterated in reverse (priority) order, so 'the next lower-priority handler for that combo' correctly describes within-scope siblings — exactly what the dispatcher does. The claimed cross-scope inference requires the reader to mis-combine two rows in a way neither asserts: the adjacent 'Scope resolution' row (line 39) describes single-winner-scope semantics ('the handler in the deepest active scope runs first'), not a fallback chain to shallower scopes. Since the statement is correct for the actual within-scope behavior and the precise contract is already rendered on the same page via Notes, this is an info-level wording-preference to mirror the Notes phrasing, not a confirmable error. Per the default-to-reject charter (statement not false; cross-scope reading is a stretch; fix is cosmetic), reject.
round: 2

### R-026 · slop · libs/ui/docs/content/utils/index.mdx:42
Claimed: utils/index.mdx falsely claims cn() is "imported by every component"; refuted — The candidate's central factual claim is demonstrably FALSE. It asserts 'Only 16 of 48 component directories actually call cn(); the other 33 (accordion, avatar, breadcrumbs, callout, card, checkbox, dialog, menu, select, tabs, tooltip, popover, radio, sidebar...) never import or invoke cn at all.' Measured reality: 47 of 48 component directories under libs/ui/registry/ui (excluding shared/) use cn() in non-test tsx; the SINGLE exception is diff-view (a thin wrapper delegating to diff-view-line/split/unified subcomponents). 143 non-test tsx files import `cn`. Every directory the candidate names as a non-user in fact uses cn() — accordion 5 files, avatar 5, card 8, dialog 9, menu 9, select 8, tabs 4, sidebar 11, breadcrumbs 4, checkbox 2, radio 2, tooltip 1, popover 1 — all directly `import { cn } from "@/lib/utils"`. Confirmed line 42 ('imported by every component') is new at HEAD (the 'Other registry utilities' section is entirely new), so it is in scope, but the prose is essentially correct: cn is used by ~98% of component directories, and the lone exception is a composition wrapper whose subcomponents use it. The proposed reword ('used by components that merge conditional classes') would churn near-universally-true documentation prose on the basis of a false count. Distinct line from rejected R-020 (line 24, composeRefs list), but rejected here on the merits because the refutation rests on the candidate's miscount being wrong, not a convention defense.
round: 2
